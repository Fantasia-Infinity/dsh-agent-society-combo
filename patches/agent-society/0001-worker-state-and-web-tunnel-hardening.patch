diff --git a/.env.agent.example b/.env.agent.example
index 1da64d0..7162c98 100644
--- a/.env.agent.example
+++ b/.env.agent.example
@@ -1,6 +1,7 @@
-# Running ./agent asks for these two non-secret model values and the API key.
-# Only non-secret values and credential references are written to .env.agent.
-# Canonical location: <repo>/.private/env/agent.env (legacy .env.agent still read).
+# `agent setup` asks for these non-secret model values and the API key.
+# Canonical location: ~/.config/agentsociety/agent.env on macOS/Linux, or
+# %APPDATA%\\AgentSociety\\agent.env on Windows. AGENT_ENV_FILE and
+# `agent setup --config` can override it. Project files remain legacy-compatible.
 AGENT_REMOTE_BASE_URL=https://api.example.com/v1
 AGENT_REMOTE_MODEL=your-model-id
 
@@ -15,8 +16,8 @@ AGENT_REMOTE_MODEL=your-model-id
 # AGENT_HUB_NODE_TOKEN=
 
 # Everything below is optional. The defaults derive identity from the signed-in
-# user and hostname, use the cloned repository as workspace, and store Pi sessions
-# below the user's home directory.
+# user and hostname, use the user's home directory as workspace, and store Pi
+# sessions below the user's home directory.
 # AGENT_PRINCIPAL_ID=human-owner
 # AGENT_PRINCIPAL_NAME=Owner
 # AGENT_ACTOR_ID=pi-macbook
@@ -143,7 +144,12 @@ AGENT_REMOTE_MODEL=your-model-id
 # AGENT_DSH_WEB_SEARCH=0
 
 # Setup generates these non-secret references automatically. Credentials live in
-# macOS Keychain, Windows Credential Manager, or Linux Secret Service.
+# macOS Keychain, Windows Credential Manager, or Linux Secret Service. If no
+# system store is available, setup defaults to environment-only credentials.
+# Explicit `agent setup --credentials file` writes a separate 0600 file and
+# doctor reports the persistence risk.
+# AGENT_CREDENTIAL_BACKEND=keychain
+# AGENT_CREDENTIAL_FILE=/absolute/path/to/agentsociety/credentials.env
 # AGENT_REMOTE_API_KEY_CREDENTIAL_SERVICE="AgentSociety Remote LLM API"
 # AGENT_REMOTE_API_KEY_CREDENTIAL_ACCOUNT=owner
 # AGENT_HUB_TOKEN_CREDENTIAL_SERVICE="AgentSociety Hub"
diff --git a/README.en.md b/README.en.md
index d5cd66c..c50ecd3 100644
--- a/README.en.md
+++ b/README.en.md
@@ -131,22 +131,35 @@ sh scripts/install-dsh-plugin.sh
 ./agent web           # dsh Web UI (same ~/.dsh sessions)
 ```
 
-The first run guides you through model connection settings (OpenAI-compatible
-URL, Model ID, API Key), then installs dependencies, builds, generates a local
-identity, and opens the TUI. Hub connection is optional and can be added later:
+The first run uses `agent setup` for configuration and does not require
+changing into the AgentSociety checkout. Choose one of local, Hub worker, or
+dispatch-only mode:
 
 ```bash
-./agent setup         # reconfigure; add Hub URL / username / password
-./agent connect       # exchange Hub credentials for a node-scoped credential
-./agent web           # browser UI; pass --port to change the port
-./agent worker        # run as a resident worker claiming Hub tasks
-./agent doctor        # health check: Hub, workspace, model, sessions
+agent setup --mode local          # local use
+agent setup --mode hub-worker     # configure Hub and register this worker
+agent setup --mode dispatch-only  # dispatch tasks without receiving them
+agent configure                   # alias for setup
+agent bootstrap                   # explicitly repair/rebuild the AgentSociety runtime
+agent connect                     # re-register/rotate the node credential
+agent web                         # browser UI
+agent worker                      # resident Hub worker
+agent doctor                      # static checks; --live enables network tests
 ```
 
-Non-secret configuration lives in the Git-ignored `.private/env/agent.env`
-(mode 0600); API keys and Hub credentials are stored only in the system
-credential store (macOS Keychain / Windows Credential Manager / Linux Secret
-Service), never as plaintext in config files.
+Non-secret configuration defaults to `~/.config/agentsociety/agent.env`
+(Windows: `%APPDATA%\\AgentSociety\\agent.env`). Use `--config` or
+`AGENT_ENV_FILE` to override it. Legacy `.private/env/agent.env` and
+`.env.agent` files remain readable and are migrated without deletion.
+New workspaces default to the user's home directory, never the managed checkout.
+
+API keys and Hub credentials prefer the system credential store (macOS Keychain,
+Windows Credential Manager, or Linux Secret Service). On a headless host without
+one, setup reads environment variables only and does not write secrets. Explicit
+`agent setup --credentials file` enables a 0600 file fallback, which doctor
+continues to report as a security warning. Non-interactive setup accepts secrets
+through environment variables or that explicit file mode, never command-line
+arguments.
 
 ### 3. Dispatch your first task
 
diff --git a/README.md b/README.md
index 1c1d308..e73d876 100644
--- a/README.md
+++ b/README.md
@@ -123,21 +123,31 @@ sh scripts/install-dsh-plugin.sh
 ./agent web           # dsh Web UI（同一 ~/.dsh session）
 ```
 
-首次运行会引导你填写模型连接信息（OpenAI-compatible URL、Model ID、API Key），
-然后自动安装依赖、构建、生成本机身份并打开 TUI。Hub 连接是可选的，之后随时
-补充：
+首次运行用 `agent setup` 完成配置，不需要先进入 AgentSociety 源码目录。向导会
+先选择使用模式：
 
 ```bash
-./agent setup         # 重新配置，可补填 Hub URL / 用户名 / 密码
-./agent connect       # 用 Hub 账号密码换本机节点凭据
-./agent web           # 浏览器 UI（--port 可改端口）
-./agent worker        # 作为常驻 worker 开始领取 Hub 任务
-./agent doctor        # 体检：Hub 连接、workspace、模型、session
+agent setup --mode local          # 本机使用
+agent setup --mode hub-worker     # 配置 Hub 并注册本机 worker
+agent setup --mode dispatch-only  # 可派发任务，但不接收远程任务
+agent configure                   # setup 的同义别名
+agent bootstrap                   # 明确修复/重建 AgentSociety runtime
+agent connect                     # 需要时重新注册节点凭据
+agent web                         # 浏览器 UI
+agent worker                      # 作为常驻 worker 领取 Hub 任务
+agent doctor                      # 静态检查；agent doctor --live 才访问网络/模型
 ```
 
-非敏感配置保存在被 Git 忽略的 `.private/env/agent.env`（权限 0600）；API Key 和
-Hub 凭据只存入系统凭据库（macOS Keychain / Windows Credential Manager / Linux
-Secret Service），配置文件里没有明文密钥。
+非敏感配置默认保存在用户级 `~/.config/agentsociety/agent.env`（Windows 为
+`%APPDATA%\\AgentSociety\\agent.env`），也可以用 `--config` 或
+`AGENT_ENV_FILE` 指定。旧的 `.private/env/agent.env` / `.env.agent` 会自动发现，
+迁移时不删除旧文件。workspace 默认是当前用户 home，不会指向受管源码 checkout。
+
+凭据优先使用 macOS Keychain、Windows Credential Manager 或 Linux Secret Service。
+无头环境没有可用凭据库时默认只读取环境变量；只有显式
+`agent setup --credentials file` 才会创建权限为 `0600` 的凭据文件，doctor 会
+持续显示风险提示。非交互模式请使用环境变量或显式凭据文件，不要把 API key 或密码
+放进命令行参数。
 
 ### 3. 派发你的第一个任务
 
diff --git a/agent b/agent
index b510191..91a012f 100755
--- a/agent
+++ b/agent
@@ -45,30 +45,60 @@ install_global_command() {
         ln -s "$repository_root/agent" "$global_command"
     fi
 
-    echo "Global command available as: $global_command"
+    echo "Global command available as: $global_command" >&2
     case ":${PATH:-}:" in
         *:"$global_bin":*) ;;
         *) echo "Add $global_bin to PATH to run 'agent' from any directory." >&2 ;;
     esac
 }
 
-if [ "$command" = "setup" ]; then
+agent_config_path() {
+    if [ -n "${AGENT_ENV_FILE:-}" ]; then
+        printf '%s\n' "$AGENT_ENV_FILE"
+    elif [ -n "${XDG_CONFIG_HOME:-}" ]; then
+        printf '%s\n' "$XDG_CONFIG_HOME/agentsociety/agent.env"
+    else
+        printf '%s\n' "${HOME}/.config/agentsociety/agent.env"
+    fi
+}
+
+has_agent_config() {
+    config_path=$(agent_config_path)
+    [ -f "$config_path" ] || \
+      [ -f "$repository_root/.private/env/agent.env" ] || \
+      [ -f "$repository_root/.env.agent" ]
+}
+
+bootstrap_if_needed() {
+    if [ ! -d "$agent_host/node_modules" ] || [ ! -f "$agent_host/dist/src/cli.js" ]; then
+        npm --prefix "$agent_host" run bootstrap
+    fi
+}
+
+bootstrap_runtime() {
+    npm --prefix "$agent_host" run bootstrap
+}
+
+if [ "$command" = "bootstrap" ] || [ "$command" = "repair" ]; then
+    bootstrap_runtime
+    install_global_command
+    exit 0
+fi
+
+if [ "$command" = "setup" ] || [ "$command" = "configure" ]; then
+    bootstrap_if_needed
     npm --prefix "$agent_host" run setup -- "$@"
     install_global_command
     exit 0
 fi
 
 did_setup=0
-if [ ! -f "$agent_host/.setup-complete" ] || \
-   { [ ! -f "$repository_root/.env.agent" ] && [ ! -f "$repository_root/.private/env/agent.env" ]; } || \
-   [ ! -d "$agent_host/node_modules" ] || [ ! -f "$agent_host/dist/src/cli.js" ]; then
+bootstrap_if_needed
+if [ "$command" != "doctor" ] && [ "$command" != "sessions" ] && ! has_agent_config; then
     npm --prefix "$agent_host" run setup
     did_setup=1
 fi
 install_global_command
-if [ "$command" = "doctor" ] && [ "$did_setup" -eq 1 ]; then
-    exit 0
-fi
 
 case "$command" in
     tui|interactive|local)
@@ -83,14 +113,17 @@ case "$command" in
     web-bridge)
         exec npm --prefix "$agent_host" run web-bridge -- "$@"
         ;;
-    doctor|sessions|dsh-doctor)
-        exec npm --prefix "$agent_host" run "$command"
+    doctor)
+        exec node "$agent_host/dist/src/cli.js" doctor "$@"
+        ;;
+    dsh-doctor|sessions)
+        exec npm --prefix "$agent_host" run start -- "$command" "$@"
         ;;
     register|once|observe|attach|bridge|connect|steer|follow-up|cancel|control|dsh-worker|dsh-once|dsh-dispatch|dsh-doctor)
         exec npm --prefix "$agent_host" run start -- "$command" "$@"
         ;;
     *)
-        echo "Usage: ./agent [setup|tui|web|web-bridge|local|worker|connect|bridge --adapter ID|doctor|dsh-doctor|sessions|register|once|observe ID|attach ID|steer|follow-up|cancel|control|dsh-worker|dsh-once|dsh-dispatch]" >&2
+        echo "Usage: ./agent [setup|configure|bootstrap|repair|tui|web|web-bridge|local|worker|connect|bridge --adapter ID|doctor [--live] [--json]|dsh-doctor|sessions|register|once|observe ID|attach ID|steer|follow-up|cancel|control|dsh-worker|dsh-once|dsh-dispatch]" >&2
         exit 2
         ;;
 esac
diff --git a/agent-host/package.json b/agent-host/package.json
index 9b347c4..ec9e426 100644
--- a/agent-host/package.json
+++ b/agent-host/package.json
@@ -9,6 +9,7 @@
   },
   "scripts": {
     "setup": "node scripts/setup.mjs",
+    "bootstrap": "node scripts/setup.mjs --bootstrap",
     "build": "tsc -p tsconfig.json",
     "check": "tsc -p tsconfig.json --noEmit",
     "apply-security-patches": "node scripts/patch-pi-brace-expansion.mjs",
diff --git a/agent-host/scripts/mcp-hub-wrapper.mjs b/agent-host/scripts/mcp-hub-wrapper.mjs
index a8b96f8..deb8623 100644
--- a/agent-host/scripts/mcp-hub-wrapper.mjs
+++ b/agent-host/scripts/mcp-hub-wrapper.mjs
@@ -19,16 +19,26 @@
 //   AGENT_HUB_MCP_KEYCHAIN_SERVICE keychain service override
 //   AGENT_HUB_MCP_KEYCHAIN_ACCOUNT keychain account override
 import { execFileSync } from "node:child_process";
-import { readFileSync } from "node:fs";
+import { existsSync, readFileSync, statSync } from "node:fs";
+import { homedir } from "node:os";
+import { join, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 
 function loadProjectEnv() {
   const candidates = [
+    process.env.AGENT_ENV_FILE,
+    process.env.XDG_CONFIG_HOME
+      ? join(process.env.XDG_CONFIG_HOME, "agentsociety", "agent.env")
+      : join(homedir(), ".config", "agentsociety", "agent.env"),
+    process.env.APPDATA
+      ? join(process.env.APPDATA, "AgentSociety", "agent.env")
+      : undefined,
     fileURLToPath(new URL("../../.private/env/agent.env", import.meta.url)),
     fileURLToPath(new URL("../../.env.agent", import.meta.url)),
   ];
   const values = {};
   for (const path of candidates) {
+    if (!path) continue;
     try {
       for (const raw of readFileSync(path, "utf8").split(/\r?\n/)) {
         const line = raw.trim();
@@ -54,6 +64,11 @@ function loadProjectEnv() {
 }
 
 const projectEnv = loadProjectEnv();
+const credentialBackend =
+  process.env.AGENT_CREDENTIAL_BACKEND || projectEnv.AGENT_CREDENTIAL_BACKEND || "keychain";
+const credentialEnv = projectEnv.AGENT_CREDENTIAL_FILE
+  ? loadCredentialEnv(resolve(projectEnv.AGENT_CREDENTIAL_FILE))
+  : {};
 
 const service =
   process.env.AGENT_HUB_MCP_KEYCHAIN_SERVICE ||
@@ -71,7 +86,7 @@ const hubUrl = (
 
 if (!hubUrl) {
   console.error(
-    "AGENT_HUB_MCP_URL is required (or AGENT_HUB_URL in .private/env/agent.env); " +
+    "AGENT_HUB_MCP_URL is required (or AGENT_HUB_URL in the AgentSociety config); " +
       "run `agent connect` first.",
   );
   process.exit(1);
@@ -89,20 +104,68 @@ if (url.protocol !== "https:") {
   process.exit(1);
 }
 
-let token = "";
-try {
-  const args = ["find-generic-password", "-s", service, "-w"];
-  if (account) args.push("-a", account);
-  token = execFileSync("/usr/bin/security", args, { encoding: "utf8" }).trim();
-} catch {
+const token = await readNodeCredential();
+if (!token) {
   console.error(
-    "Hub node credential not found in the keychain; run `agent connect` first.",
+    "Hub node credential not found in the configured backend; run `agent connect` first.",
   );
   process.exit(1);
 }
-if (!token) {
-  console.error("Hub node credential is empty.");
-  process.exit(1);
+
+function loadCredentialEnv(path) {
+  if (!existsSync(path)) return {};
+  if (process.platform !== "win32" && (statSync(path).mode & 0o777) !== 0o600) {
+    console.error(`Credential file must have permissions 0600: ${path}`);
+    process.exit(1);
+  }
+  return parseEnv(readFileSync(path, "utf8"));
+}
+
+async function readNodeCredential() {
+  const configured =
+    process.env.AGENT_HUB_NODE_TOKEN?.trim() ||
+    process.env.AGENT_HUB_TOKEN?.trim() ||
+    credentialEnv.AGENT_HUB_NODE_TOKEN?.trim() ||
+    credentialEnv.AGENT_HUB_TOKEN?.trim();
+  if (configured) return configured;
+  if (credentialBackend === "env" || credentialBackend === "file") return "";
+  return readSystemCredential();
+}
+
+async function readSystemCredential() {
+  try {
+    const { Entry } = await import("@napi-rs/keyring");
+    return new Entry(service, account).getPassword()?.trim() || "";
+  } catch {
+    if (process.platform !== "darwin") return "";
+    try {
+      const args = ["find-generic-password", "-s", service, "-w"];
+      if (account) args.push("-a", account);
+      return execFileSync("/usr/bin/security", args, { encoding: "utf8" }).trim();
+    } catch {
+      return "";
+    }
+  }
+}
+
+function parseEnv(text) {
+  const values = {};
+  for (const raw of text.split(/\r?\n/)) {
+    const line = raw.trim();
+    if (!line || line.startsWith("#")) continue;
+    const eq = line.indexOf("=");
+    if (eq <= 0) continue;
+    const key = line.slice(0, eq).trim();
+    let value = line.slice(eq + 1).trim();
+    if (
+      (value.startsWith('"') && value.endsWith('"')) ||
+      (value.startsWith("'") && value.endsWith("'"))
+    ) {
+      value = value.slice(1, -1);
+    }
+    values[key] = value;
+  }
+  return values;
 }
 
 process.stdin.setEncoding("utf8");
diff --git a/agent-host/scripts/setup.mjs b/agent-host/scripts/setup.mjs
index 7dddd11..5a7b36d 100755
--- a/agent-host/scripts/setup.mjs
+++ b/agent-host/scripts/setup.mjs
@@ -6,167 +6,331 @@ import {
   mkdirSync,
   readFileSync,
   renameSync,
+  statSync,
   writeFileSync,
 } from "node:fs";
 import { createInterface } from "node:readline/promises";
 import { stdin, stdout } from "node:process";
-import { dirname, resolve } from "node:path";
+import { dirname, join, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 import { spawnSync } from "node:child_process";
-import { userInfo } from "node:os";
+import { homedir, userInfo } from "node:os";
 import { Writable } from "node:stream";
 
 const agentHostDir = resolve(dirname(fileURLToPath(import.meta.url)), "..");
 const repositoryRoot = resolve(agentHostDir, "..");
-const legacyConfigPath = resolve(repositoryRoot, ".env.agent");
-const privateConfigPath = resolve(
-  repositoryRoot,
-  ".private",
-  "env",
-  "agent.env",
-);
+const args = parseArguments(process.argv.slice(2));
 const configPath = resolve(
-  process.env.AGENT_SETUP_CONFIG_PATH ||
-    (existsSync(privateConfigPath)
-      ? privateConfigPath
-      : existsSync(legacyConfigPath)
-        ? legacyConfigPath
-        : privateConfigPath),
-);
-const workspaceRoot = resolve(
-  process.env.AGENT_SETUP_WORKSPACE || repositoryRoot,
+  args.config ||
+    process.env.AGENT_SETUP_CONFIG_PATH ||
+    process.env.AGENT_ENV_FILE ||
+    defaultAgentConfigPath(),
 );
+const legacyConfigPaths = [
+  resolve(repositoryRoot, ".private", "env", "agent.env"),
+  resolve(repositoryRoot, ".env.agent"),
+];
 const completionMarker = resolve(agentHostDir, ".setup-complete");
-const configureOnly = process.argv.includes("--configure-only");
 
 async function main() {
   assertNodeVersion();
-  if (!configureOnly) {
-    run(npmCommand(), ["ci", "--ignore-scripts"]);
-    run(process.execPath, ["scripts/patch-pi-brace-expansion.mjs"]);
-    run(process.execPath, ["scripts/patch-pi-brace-expansion.mjs", "--check"]);
-  }
-  const existingText = existsSync(configPath)
-    ? normalizeConfigHeader(readFileSync(configPath, "utf8"))
-    : "# Generated by AgentSociety setup. Secret values live in the system credential store.\n";
+  if (args.bootstrap) {
+    bootstrapRuntime();
+    return;
+  }
+
+  const sourcePath =
+    existsSync(configPath)
+      ? configPath
+      : args.explicitConfig
+        ? undefined
+        : legacyConfigPaths.find((path) => existsSync(path));
+  const existingText = sourcePath
+    ? normalizeConfigHeader(readFileSync(sourcePath, "utf8"))
+    : "# Generated by AgentSociety setup. Non-secret settings live here; secrets use the selected credential backend.\n";
   const existing = parseEnv(existingText);
-  const credentials = await createCredentialStore();
-  const { values, hubPasswordMode } = await collectValues(existing, credentials);
-  writeConfig(existingText, values);
+  const workspaceRoot = resolve(
+    process.env.AGENT_SETUP_WORKSPACE?.trim() ||
+      existing.AGENT_WORKSPACE_ROOT?.trim() ||
+      homedir(),
+  );
+  mkdirSync(workspaceRoot, { recursive: true, mode: 0o700 });
+
+  const credentials = await createCredentialStore(
+    args.credentials,
+    configPath,
+    existing.AGENT_CREDENTIAL_FILE,
+  );
+  const { values, hubPasswordMode, mode } = await collectValues(
+    existing,
+    credentials,
+    workspaceRoot,
+  );
+  writeConfig(existingText, values, configPath);
+  writeFileSync(completionMarker, `${new Date().toISOString()}\n`, {
+    encoding: "utf8",
+    mode: 0o600,
+  });
+  chmodSync(completionMarker, 0o600);
+
   console.log(`\nSaved non-secret Agent configuration to ${configPath}`);
+  if (sourcePath && sourcePath !== configPath) {
+    console.log(
+      `Migrated readable settings from legacy config ${sourcePath}; the legacy file was kept unchanged.`,
+    );
+  }
+  console.log(`Credential backend: ${credentials.summary}`);
+  if (credentials.backend === "file") {
+    console.warn(
+      `WARNING: credentials are persisted in ${credentials.credentialFile} with mode 0600.`,
+    );
+  } else if (credentials.backend === "env") {
+    console.warn(
+      "Credentials are environment-only and will not survive a new shell. Export them before starting AgentSociety.",
+    );
+  }
+  console.log(`Mode: ${mode}`);
+  console.log(`Permission: ${values.AGENT_DSH_PERMISSION_MODE}`);
+  if (mode === "hub-worker" && hubPasswordMode && !args.configureOnly) {
+    process.env.AGENT_ENV_FILE = configPath;
+    console.log("Registering this machine as a Hub worker...");
+    run(process.execPath, ["dist/src/cli.js", "connect", "--non-interactive"]);
+    console.log("Hub node credential registered and saved.");
+  } else if (mode === "hub-worker") {
+    console.log("Next: agent connect   # register this machine as a Hub worker");
+  }
+  console.log("Next: agent doctor    # static checks; add --live for network tests");
+  console.log("Next: agent web       # optional local DSH Web bridge");
+}
 
-  if (!configureOnly) {
-    run(npmCommand(), ["run", "build"]);
-    run(process.execPath, ["dist/src/cli.js", "doctor"]);
-    if (hubPasswordMode) {
-      run(process.execPath, ["dist/src/cli.js", "connect"]);
+function parseArguments(argv) {
+  const result = {
+    mode: process.env.AGENT_SETUP_MODE?.trim() || "",
+    config: process.env.AGENT_SETUP_CONFIG_PATH?.trim() || "",
+    credentials: process.env.AGENT_SETUP_CREDENTIALS?.trim() || "auto",
+    nonInteractive: false,
+    bootstrap: false,
+    configureOnly: false,
+    explicitConfig: Boolean(
+      process.env.AGENT_SETUP_CONFIG_PATH?.trim() ||
+        process.env.AGENT_ENV_FILE?.trim(),
+    ),
+  };
+  for (let index = 0; index < argv.length; index += 1) {
+    const arg = argv[index];
+    const next = argv[index + 1];
+    if (arg === "--mode") {
+      result.mode = next || "";
+      index += 1;
+    } else if (arg?.startsWith("--mode=")) {
+      result.mode = arg.slice("--mode=".length);
+    } else if (arg === "--config") {
+      result.config = next || "";
+      result.explicitConfig = true;
+      index += 1;
+    } else if (arg?.startsWith("--config=")) {
+      result.config = arg.slice("--config=".length);
+      result.explicitConfig = true;
+    } else if (arg === "--credentials") {
+      result.credentials = next || "";
+      index += 1;
+    } else if (arg?.startsWith("--credentials=")) {
+      result.credentials = arg.slice("--credentials=".length);
+    } else if (arg === "--non-interactive") {
+      result.nonInteractive = true;
+    } else if (arg === "--bootstrap" || arg === "--repair") {
+      result.bootstrap = true;
+    } else if (arg === "--configure-only") {
+      // Kept for existing scripts and tests. Setup is configuration-only now.
+      result.configureOnly = true;
+    } else if (arg === "--help" || arg === "-h") {
+      console.log(`Usage: agent setup [options]
+
+Options:
+  --mode local|hub-worker|dispatch-only
+  --config <path>                 user configuration file override
+  --credentials auto|env|file     credential backend (file is explicit and 0600)
+  --non-interactive               require values from environment/configuration
+  --bootstrap                     install/build the AgentSociety runtime once
+  --repair                        alias for --bootstrap
+`);
+      process.exit(0);
+    } else if (arg) {
+      throw new Error(`Unknown setup option: ${arg}`);
     }
-    writeFileSync(completionMarker, `${new Date().toISOString()}\n`, {
-      encoding: "utf8",
-      mode: 0o600,
-    });
-    chmodSync(completionMarker, 0o600);
-    console.log("\nAgent setup is complete. Run ./agent to open the DeepSeek Harness TUI (falls back to Pi).");
   }
+  if (!result.config && process.env.AGENT_ENV_FILE?.trim()) {
+    result.config = process.env.AGENT_ENV_FILE.trim();
+    result.explicitConfig = true;
+  }
+  if (!["auto", "env", "file"].includes(result.credentials)) {
+    throw new Error("--credentials must be auto, env, or file");
+  }
+  if (
+    result.mode &&
+    !["local", "hub-worker", "dispatch-only"].includes(result.mode)
+  ) {
+    throw new Error("--mode must be local, hub-worker, or dispatch-only");
+  }
+  return result;
+}
+
+function defaultAgentConfigPath() {
+  const configHome =
+    process.platform === "win32"
+      ? process.env.APPDATA?.trim() || join(homedir(), "AppData", "Roaming")
+      : process.env.XDG_CONFIG_HOME?.trim() || join(homedir(), ".config");
+  return join(configHome, "agentsociety", "agent.env");
+}
+
+function bootstrapRuntime() {
+  run(npmCommand(), ["ci", "--ignore-scripts"]);
+  run(process.execPath, ["scripts/patch-pi-brace-expansion.mjs"]);
+  run(process.execPath, ["scripts/patch-pi-brace-expansion.mjs", "--check"]);
+  run(npmCommand(), ["run", "build"]);
+  console.log("AgentSociety runtime bootstrap is complete.");
+}
+
+function interactiveTerminal() {
+  return Boolean(!args.nonInteractive && stdin.isTTY && stdout.isTTY);
 }
 
 function normalizeConfigHeader(text) {
-  return text.replace(
-    "# Generated by AgentSociety setup. This file contains secrets; do not commit it.",
-    "# Generated by AgentSociety setup. Secret values live in the system credential store.",
-  );
+  return text
+    .replace(
+      "# Generated by AgentSociety setup. This file contains secrets; do not commit it.",
+      "# Generated by AgentSociety setup. Non-secret settings live here; secrets use the selected credential backend.",
+    )
+    .replace(
+      "# Generated by AgentSociety setup. Secret values live in the system credential store.",
+      "# Generated by AgentSociety setup. Non-secret settings live here; secrets use the selected credential backend.",
+    );
 }
 
-async function collectValues(existingValues, credentials) {
+async function collectValues(existingValues, credentials, workspaceRoot) {
   const namespace =
     process.env.AGENT_CREDENTIAL_NAMESPACE?.trim() || "AgentSociety";
-  const keys = [
-    {
-      key: "AGENT_REMOTE_BASE_URL",
-      label: "OpenAI-compatible model URL",
-      validate: (value) => validateUrl(value, "Model URL"),
-    },
-    {
-      key: "AGENT_REMOTE_MODEL",
-      label: "Model ID",
-    },
-  ];
-  const values = {};
-  let hubPasswordMode = false;
-  const interactive = Boolean(stdin.isTTY && stdout.isTTY);
+  const interactive = interactiveTerminal();
   const mutedOutput = new MutedOutput();
   const terminal = interactive
     ? createInterface({ input: stdin, output: mutedOutput, terminal: true })
     : undefined;
-  try {
-    for (const item of keys) {
-      const current = process.env[item.key]?.trim() || existingValues[item.key] || "";
-      let value = current;
-      if (interactive) {
-        value = await askVisible(terminal, item.label, current);
-      }
-      if (!value || looksLikePlaceholder(value)) {
-        throw new Error(`${item.label} is required`);
-      }
-      rejectUnsupportedEnvCharacters(item.label, value);
-      item.validate?.(value);
-      values[item.key] = value;
-    }
+  const values = {
+    AGENT_CREDENTIAL_BACKEND: credentials.backend,
+    AGENT_CREDENTIAL_FILE:
+      credentials.backend === "file" ? credentials.credentialFile : null,
+    AGENT_HUB_NODE_TOKEN: null,
+  };
+  let hubPasswordMode = false;
 
-    const modelCredential = credentialReference(existingValues, "model");
-    const configuredModelKey =
-      process.env.AGENT_REMOTE_API_KEY?.trim() ||
-      process.env.LLM_API_KEY?.trim() ||
-      existingValues.AGENT_REMOTE_API_KEY ||
-      existingValues.LLM_API_KEY ||
-      credentials.read(modelCredential, "remote model") ||
+  try {
+    const mode = await selectMode(existingValues, terminal, interactive);
+    const existingRemoteBase =
+      process.env.AGENT_REMOTE_BASE_URL?.trim() ||
+      process.env.LLM_BASE_URL?.trim() ||
+      existingValues.AGENT_REMOTE_BASE_URL ||
+      existingValues.LLM_BASE_URL ||
+      "";
+    const existingRemoteModel =
+      process.env.AGENT_REMOTE_MODEL?.trim() ||
+      process.env.LLM_MODEL?.trim() ||
+      existingValues.AGENT_REMOTE_MODEL ||
+      existingValues.LLM_MODEL ||
       "";
-    const modelApiKey = interactive
-      ? await askSecret(
+    const existingPiProvider =
+      process.env.PI_PROVIDER?.trim() || existingValues.PI_PROVIDER || "";
+    const existingPiModel =
+      process.env.PI_MODEL?.trim() || existingValues.PI_MODEL || "";
+    const usePiConfig =
+      Boolean(existingPiProvider && existingPiModel) &&
+      !existingRemoteBase &&
+      !existingRemoteModel;
+
+    if (mode === "local" || mode === "hub-worker" || mode === "dispatch-only") {
+      if (usePiConfig) {
+        values.PI_PROVIDER = existingPiProvider;
+        values.PI_MODEL = existingPiModel;
+        values.AGENT_REMOTE_BASE_URL = null;
+        values.AGENT_REMOTE_MODEL = null;
+      } else {
+        const provider = existingRemoteBase && existingRemoteModel
+          ? "existing"
+          : await selectProvider(terminal, interactive);
+        const model = await collectModelValues(
+          provider,
+          existingRemoteBase,
+          existingRemoteModel,
+          terminal,
+          interactive,
+        );
+        values.AGENT_REMOTE_BASE_URL = model.baseUrl;
+        values.AGENT_REMOTE_MODEL = model.model;
+        values.PI_PROVIDER = null;
+        values.PI_MODEL = null;
+
+        const modelCredential = credentialReference(existingValues, "model");
+        const configuredModelKey = configuredSecret(
+          existingValues,
+          credentials,
+          modelCredential,
+          "AGENT_REMOTE_API_KEY",
+          "LLM_API_KEY",
+          "remote model",
+        );
+        const modelApiKey = await obtainSecret(
           terminal,
           mutedOutput,
           "Model API key",
           configuredModelKey,
-        )
-      : configuredModelKey;
-    if (!modelApiKey || looksLikePlaceholder(modelApiKey)) {
-      throw new Error("Model API key is required");
-    }
-    rejectUnsupportedEnvCharacters("Model API key", modelApiKey);
-    credentials.write(modelCredential, modelApiKey, "remote model");
-    values.AGENT_REMOTE_API_KEY = null;
-    values.LLM_API_KEY = null;
-    values.AGENT_REMOTE_API_KEY_KEYCHAIN_SERVICE = null;
-    values.AGENT_REMOTE_API_KEY_KEYCHAIN_ACCOUNT = null;
-    values.AGENT_REMOTE_API_KEY_CREDENTIAL_SERVICE = modelCredential.service;
-    values.AGENT_REMOTE_API_KEY_CREDENTIAL_ACCOUNT = modelCredential.account;
-
-    const configuredHubUrl =
-      process.env.AGENT_HUB_URL?.trim() || existingValues.AGENT_HUB_URL || "";
-    let hubUrl = configuredHubUrl;
-    if (interactive) {
-      const hint = configuredHubUrl
-        ? `${configuredHubUrl}; Enter keeps it, - disables`
-        : "optional; Enter skips";
-      const answer = (await terminal.question(`Hub URL [${hint}]: `)).trim();
-      hubUrl = answer === "-" ? "" : answer || configuredHubUrl;
+          credentials,
+          "AGENT_REMOTE_API_KEY",
+        );
+        rejectUnsupportedEnvCharacters("Model API key", modelApiKey);
+        credentials.write(
+          modelCredential,
+          modelApiKey,
+          "remote model",
+          "AGENT_REMOTE_API_KEY",
+        );
+        setCredentialReferences(
+          values,
+          "AGENT_REMOTE_API_KEY",
+          modelCredential,
+          credentials,
+        );
+      }
     }
-    if (hubUrl) {
+
+    if (mode === "local") {
+      clearHubValues(values);
+    } else {
+      const configuredHubUrl =
+        process.env.AGENT_HUB_URL?.trim() ||
+        existingValues.AGENT_HUB_URL ||
+        "";
+      const hubUrl = interactive
+        ? await askVisible(
+            terminal,
+            "Hub URL",
+            configuredHubUrl,
+          )
+        : configuredHubUrl;
+      if (!hubUrl || hubUrl === "-") {
+        throw new Error(
+          "Hub URL is required for hub-worker and dispatch-only modes",
+        );
+      }
       rejectUnsupportedEnvCharacters("Hub URL", hubUrl);
       if (looksLikePlaceholder(hubUrl)) throw new Error("Hub URL is required");
       validateUrl(hubUrl, "Hub URL");
-      let hubUsername =
+
+      const configuredUsername =
         process.env.AGENT_HUB_USERNAME?.trim() ||
         existingValues.AGENT_HUB_USERNAME ||
         "";
-      if (interactive) {
-        const answer = (
-          await terminal.question(
-            `Hub username [${hubUsername || "required (e.g. alice)"}]: `,
-          )
-        ).trim();
-        if (answer) hubUsername = answer;
-      }
+      const hubUsername = interactive
+        ? await askVisible(terminal, "Hub username", configuredUsername)
+        : configuredUsername;
       if (hubUsername) {
         rejectUnsupportedEnvCharacters("Hub username", hubUsername);
         const passwordReference = {
@@ -178,100 +342,525 @@ async function collectValues(existingValues, credentials) {
             process.env.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT?.trim() ||
             existingValues.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT ||
             hubUsername,
+          ...(process.env.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE?.trim() ||
+          existingValues.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE
+            ? {
+                legacy: {
+                  service:
+                    process.env.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE?.trim() ||
+                    existingValues.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE,
+                  account:
+                    process.env.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT?.trim() ||
+                    existingValues.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT,
+                },
+              }
+            : {}),
         };
-        const configuredHubPassword =
-          process.env.AGENT_HUB_PASSWORD?.trim() ||
-          existingValues.AGENT_HUB_PASSWORD ||
-          credentials.read(passwordReference, "Hub password") ||
-          "";
-        const hubPassword = interactive
-          ? await askSecret(
-              terminal,
-              mutedOutput,
-              "Hub password",
-              configuredHubPassword,
-            )
-          : configuredHubPassword;
-        if (!hubPassword || looksLikePlaceholder(hubPassword)) {
-          throw new Error(
-            "Hub password is required when Hub username is configured",
-          );
-        }
+        const configuredHubPassword = configuredSecret(
+          existingValues,
+          credentials,
+          passwordReference,
+          "AGENT_HUB_PASSWORD",
+          undefined,
+          "Hub password",
+        );
+        const hubPassword = await obtainSecret(
+          terminal,
+          mutedOutput,
+          "Hub password",
+          configuredHubPassword,
+          credentials,
+          "AGENT_HUB_PASSWORD",
+        );
         rejectUnsupportedEnvCharacters("Hub password", hubPassword);
-        credentials.write(passwordReference, hubPassword, "Hub password");
+        credentials.write(
+          passwordReference,
+          hubPassword,
+          "Hub password",
+          "AGENT_HUB_PASSWORD",
+        );
         values.AGENT_HUB_URL = hubUrl;
         values.AGENT_HUB_USERNAME = hubUsername;
-        values.AGENT_HUB_PASSWORD = null;
-        values.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE = null;
-        values.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT = null;
-        values.AGENT_HUB_PASSWORD_CREDENTIAL_SERVICE =
-          passwordReference.service;
-        values.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT =
-          passwordReference.account;
-        values.AGENT_HUB_TOKEN = null;
-        values.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE = null;
-        values.AGENT_HUB_TOKEN_KEYCHAIN_ACCOUNT = null;
-        values.AGENT_HUB_TOKEN_CREDENTIAL_SERVICE = null;
-        values.AGENT_HUB_TOKEN_CREDENTIAL_ACCOUNT = null;
+        setCredentialReferences(
+          values,
+          "AGENT_HUB_PASSWORD",
+          passwordReference,
+          credentials,
+        );
+        clearHubTokenValues(values);
         hubPasswordMode = true;
       } else {
         console.warn(
           "Hub token mode is deprecated; register a password account and rerun setup.",
         );
         const hubCredential = credentialReference(existingValues, "hub");
-        const configuredHubToken =
-          process.env.AGENT_HUB_TOKEN?.trim() ||
-          existingValues.AGENT_HUB_TOKEN ||
-          credentials.read(hubCredential, "Hub") ||
-          "";
-        const hubToken = interactive
-          ? await askSecret(terminal, mutedOutput, "Hub token", configuredHubToken)
-          : configuredHubToken;
-        if (!hubToken || looksLikePlaceholder(hubToken)) {
-          throw new Error("Hub token is required when Hub URL is configured");
-        }
+        const configuredHubToken = configuredSecret(
+          existingValues,
+          credentials,
+          hubCredential,
+          "AGENT_HUB_TOKEN",
+          undefined,
+          "Hub",
+        );
+        const hubToken = await obtainSecret(
+          terminal,
+          mutedOutput,
+          "Hub token",
+          configuredHubToken,
+          credentials,
+          "AGENT_HUB_TOKEN",
+        );
         if (hubToken.length < 24) {
           throw new Error("Hub token must contain at least 24 characters");
         }
         rejectUnsupportedEnvCharacters("Hub token", hubToken);
-        credentials.write(hubCredential, hubToken, "Hub");
+        credentials.write(
+          hubCredential,
+          hubToken,
+          "Hub",
+          "AGENT_HUB_TOKEN",
+        );
         values.AGENT_HUB_URL = hubUrl;
         values.AGENT_HUB_USERNAME = null;
         values.AGENT_HUB_PASSWORD = null;
-        values.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE = null;
-        values.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT = null;
-        values.AGENT_HUB_PASSWORD_CREDENTIAL_SERVICE = null;
-        values.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT = null;
-        values.AGENT_HUB_TOKEN = null;
-        values.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE = null;
-        values.AGENT_HUB_TOKEN_KEYCHAIN_ACCOUNT = null;
-        values.AGENT_HUB_TOKEN_CREDENTIAL_SERVICE = hubCredential.service;
-        values.AGENT_HUB_TOKEN_CREDENTIAL_ACCOUNT = hubCredential.account;
+        setCredentialReferences(
+          values,
+          "AGENT_HUB_TOKEN",
+          hubCredential,
+          credentials,
+        );
+        clearHubPasswordValues(values);
       }
-    } else {
-      const directlyConfiguredHubToken =
-        process.env.AGENT_HUB_TOKEN?.trim() || existingValues.AGENT_HUB_TOKEN || "";
-      if (!interactive && directlyConfiguredHubToken) {
-        throw new Error("AGENT_HUB_URL is required when AGENT_HUB_TOKEN is set");
+    }
+
+    if (mode !== "local") {
+      const nodeCredential = credentialReference(existingValues, "node");
+      const configuredNodeToken = configuredSecret(
+        existingValues,
+        credentials,
+        nodeCredential,
+        "AGENT_HUB_NODE_TOKEN",
+        undefined,
+        "Hub node credential",
+      );
+      if (configuredNodeToken) {
+        rejectUnsupportedEnvCharacters("Hub node credential", configuredNodeToken);
+        credentials.write(
+          nodeCredential,
+          configuredNodeToken,
+          "Hub node credential",
+          "AGENT_HUB_NODE_TOKEN",
+        );
+        setCredentialReferences(
+          values,
+          "AGENT_HUB_NODE_TOKEN",
+          nodeCredential,
+          credentials,
+        );
       }
-      values.AGENT_HUB_URL = null;
-      values.AGENT_HUB_TOKEN = null;
-      values.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE = null;
-      values.AGENT_HUB_TOKEN_KEYCHAIN_ACCOUNT = null;
-      values.AGENT_HUB_TOKEN_CREDENTIAL_SERVICE = null;
-      values.AGENT_HUB_TOKEN_CREDENTIAL_ACCOUNT = null;
-      values.AGENT_HUB_USERNAME = null;
-      values.AGENT_HUB_PASSWORD = null;
-      values.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE = null;
-      values.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT = null;
-      values.AGENT_HUB_PASSWORD_CREDENTIAL_SERVICE = null;
-      values.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT = null;
     }
+
+    const webEnabled =
+      mode !== "local" &&
+      (interactive
+        ? await askYesNo(
+            terminal,
+            "Enable optional DSH Web Bridge capability?",
+            existingValues.AGENT_HUB_DSH_WEB === "1",
+          )
+        : existingValues.AGENT_HUB_DSH_WEB === "1");
+    values.AGENT_HUB_DSH_WEB = webEnabled ? "1" : null;
+    values.AGENT_HUB_RECEIVE_DISABLED =
+      mode === "dispatch-only" ? "1" : null;
+
+    const permission = await selectPermission(
+      existingValues,
+      terminal,
+      interactive,
+    );
+    values.AGENT_REMOTE_TOOL_POLICY = permission.remote;
+    values.AGENT_DSH_PERMISSION_MODE = permission.dsh;
+    values.AGENT_WORKSPACE_ROOT = workspaceRoot;
+    values.AGENT_CREDENTIAL_BACKEND = credentials.backend;
+    values.AGENT_CREDENTIAL_FILE =
+      credentials.backend === "file" ? credentials.credentialFile : null;
+
+    return { values, hubPasswordMode, mode };
   } finally {
     terminal?.close();
   }
-  values.AGENT_WORKSPACE_ROOT = workspaceRoot;
-  return { values, hubPasswordMode };
+}
+
+async function selectMode(existingValues, terminal, interactive) {
+  const configured =
+    args.mode ||
+    process.env.AGENT_SETUP_MODE?.trim() ||
+    (existingValues.AGENT_HUB_RECEIVE_DISABLED === "1"
+      ? "dispatch-only"
+      : existingValues.AGENT_HUB_URL || process.env.AGENT_HUB_URL
+        ? "hub-worker"
+        : "");
+  if (configured) return configured;
+  if (!interactive) return "local";
+  return askChoice(
+    terminal,
+    "How will this installation be used?",
+    [
+      ["local", "本机使用"],
+      ["hub-worker", "Hub worker（接收远程任务）"],
+      ["dispatch-only", "dispatch-only（只派发，不接收）"],
+    ],
+    "local",
+  );
+}
+
+async function selectProvider(terminal, interactive) {
+  const configured = process.env.AGENT_SETUP_PROVIDER?.trim();
+  if (configured) {
+    if (!["deepseek", "custom", "existing"].includes(configured)) {
+      throw new Error(
+        "AGENT_SETUP_PROVIDER must be deepseek, custom, or existing",
+      );
+    }
+    return configured;
+  }
+  if (!interactive) return "deepseek";
+  return askChoice(
+    terminal,
+    "Model provider",
+    [
+      ["deepseek", "DeepSeek official preset"],
+      ["existing", "Reuse existing dsh/Pi-compatible settings"],
+      ["custom", "Custom OpenAI-compatible endpoint"],
+    ],
+    "deepseek",
+  );
+}
+
+async function collectModelValues(
+  provider,
+  existingBase,
+  existingModel,
+  terminal,
+  interactive,
+) {
+  if (provider === "existing") {
+    if (!existingBase || !existingModel) {
+      throw new Error(
+        "No complete existing model configuration was found; choose custom or deepseek.",
+      );
+    }
+    return { baseUrl: existingBase, model: existingModel };
+  }
+  const defaultBase =
+    provider === "deepseek" ? "https://api.deepseek.com" : existingBase;
+  const defaultModel =
+    provider === "deepseek" ? "deepseek-v4-flash" : existingModel;
+  const baseUrl = interactive
+    ? await askVisible(terminal, "OpenAI-compatible model URL", defaultBase)
+    : defaultBase;
+  const model = interactive
+    ? await askVisible(terminal, "Model ID", defaultModel)
+    : defaultModel;
+  if (!baseUrl || looksLikePlaceholder(baseUrl)) {
+    throw new Error("Model URL is required");
+  }
+  if (!model || looksLikePlaceholder(model)) {
+    throw new Error("Model ID is required");
+  }
+  rejectUnsupportedEnvCharacters("Model URL", baseUrl);
+  rejectUnsupportedEnvCharacters("Model ID", model);
+  validateUrl(baseUrl, "Model URL");
+  return { baseUrl, model };
+}
+
+async function selectPermission(existingValues, terminal, interactive) {
+  const currentRemote =
+    process.env.AGENT_REMOTE_TOOL_POLICY?.trim() ||
+    existingValues.AGENT_REMOTE_TOOL_POLICY ||
+    "";
+  const currentDsh =
+    process.env.AGENT_DSH_PERMISSION_MODE?.trim() ||
+    existingValues.AGENT_DSH_PERMISSION_MODE ||
+    "";
+  if (currentRemote && currentDsh) {
+    return { remote: currentRemote, dsh: currentDsh };
+  }
+  if (currentRemote) {
+    return {
+      remote: currentRemote,
+      dsh: currentRemote === "full" ? "workspace-write" : "read-only",
+    };
+  }
+  if (currentDsh) {
+    return {
+      remote: currentDsh === "read-only" ? "read_only" : "full",
+      dsh: currentDsh,
+    };
+  }
+  const choice = interactive
+    ? await askChoice(
+        terminal,
+        "Remote tool permission",
+        [
+          ["read-only", "read-only"],
+          ["workspace-write", "workspace-write（推荐）"],
+          ["full", "full"],
+        ],
+        "workspace-write",
+      )
+    : "workspace-write";
+  const values = {
+    "read-only": { remote: "read_only", dsh: "read-only" },
+    "workspace-write": { remote: "full", dsh: "workspace-write" },
+    full: { remote: "full", dsh: "danger-full-access" },
+  };
+  return values[choice];
+}
+
+function clearHubValues(values) {
+  values.AGENT_HUB_URL = null;
+  values.AGENT_HUB_USERNAME = null;
+  values.AGENT_HUB_RECEIVE_DISABLED = null;
+  values.AGENT_HUB_DSH_WEB = null;
+  clearHubPasswordValues(values);
+  clearHubTokenValues(values);
+  values.AGENT_HUB_NODE_TOKEN = null;
+  values.AGENT_HUB_NODE_TOKEN_CREDENTIAL_SERVICE = null;
+  values.AGENT_HUB_NODE_TOKEN_CREDENTIAL_ACCOUNT = null;
+  values.AGENT_HUB_NODE_TOKEN_KEYCHAIN_SERVICE = null;
+  values.AGENT_HUB_NODE_TOKEN_KEYCHAIN_ACCOUNT = null;
+}
+
+function clearHubPasswordValues(values) {
+  values.AGENT_HUB_PASSWORD = null;
+  values.AGENT_HUB_PASSWORD_CREDENTIAL_SERVICE = null;
+  values.AGENT_HUB_PASSWORD_CREDENTIAL_ACCOUNT = null;
+  values.AGENT_HUB_PASSWORD_KEYCHAIN_SERVICE = null;
+  values.AGENT_HUB_PASSWORD_KEYCHAIN_ACCOUNT = null;
+}
+
+function clearHubTokenValues(values) {
+  values.AGENT_HUB_TOKEN = null;
+  values.AGENT_HUB_TOKEN_CREDENTIAL_SERVICE = null;
+  values.AGENT_HUB_TOKEN_CREDENTIAL_ACCOUNT = null;
+  values.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE = null;
+  values.AGENT_HUB_TOKEN_KEYCHAIN_ACCOUNT = null;
+}
+
+function setCredentialReferences(values, key, reference, credentials) {
+  values[key] = null;
+  if (key === "AGENT_REMOTE_API_KEY") values.LLM_API_KEY = null;
+  values[`${key}_KEYCHAIN_SERVICE`] = null;
+  values[`${key}_KEYCHAIN_ACCOUNT`] = null;
+  values[`${key}_CREDENTIAL_SERVICE`] = null;
+  values[`${key}_CREDENTIAL_ACCOUNT`] = null;
+  if (credentials.backend === "keychain") {
+    values[`${key}_CREDENTIAL_SERVICE`] = reference.service;
+    values[`${key}_CREDENTIAL_ACCOUNT`] = reference.account;
+  }
+}
+
+function configuredSecret(
+  existingValues,
+  credentials,
+  reference,
+  primaryKey,
+  aliasKey,
+  label,
+) {
+  if (credentials.backend === "env") {
+    return (
+      process.env[primaryKey]?.trim() ||
+      (aliasKey ? process.env[aliasKey]?.trim() : "") ||
+      ""
+    );
+  }
+  return (
+    process.env[primaryKey]?.trim() ||
+    (aliasKey ? process.env[aliasKey]?.trim() : "") ||
+    existingValues[primaryKey] ||
+    (aliasKey ? existingValues[aliasKey] : "") ||
+    credentials.read(reference, label, primaryKey) ||
+    ""
+  );
+}
+
+async function obtainSecret(
+  terminal,
+  output,
+  label,
+  configured,
+  credentials,
+  envKey,
+) {
+  const canPrompt =
+    interactiveTerminal() &&
+    (credentials.backend !== "env" || credentials.allowInteractiveEnv === true);
+  if (!configured && credentials.backend === "env" && !canPrompt) {
+    throw new Error(
+      `No ${label} was supplied. Export ${envKey} and rerun setup, or explicitly choose --credentials file.`,
+    );
+  }
+  const value = canPrompt ? await askSecret(terminal, output, label, configured) : configured;
+  if (!value || looksLikePlaceholder(value)) {
+    throw new Error(`${label} is required`);
+  }
+  return value;
+}
+
+async function createCredentialStore(requested, configPathValue, existingFile) {
+  const configuredFile =
+    process.env.AGENT_CREDENTIAL_FILE?.trim() || existingFile?.trim();
+  const credentialFile = resolve(
+    configuredFile || join(dirname(configPathValue), "credentials.env"),
+  );
+  if (requested === "file") return fileCredentialStore(credentialFile);
+  if (requested === "env") return environmentCredentialStore();
+  if (configuredFile && requested === "auto") {
+    return fileCredentialStore(credentialFile);
+  }
+
+  let store;
+  try {
+    const { Entry } = await import("@napi-rs/keyring");
+    store = {
+      backend: "keychain",
+      summary: "system credential store (Keychain/Credential Manager/Secret Service)",
+      credentialFile: undefined,
+      allowInteractiveEnv: false,
+      read(reference, label) {
+        try {
+          const password =
+            new Entry(reference.service, reference.account).getPassword() ||
+            undefined;
+          if (password || !reference.legacy) return password;
+          return readLegacyCredential(reference.legacy, Entry);
+        } catch {
+          return undefined;
+        }
+      },
+      write(reference, secret, label) {
+        try {
+          new Entry(reference.service, reference.account).setPassword(secret);
+        } catch {
+          if (requested === "auto") {
+            store.backend = "env";
+            store.summary = "environment variables only (system store unavailable; not persisted)";
+            store.allowInteractiveEnv = true;
+            store.read = (_reference, _label, key) =>
+              process.env[key]?.trim() || undefined;
+            store.write = (_reference, secret, _label, key) => {
+              if (key) process.env[key] = secret;
+            };
+            console.warn(
+              "System credential store is unavailable; falling back to environment-only credentials. No secret was written to disk.",
+            );
+            return;
+          }
+          throw new Error(
+            `Could not save the ${label} credential in the system store; rerun with --credentials file if this host has no usable keychain`,
+          );
+        }
+      },
+    };
+    return store;
+  } catch {
+    console.warn(
+      "System credential store unavailable; defaulting to environment-only credentials. Use --credentials file only if you explicitly accept 0600 file storage.",
+    );
+    return environmentCredentialStore();
+  }
+}
+
+function environmentCredentialStore() {
+  return {
+    backend: "env",
+    summary: "environment variables only (not persisted)",
+    credentialFile: undefined,
+    read() {
+      return undefined;
+    },
+    write() {},
+  };
+}
+
+function fileCredentialStore(path) {
+  return {
+    backend: "file",
+    summary: "explicit 0600 credential file",
+    credentialFile: path,
+    read(_reference, label, key) {
+      return readFileCredential(path, key, label);
+    },
+    write(_reference, secret, label, key) {
+      writeFileCredential(path, key, secret, label);
+    },
+  };
+}
+
+function readFileCredential(path, key, label) {
+  if (!existsSync(path)) return undefined;
+  assertCredentialFileMode(path, label);
+  return parseEnv(readFileSync(path, "utf8"))[key]?.trim() || undefined;
+}
+
+function writeFileCredential(path, key, value, label) {
+  if (/\r|\n/u.test(value)) {
+    throw new Error(`${label} cannot contain newlines`);
+  }
+  mkdirSync(dirname(path), { recursive: true, mode: 0o700 });
+  if (process.platform !== "win32") chmodSync(dirname(path), 0o700);
+  const original = existsSync(path) ? readFileSync(path, "utf8") : "";
+  const lines = original.split(/\r?\n/u);
+  let replaced = false;
+  const next = lines.map((line) => {
+    const match = line.match(/^\s*([A-Z][A-Z0-9_]*)\s*=/u);
+    if (!match || match[1] !== key) return line;
+    replaced = true;
+    return `${key}=${dotenvValue(value)}`;
+  });
+  if (!replaced) {
+    if (next.at(-1)?.trim()) next.push("");
+    next.push(`${key}=${dotenvValue(value)}`);
+  }
+  const temporary = `${path}.${process.pid}.tmp`;
+  writeFileSync(temporary, `${next.join("\n").replace(/\n+$/u, "")}\n`, {
+    encoding: "utf8",
+    mode: 0o600,
+  });
+  if (process.platform !== "win32") chmodSync(temporary, 0o600);
+  renameSync(temporary, path);
+  if (process.platform !== "win32") chmodSync(path, 0o600);
+}
+
+function assertCredentialFileMode(path, label) {
+  if (process.platform === "win32") return;
+  let mode;
+  try {
+    mode = statSync(path).mode & 0o777;
+  } catch {
+    throw new Error(`Could not inspect the ${label}`);
+  }
+  if (mode !== 0o600) {
+    throw new Error(
+      `The ${label} must have permissions 0600; run chmod 600 ${path}`,
+    );
+  }
+}
+
+function readLegacyCredential(reference, Entry) {
+  if (process.platform !== "darwin") {
+    return new Entry(reference.service, reference.account || "").getPassword() || undefined;
+  }
+  const args = ["find-generic-password"];
+  if (reference.account) args.push("-a", reference.account);
+  args.push("-s", reference.service, "-w");
+  const result = spawnSync("/usr/bin/security", args, {
+    encoding: "utf8",
+    stdio: ["ignore", "pipe", "ignore"],
+  });
+  return result.status === 0 ? result.stdout.trim() || undefined : undefined;
 }
 
 function credentialReference(existingValues, kind) {
@@ -299,6 +888,27 @@ function credentialReference(existingValues, kind) {
         : {}),
     };
   }
+  if (kind === "node") {
+    const legacyService =
+      process.env.AGENT_HUB_NODE_TOKEN_KEYCHAIN_SERVICE?.trim() ||
+      existingValues.AGENT_HUB_NODE_TOKEN_KEYCHAIN_SERVICE;
+    const legacyAccount =
+      process.env.AGENT_HUB_NODE_TOKEN_KEYCHAIN_ACCOUNT?.trim() ||
+      existingValues.AGENT_HUB_NODE_TOKEN_KEYCHAIN_ACCOUNT;
+    return {
+      service:
+        process.env.AGENT_HUB_NODE_TOKEN_CREDENTIAL_SERVICE?.trim() ||
+        existingValues.AGENT_HUB_NODE_TOKEN_CREDENTIAL_SERVICE ||
+        `${namespace} Hub Node`,
+      account:
+        process.env.AGENT_HUB_NODE_TOKEN_CREDENTIAL_ACCOUNT?.trim() ||
+        existingValues.AGENT_HUB_NODE_TOKEN_CREDENTIAL_ACCOUNT ||
+        defaultAccount,
+      ...(legacyService
+        ? { legacy: { service: legacyService, account: legacyAccount } }
+        : {}),
+    };
+  }
   const legacyService =
     process.env.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE?.trim() ||
     existingValues.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE;
@@ -320,50 +930,6 @@ function credentialReference(existingValues, kind) {
   };
 }
 
-async function createCredentialStore() {
-  let Entry;
-  try {
-    ({ Entry } = await import("@napi-rs/keyring"));
-  } catch {
-    throw new Error(
-      "The native system credential store is unavailable; setup will not write secrets to a file",
-    );
-  }
-  return {
-    read(reference, label) {
-      try {
-        const password =
-          new Entry(reference.service, reference.account).getPassword() || undefined;
-        if (password || !reference.legacy) return password;
-        return readLegacyCredential(reference.legacy, Entry);
-      } catch {
-        throw new Error(`Could not read the ${label} credential from the system store`);
-      }
-    },
-    write(reference, secret, label) {
-      try {
-        new Entry(reference.service, reference.account).setPassword(secret);
-      } catch {
-        throw new Error(`Could not save the ${label} credential in the system store`);
-      }
-    },
-  };
-}
-
-function readLegacyCredential(reference, Entry) {
-  if (process.platform !== "darwin") {
-    return new Entry(reference.service, reference.account || "").getPassword() || undefined;
-  }
-  const args = ["find-generic-password"];
-  if (reference.account) args.push("-a", reference.account);
-  args.push("-s", reference.service, "-w");
-  const result = spawnSync("/usr/bin/security", args, {
-    encoding: "utf8",
-    stdio: ["ignore", "pipe", "ignore"],
-  });
-  return result.status === 0 ? result.stdout.trim() || undefined : undefined;
-}
-
 async function askVisible(terminal, label, current) {
   const hint = current ? ` [${current}]` : "";
   const answer = (await terminal.question(`${label}${hint}: `)).trim();
@@ -379,6 +945,31 @@ async function askSecret(terminal, output, label, current) {
   return answer || current;
 }
 
+async function askChoice(terminal, label, choices, defaultId) {
+  stdout.write(`\n${label}\n`);
+  for (const [id, description] of choices) {
+    stdout.write(`  ${id === defaultId ? "*" : " "} ${id} — ${description}\n`);
+  }
+  const answer = (await terminal.question(`Choice [${defaultId}]: `)).trim();
+  const selected = answer || defaultId;
+  const byNumber = choices[Number(selected) - 1]?.[0];
+  const byId = choices.find(([id]) => id === selected)?.[0];
+  const value = byId || byNumber;
+  if (!value) throw new Error(`Invalid choice for ${label}: ${selected}`);
+  return value;
+}
+
+async function askYesNo(terminal, label, current) {
+  const defaultValue = current ? "Y/n" : "y/N";
+  const answer = (await terminal.question(`${label} [${defaultValue}]: `))
+    .trim()
+    .toLowerCase();
+  if (!answer) return current;
+  if (["y", "yes"].includes(answer)) return true;
+  if (["n", "no"].includes(answer)) return false;
+  throw new Error(`Please answer yes or no for ${label}`);
+}
+
 class MutedOutput extends Writable {
   muted = false;
 
@@ -402,12 +993,12 @@ function parseEnv(text) {
     ) {
       value = value.slice(1, -1);
     }
-    values[key] = value;
+    if (key) values[key] = value;
   }
   return values;
 }
 
-function writeConfig(original, values) {
+function writeConfig(original, values, path) {
   const lines = original.split(/\r?\n/u);
   const remaining = new Map(Object.entries(values));
   const next = lines
@@ -426,14 +1017,16 @@ function writeConfig(original, values) {
       if (value !== null) next.push(`${key}=${dotenvValue(value)}`);
     }
   }
-  mkdirSync(dirname(configPath), { recursive: true });
-  const temporary = `${configPath}.${process.pid}.tmp`;
+  mkdirSync(dirname(path), { recursive: true, mode: 0o700 });
+  if (process.platform !== "win32") chmodSync(dirname(path), 0o700);
+  const temporary = `${path}.${process.pid}.tmp`;
   writeFileSync(temporary, `${next.join("\n").replace(/\n+$/u, "")}\n`, {
     encoding: "utf8",
     mode: 0o600,
   });
   chmodSync(temporary, 0o600);
-  renameSync(temporary, configPath);
+  renameSync(temporary, path);
+  chmodSync(path, 0o600);
 }
 
 function dotenvValue(value) {
@@ -455,7 +1048,7 @@ function validateUrl(value, label) {
 }
 
 function rejectUnsupportedEnvCharacters(label, value) {
-  if (/[\r\n]/u.test(value)) throw new Error(`${label} cannot contain newlines`);
+  if (/\r|\n/u.test(value)) throw new Error(`${label} cannot contain newlines`);
 }
 
 function looksLikePlaceholder(value) {
diff --git a/agent-host/src/adapter-session-registry.ts b/agent-host/src/adapter-session-registry.ts
index 1d8ad1f..7a95efc 100644
--- a/agent-host/src/adapter-session-registry.ts
+++ b/agent-host/src/adapter-session-registry.ts
@@ -7,6 +7,7 @@ import {
   renameSync,
   rmSync,
   writeFileSync,
+  unlinkSync,
 } from "node:fs";
 import { join, resolve } from "node:path";
 
@@ -92,12 +93,16 @@ export class AdapterSessionRegistry {
     chmodSync(this.recordsDir, 0o700);
     const target = this.recordPath(record.key);
     const temporary = `${target}.${process.pid}.${randomUUID()}.tmp`;
-    writeFileSync(temporary, `${JSON.stringify(record, null, 2)}\n`, {
-      encoding: "utf8",
-      mode: 0o600,
-    });
-    chmodSync(temporary, 0o600);
-    renameSync(temporary, target);
+    try {
+      writeFileSync(temporary, `${JSON.stringify(record, null, 2)}\n`, {
+        encoding: "utf8",
+        mode: 0o600,
+      });
+      chmodSync(temporary, 0o600);
+      renameSync(temporary, target);
+    } finally {
+      try { unlinkSync(temporary); } catch { /* rename completed or temp was never created */ }
+    }
   }
 }
 
diff --git a/agent-host/src/cli.ts b/agent-host/src/cli.ts
index de04016..4bf8c9e 100644
--- a/agent-host/src/cli.ts
+++ b/agent-host/src/cli.ts
@@ -1,7 +1,16 @@
 #!/usr/bin/env node
 
 import { dirname, resolve } from "node:path";
-import { existsSync, mkdirSync, readFileSync, realpathSync, writeFileSync } from "node:fs";
+import {
+  accessSync,
+  chmodSync,
+  constants,
+  existsSync,
+  mkdirSync,
+  readFileSync,
+  realpathSync,
+  writeFileSync,
+} from "node:fs";
 import { createRequire } from "node:module";
 import { fileURLToPath, pathToFileURL } from "node:url";
 import { createInterface } from "node:readline/promises";
@@ -21,6 +30,7 @@ import { controlTask } from "./controller.js";
 import {
   deleteSystemCredential,
   writeSystemCredential,
+  writeCredentialFile,
 } from "./credential-store.js";
 import { DshAgentEngine } from "./dsh-engine.js";
 import { runDshDoctor } from "./dsh-doctor.js";
@@ -43,6 +53,8 @@ import { TaskWorker } from "./worker.js";
 
 async function main(): Promise<void> {
   const command = process.argv[2] ?? "interactive";
+  const doctorOptions =
+    command === "doctor" ? parseDoctorOptions(process.argv.slice(3)) : undefined;
   // A self-update that deferred npm ci (because the previous worker process
   // held Windows DLL locks on node_modules) is applied here, before any
   // credentials load: the keyring native addon is not loaded yet, so npm ci
@@ -83,6 +95,7 @@ async function main(): Promise<void> {
   const config = loadConfig({
     allowDshPlugin: pluginWorkerDefault,
     allowDshTui: dshTuiDefault,
+    allowIncomplete: command === "doctor",
   });
   const hubRuntimeDisabled = process.env.AGENT_HUB_RUNTIME_DISABLED === "1";
   if (
@@ -106,10 +119,19 @@ async function main(): Promise<void> {
   const workerConfig = dshReceivingCommand
     ? resolveDshIdentity(config)
     : config;
-  const hub =
-    config.hubEnabled && !hubRuntimeDisabled
-      ? await resolveHubClient(workerConfig, dshReceivingCommand ? "dsh" : "pi")
-      : undefined;
+  let hub: HubClient | undefined;
+  let hubResolutionError: unknown;
+  if (config.hubEnabled && !hubRuntimeDisabled) {
+    try {
+      hub = await resolveHubClient(
+        workerConfig,
+        dshReceivingCommand ? "dsh" : "pi",
+      );
+    } catch (error) {
+      if (command !== "doctor") throw error;
+      hubResolutionError = error;
+    }
+  }
   if (command === "__tui-child") {
     const runId = process.argv[3]?.trim();
     if (!runId) throw new Error("Internal TUI child requires a run_id");
@@ -178,7 +200,7 @@ async function main(): Promise<void> {
     if (adapter.id === "dsh" && process.env.AGENT_DSH_COMMAND?.trim()) {
       adapter = {
         ...adapter,
-        command: [...(config.dshCommand ?? ["dsh"]), "--profile", "headless"],
+        command: [...dshCommandForSpawn(config), "--profile", "headless"],
       };
     }
     let bridgeConfig = resolveAdapterIdentity(config, adapter);
@@ -228,7 +250,10 @@ async function main(): Promise<void> {
     return;
   }
   if (command === "doctor") {
-    await runDoctor(config, hub);
+    await runDoctor(config, hub, {
+      ...(doctorOptions ?? { live: false, json: false }),
+      ...(hubResolutionError ? { hubError: hubResolutionError } : {}),
+    });
     return;
   }
   if (command === "dsh-doctor") {
@@ -604,13 +629,23 @@ async function runWebBridge(
     process.env.AGENT_DSH_WEB_TARGET?.trim() || "http://127.0.0.1:3080";
   const dshWeb = await ensureDshWeb(config, hub, target);
   await advertiseDshWeb(config, hub);
-  await ensureDefaultWorkspace(config, target);
+  const dshAuthUrl =
+    process.env.AGENT_DSH_WEB_AUTH_URL?.trim() ||
+    (await dshWeb?.authUrl);
   const bridge = new WebBridge({
     hubUrl: config.hubUrl!,
     nodeToken: hub.nodeToken,
     nodeId: config.nodeId,
     target,
+    ...(dshAuthUrl ? { dshAuthUrl } : {}),
+    ...(process.env.AGENT_DSH_WEB_COOKIE?.trim()
+      ? { dshCookie: process.env.AGENT_DSH_WEB_COOKIE.trim() }
+      : {}),
   });
+  const dshCookie =
+    process.env.AGENT_DSH_WEB_COOKIE?.trim() ||
+    (await bridge.authenticate());
+  await ensureDefaultWorkspace(config, target, dshCookie);
   let stopping = false;
   const stop = () => {
     if (stopping) return;
@@ -676,6 +711,7 @@ async function ensureDshWeb(
 async function ensureDefaultWorkspace(
   config: AgentHostConfig,
   target: string,
+  dshCookie?: string,
 ): Promise<void> {
   const root = (
     process.env.AGENT_DSH_WEB_DEFAULT_WORKSPACE?.trim() || homedir()
@@ -687,7 +723,7 @@ async function ensureDefaultWorkspace(
     const rpc = (method: string, payload: unknown) =>
       fetch(`${base}${method}`, {
         method: "POST",
-        headers: { "Content-Type": "application/json" },
+        headers: { "Content-Type": "application/json", ...(dshCookie ? { cookie: dshCookie } : {}) },
         body: JSON.stringify({
           type: "client-request",
           rpcId: `ws-default-${Date.now()}`,
@@ -739,9 +775,9 @@ function startDshWebChild(
   );
   const port = new URL(target).port || "3080";
   const command = profileReady
-    ? [...(config.dshCommand ?? ["dsh"]), "--profile", profile, "--port", port]
+    ? [...dshCommandForSpawn(config), "--profile", profile, "--port", port]
     : [
-        ...(config.dshCommand ?? ["dsh"]),
+        ...dshCommandForSpawn(config),
         "web",
         "--patch",
         legacyPatch,
@@ -754,6 +790,7 @@ function startDshWebChild(
   });
   console.log(`Starting local dsh web for bridge: ${command.join(" ")}`);
   return startDshChild(command, env, {
+    captureWebAuth: true,
     onError: (error) => {
       console.error(`Could not start dsh web for bridge: ${error.message}`);
       return false;
@@ -810,8 +847,8 @@ async function runDshWeb(
   );
   const webArgs = process.argv.slice(3);
   const command = profileReady
-    ? [...(config.dshCommand ?? ["dsh"]), "--profile", profile, ...webArgs]
-    : [...(config.dshCommand ?? ["dsh"]), "web", "--patch", legacyPatch, ...webArgs];
+    ? [...dshCommandForSpawn(config), "--profile", profile, ...webArgs]
+    : [...dshCommandForSpawn(config), "web", "--patch", legacyPatch, ...webArgs];
   if (!profileReady) {
     console.warn(
       existsSync(profilePackage)
@@ -856,7 +893,7 @@ async function runDshPluginWorker(
     return false;
   }
   const command = [
-    ...(config.dshCommand ?? ["dsh"]),
+    ...dshCommandForSpawn(config),
     "--profile",
     profile,
   ];
@@ -924,7 +961,7 @@ async function runDshDispatch(
     throw new Error(`DeepSeek Harness dispatch patch not found: ${patchPath}`);
   }
   const command = [
-    ...(config.dshCommand ?? ["dsh"]),
+    ...dshCommandForSpawn(config),
     "web",
     "--patch",
     patchPath,
@@ -976,6 +1013,50 @@ function hostCapabilities(
   ];
 }
 
+function dshCommandForSpawn(config: AgentHostConfig): string[] {
+  const command = [...(config.dshCommand ?? ["dsh"])]
+  const executable = command[0]
+  if (!executable || process.platform === "win32") return command
+  if (executable !== "dsh" && !executable.endsWith("/dsh")) return command
+  const resolved = executable.includes("/")
+    ? executable
+    : (process.env.PATH ?? "")
+        .split(":")
+        .map((entry) => (entry ? resolve(entry, executable) : ""))
+        .find((candidate) => candidate && existsSync(candidate))
+  if (!resolved) return command
+  try {
+    accessSync(resolved, constants.X_OK)
+    return command
+  } catch {
+    try {
+      const target = realpathSync(resolved)
+      if (target.endsWith(".js")) {
+        console.warn(
+          `dsh launcher ${resolved} is not executable; invoking it through ${process.execPath}`,
+        )
+        return [process.execPath, target, ...command.slice(1)]
+      }
+    } catch {
+      // Preserve the original command so the caller can report its spawn error.
+    }
+    return command
+  }
+}
+
+function parseDoctorOptions(args: string[]): { live: boolean; json: boolean } {
+  let live = false;
+  let json = false;
+  for (const arg of args) {
+    if (arg === "--live") live = true;
+    else if (arg === "--json") json = true;
+    else {
+      throw new Error("agent doctor accepts only --live and --json");
+    }
+  }
+  return { live, json };
+}
+
 async function resolveNodeCredential(
   config: AgentHostConfig,
   kind: WorkerRuntimeKind = "pi",
@@ -997,7 +1078,7 @@ async function resolveNodeCredential(
           origin: "worker-boot",
           workspace_root: config.workspaceRoot,
           runtime: kind,
-          runtime_version: kind === "dsh" ? "0.1.0-rc.5" : "0.83.0",
+          runtime_version: kind === "dsh" ? "0.1.2-alpha.2" : "0.83.0",
           remote_tool_policy: config.remoteToolPolicy,
           builtin_capabilities: config.builtinCapabilitiesEnabled,
           worker_session_mode: config.workerSessionMode,
@@ -1037,7 +1118,14 @@ async function resolveNodeCredential(
     let saved = true;
     if (!config.hubNodeTokenCacheDisabled) {
       try {
-        writeSystemCredential(service, account, login.node_token, "Hub node credential");
+        saved = persistConfiguredCredential(
+          config,
+          "AGENT_HUB_NODE_TOKEN",
+          service,
+          account,
+          login.node_token,
+          "Hub node credential",
+        );
       } catch (error) {
         saved = false;
         console.warn(
@@ -1055,6 +1143,29 @@ async function resolveNodeCredential(
   );
 }
 
+function persistConfiguredCredential(
+  config: AgentHostConfig,
+  key: string,
+  service: string,
+  account: string,
+  value: string,
+  label: string,
+): boolean {
+  if (config.credentialBackend === "env") {
+    // Environment-only mode is deliberately non-persistent. Keep the token
+    // available to this process so the current Hub operation can complete,
+    // but never write it to the config file or keychain.
+    process.env[key] = value;
+    return false;
+  }
+  if (config.credentialFile) {
+    writeCredentialFile(config.credentialFile, key, value, label);
+    return true;
+  }
+  writeSystemCredential(service, account, value, label);
+  return true;
+}
+
 async function resolveHubClient(
   config: AgentHostConfig,
   kind: WorkerRuntimeKind = "pi",
@@ -1069,7 +1180,9 @@ async function connectCommand(config: AgentHostConfig): Promise<void> {
   }
   let username = config.hubUsername ?? "";
   let password = config.hubPassword ?? "";
-  const interactive = Boolean(stdin.isTTY && stdout.isTTY);
+  const interactive =
+    Boolean(stdin.isTTY && stdout.isTTY) &&
+    !process.argv.slice(3).includes("--non-interactive");
   if (interactive) {
     const muted = new MutedOutput();
     const rl = createInterface({ input: stdin, output: muted, terminal: true });
@@ -1135,17 +1248,34 @@ async function connectCommand(config: AgentHostConfig): Promise<void> {
   const envPath = agentEnvPath(
     resolve(dirname(fileURLToPath(import.meta.url)), "..", "..", ".."),
   );
-  mkdirSync(dirname(envPath), { recursive: true });
-  ensureEnvLine(
-    envPath,
-    "AGENT_HUB_NODE_TOKEN_CREDENTIAL_SERVICE",
-    nodeService,
-  );
-  ensureEnvLine(envPath, "AGENT_HUB_NODE_TOKEN_CREDENTIAL_ACCOUNT", nodeAccount);
+  if (
+    config.configPath &&
+    resolve(config.configPath) !== resolve(envPath) &&
+    !existsSync(envPath)
+  ) {
+    migrateLegacyConfig(config.configPath, envPath);
+  }
+  if (config.credentialBackend !== "env" && !config.credentialFile) {
+    mkdirSync(dirname(envPath), { recursive: true });
+    if (config.hubUrl) ensureEnvLine(envPath, "AGENT_HUB_URL", config.hubUrl);
+    ensureEnvLine(envPath, "AGENT_HUB_USERNAME", username);
+    ensureEnvLine(
+      envPath,
+      "AGENT_HUB_NODE_TOKEN_CREDENTIAL_SERVICE",
+      nodeService,
+    );
+    ensureEnvLine(envPath, "AGENT_HUB_NODE_TOKEN_CREDENTIAL_ACCOUNT", nodeAccount);
+  }
   let saved = false;
   try {
-    writeSystemCredential(nodeService, nodeAccount, token, "Hub node credential");
-    saved = true;
+    saved = persistConfiguredCredential(
+      config,
+      "AGENT_HUB_NODE_TOKEN",
+      nodeService,
+      nodeAccount,
+      token,
+      "Hub node credential",
+    );
   } catch (error) {
     console.warn(
       `Could not save the node credential to the system store (${error instanceof Error ? error.message : String(error)}); ` +
@@ -1156,7 +1286,14 @@ async function connectCommand(config: AgentHostConfig): Promise<void> {
     const passwordService =
       process.env.AGENT_HUB_PASSWORD_CREDENTIAL_SERVICE?.trim() ||
       "AgentSociety Hub Password";
-    writeSystemCredential(passwordService, username, password, "Hub password");
+    persistConfiguredCredential(
+      config,
+      "AGENT_HUB_PASSWORD",
+      passwordService,
+      username,
+      password,
+      "Hub password",
+    );
   } catch (error) {
     console.warn(
       `Could not save the Hub password to the system store (${error instanceof Error ? error.message : String(error)}); worker restarts may need to run connect again.`,
@@ -1166,7 +1303,16 @@ async function connectCommand(config: AgentHostConfig): Promise<void> {
     `Connected to Hub as ${username} on node ${config.nodeId} (${config.actorId})`,
   );
   if (saved) {
-    console.log("Node credential saved to the system credential store.");
+    console.log(
+      config.credentialFile
+        ? `Node credential saved to ${config.credentialFile} (0600).`
+        : "Node credential saved to the system credential store.",
+    );
+  } else if (config.credentialBackend === "env") {
+    console.warn(
+      "Credential backend is environment-only; the new node credential was not persisted. " +
+        "Export AGENT_HUB_NODE_TOKEN before a future restart or run `agent connect` again.",
+    );
   } else {
     console.warn(
       "The system credential store is unavailable on this machine. " +
@@ -1195,6 +1341,29 @@ function ensureEnvLine(path: string, key: string, value: string): void {
     encoding: "utf8",
     mode: 0o600,
   });
+  chmodSync(path, 0o600);
+}
+
+function migrateLegacyConfig(source: string, target: string): void {
+  const secretKeys = new Set([
+    "AGENT_REMOTE_API_KEY",
+    "LLM_API_KEY",
+    "AGENT_HUB_TOKEN",
+    "AGENT_HUB_PASSWORD",
+    "AGENT_HUB_NODE_TOKEN",
+  ]);
+  const lines = readFileSync(source, "utf8").split(/\r?\n/u);
+  const sanitized = lines.filter((line) => {
+    const key = line.match(/^\s*([A-Z][A-Z0-9_]*)\s*=/u)?.[1];
+    return !key || !secretKeys.has(key);
+  });
+  mkdirSync(dirname(target), { recursive: true, mode: 0o700 });
+  writeFileSync(target, `${sanitized.join("\n").replace(/\n+$/u, "")}\n`, {
+    encoding: "utf8",
+    mode: 0o600,
+  });
+  chmodSync(target, 0o600);
+  console.log(`Migrated non-secret settings to ${target}; kept ${source}.`);
 }
 
 class MutedOutput extends Writable {
diff --git a/agent-host/src/config.ts b/agent-host/src/config.ts
index 8478a09..0f14097 100644
--- a/agent-host/src/config.ts
+++ b/agent-host/src/config.ts
@@ -1,8 +1,9 @@
 import { existsSync, readFileSync } from "node:fs";
 import { homedir, hostname, userInfo } from "node:os";
-import { resolve } from "node:path";
+import { join, resolve } from "node:path";
 
 import {
+  readCredentialEnv,
   readLegacyMacKeychainCredential,
   readSystemCredential,
 } from "./credential-store.js";
@@ -20,12 +21,19 @@ export type DshPermissionMode =
   | "danger-full-access";
 
 export interface AgentHostConfig {
+  /** Effective non-secret configuration file after central/legacy discovery. */
+  configPath?: string;
+  /** Explicit 0600 credential file, when the user selected file storage. */
+  credentialFile?: string;
+  credentialBackend?: "keychain" | "file" | "env";
+  credentialFileError?: string;
   hubEnabled: boolean;
   hubUrl?: string;
   hubToken?: string;
   hubUsername?: string;
   hubPassword?: string;
   hubNodeToken?: string;
+  hubReceiveDisabled?: boolean;
   hubNodeTokenCacheDisabled?: boolean;
   principalId: string;
   principalDisplayName: string;
@@ -93,27 +101,30 @@ export interface AgentHostConfig {
   dshWebProfile?: string;
 }
 
-export function loadProjectEnv(path?: string): void {
+export function loadProjectEnv(path?: string): string | undefined {
   const envPath = path ? resolve(path) : discoverProjectEnv();
-  if (!envPath || !existsSync(envPath)) return;
-  for (const rawLine of readFileSync(envPath, "utf8").split(/\r?\n/u)) {
-    const line = rawLine.trim();
-    if (!line || line.startsWith("#") || !line.includes("=")) continue;
-    const separator = line.indexOf("=");
-    const key = line.slice(0, separator).trim();
-    let value = line.slice(separator + 1).trim();
-    if (
-      (value.startsWith('"') && value.endsWith('"')) ||
-      (value.startsWith("'") && value.endsWith("'"))
-    ) {
-      value = value.slice(1, -1);
+  if (envPath && existsSync(envPath)) {
+    loadEnvFile(envPath);
+  }
+  const credentialFile = process.env.AGENT_CREDENTIAL_FILE?.trim();
+  if (credentialFile) {
+    try {
+      for (const [key, value] of Object.entries(
+        readCredentialEnv(resolve(credentialFile)),
+      )) {
+        if (process.env[key] === undefined) process.env[key] = value;
+      }
+    } catch (error) {
+      process.env.AGENT_CREDENTIAL_FILE_ERROR =
+        error instanceof Error ? error.message : String(error);
     }
-    if (key && process.env[key] === undefined) process.env[key] = value;
   }
+  return envPath;
 }
 
 export function discoverProjectEnv(cwd = process.cwd()): string | undefined {
   return firstExisting([
+    defaultAgentConfigPath(),
     resolve(cwd, ".private/env/agent.env"),
     resolve(cwd, ".env.agent"),
     resolve(cwd, "../.private/env/agent.env"),
@@ -123,18 +134,45 @@ export function discoverProjectEnv(cwd = process.cwd()): string | undefined {
   ]);
 }
 
+/** User-level configuration path shared by setup, connect, worker and doctor. */
+export function defaultAgentConfigPath(): string {
+  const configHome =
+    process.platform === "win32"
+      ? process.env.APPDATA?.trim() || join(homedir(), "AppData", "Roaming")
+      : process.env.XDG_CONFIG_HOME?.trim() || join(homedir(), ".config");
+  return resolve(configHome, "agentsociety", "agent.env");
+}
+
+/** Legacy project paths are still readable and are never removed automatically. */
+export function legacyAgentConfigPaths(root: string): string[] {
+  return [resolve(root, ".private/env/agent.env"), resolve(root, ".env.agent")];
+}
+
 /**
- * Canonical agent env file for a repository root. Prefers the private layout,
- * falls back to the legacy root-level `.env.agent`, and otherwise returns the
- * new canonical path so setup/connect can create it.
+ * Canonical user-level AgentSociety env file. The root argument is retained
+ * for callers from the pre-centralized configuration API.
  */
-export function agentEnvPath(root: string): string {
-  return (
-    firstExisting([
-      resolve(root, ".private/env/agent.env"),
-      resolve(root, ".env.agent"),
-    ]) ?? resolve(root, ".private/env/agent.env")
-  );
+export function agentEnvPath(_root: string): string {
+  return process.env.AGENT_ENV_FILE?.trim()
+    ? resolve(process.env.AGENT_ENV_FILE)
+    : defaultAgentConfigPath();
+}
+
+function loadEnvFile(path: string): void {
+  for (const rawLine of readFileSync(path, "utf8").split(/\r?\n/u)) {
+    const line = rawLine.trim();
+    if (!line || line.startsWith("#") || !line.includes("=")) continue;
+    const separator = line.indexOf("=");
+    const key = line.slice(0, separator).trim();
+    let value = line.slice(separator + 1).trim();
+    if (
+      (value.startsWith('"') && value.endsWith('"')) ||
+      (value.startsWith("'") && value.endsWith("'"))
+    ) {
+      value = value.slice(1, -1);
+    }
+    if (key && process.env[key] === undefined) process.env[key] = value;
+  }
 }
 
 function firstExisting(paths: string[]): string | undefined {
@@ -234,9 +272,13 @@ function isBannedRemoteHost(host: string): boolean {
 }
 
 export function loadConfig(
-  options: { allowDshPlugin?: boolean; allowDshTui?: boolean } = {},
+  options: {
+    allowDshPlugin?: boolean;
+    allowDshTui?: boolean;
+    allowIncomplete?: boolean;
+  } = {},
 ): AgentHostConfig {
-  loadProjectEnv(process.env.AGENT_ENV_FILE);
+  const configPath = loadProjectEnv(process.env.AGENT_ENV_FILE);
   const host = stableSlug(hostname());
   const owner = stableSlug(userInfo().username);
   const rawWorkerRuntime =
@@ -295,6 +337,7 @@ export function loadConfig(
         process.env.AGENT_HUB_TOKEN_KEYCHAIN_SERVICE?.trim(),
         process.env.AGENT_HUB_TOKEN_KEYCHAIN_ACCOUNT?.trim(),
         "Hub",
+        options.allowIncomplete,
       );
   const hubUsername = hubRuntimeDisabled
     ? undefined
@@ -325,6 +368,7 @@ export function loadConfig(
     hubUsername,
     hubPassword,
     hubNodeToken,
+    options.allowIncomplete,
   );
 
   const piProvider = process.env.PI_PROVIDER?.trim() || undefined;
@@ -350,6 +394,7 @@ export function loadConfig(
       process.env.AGENT_REMOTE_API_KEY_KEYCHAIN_SERVICE?.trim(),
       process.env.AGENT_REMOTE_API_KEY_KEYCHAIN_ACCOUNT?.trim(),
       "remote model",
+      options.allowIncomplete,
     );
   const dshConfigured = Boolean(
     process.env.AGENT_DSH_CONFIG?.trim() ||
@@ -362,7 +407,8 @@ export function loadConfig(
     (!remoteBaseUrl || !remoteModel) &&
     !dshConfigured &&
     !dshPluginConfigured &&
-    options.allowDshTui !== true
+    options.allowDshTui !== true &&
+    options.allowIncomplete !== true
   ) {
     throw new Error(
       "Configure PI_PROVIDER/PI_MODEL, a remote LLM_BASE_URL/LLM_MODEL, or the DeepSeek Harness runtime (AGENT_DSH_CONFIG/AGENT_DSH_RUNTIME_BIN)",
@@ -408,11 +454,25 @@ export function loadConfig(
   );
 
   const workspaceRoot = resolve(
-    process.env.AGENT_WORKSPACE_ROOT?.trim() || process.cwd(),
+    process.env.AGENT_WORKSPACE_ROOT?.trim() || homedir(),
   );
+  const credentialFile = process.env.AGENT_CREDENTIAL_FILE?.trim();
   return {
+    ...(configPath ? { configPath } : {}),
+    ...(credentialFile ? { credentialFile: resolve(credentialFile) } : {}),
+    ...(process.env.AGENT_CREDENTIAL_FILE_ERROR
+      ? { credentialFileError: process.env.AGENT_CREDENTIAL_FILE_ERROR }
+      : {}),
+    credentialBackend:
+      process.env.AGENT_CREDENTIAL_BACKEND?.trim() === "file" ||
+      credentialFile
+        ? "file"
+        : process.env.AGENT_CREDENTIAL_BACKEND?.trim() === "env"
+          ? "env"
+          : "keychain",
     ...hubConfig,
     ...(hubNodeToken ? { hubNodeToken } : {}),
+    hubReceiveDisabled: process.env.AGENT_HUB_RECEIVE_DISABLED === "1",
     principalId: required("AGENT_PRINCIPAL_ID", `human-${owner}`),
     principalDisplayName: required(
       "AGENT_PRINCIPAL_NAME",
@@ -499,9 +559,17 @@ function configuredCredential(
   legacyService: string | undefined,
   legacyAccount: string | undefined,
   label: string,
+  allowUnavailable = false,
 ): string | undefined {
-  if (service || account) return readSystemCredential(service, account, label);
-  return readLegacyMacKeychainCredential(legacyService, legacyAccount, label);
+  const backend = process.env.AGENT_CREDENTIAL_BACKEND?.trim();
+  if (backend === "env" || backend === "file") return undefined;
+  try {
+    if (service || account) return readSystemCredential(service, account, label);
+    return readLegacyMacKeychainCredential(legacyService, legacyAccount, label);
+  } catch (error) {
+    if (allowUnavailable) return undefined;
+    throw error;
+  }
 }
 
 function optionalConfiguredCredential(
@@ -530,6 +598,7 @@ export function resolveHubConfig(
   hubUsername?: string,
   hubPassword?: string,
   hubNodeToken?: string,
+  allowIncomplete = false,
 ): { hubEnabled: false } | {
   hubEnabled: true;
   hubUrl: string;
@@ -542,8 +611,16 @@ export function resolveHubConfig(
   const hasPassword = Boolean(hubUsername && hubPassword);
   const hasNodeToken = Boolean(hubNodeToken);
   const hasPartialUsername = Boolean(hubUsername && !hubPassword);
-  if ((hubUrl === undefined && (hasToken || hasPassword || hasNodeToken || hasPartialUsername)) ||
-      (hubUrl !== undefined && !hasToken && !hasPassword && !hasNodeToken && !hasPartialUsername)) {
+  if (
+    !allowIncomplete &&
+    ((hubUrl === undefined &&
+      (hasToken || hasPassword || hasNodeToken || hasPartialUsername)) ||
+      (hubUrl !== undefined &&
+        !hasToken &&
+        !hasPassword &&
+        !hasNodeToken &&
+        !hasPartialUsername))
+  ) {
     throw new Error(
       "AGENT_HUB_URL and Hub credentials must be configured together",
     );
diff --git a/agent-host/src/credential-store.ts b/agent-host/src/credential-store.ts
index 269bb1a..5e968b7 100644
--- a/agent-host/src/credential-store.ts
+++ b/agent-host/src/credential-store.ts
@@ -1,5 +1,15 @@
 import { execFileSync } from "node:child_process";
 import { createRequire } from "node:module";
+import {
+  chmodSync,
+  existsSync,
+  mkdirSync,
+  readFileSync,
+  renameSync,
+  statSync,
+  writeFileSync,
+} from "node:fs";
+import { dirname } from "node:path";
 
 // @napi-rs/keyring loads a native DLL on Windows. Eagerly importing it at
 // module scope pins node_modules files for the lifetime of the process, so
@@ -94,3 +104,108 @@ export function readLegacyMacKeychainCredential(
     throw new Error(`Could not load the ${label} credential from Keychain`);
   }
 }
+
+/**
+ * Read the explicit file-backed credential fallback. The file is intentionally
+ * a tiny dotenv subset so it can be inspected and rotated without adding a
+ * second configuration format.
+ */
+export function readCredentialFile(
+  path: string | undefined,
+  key: string,
+  label: string,
+): string | undefined {
+  if (!path || !existsSync(path)) return undefined;
+  assertCredentialFileMode(path, label);
+  const value = parseCredentialFile(readFileSync(path, "utf8"))[key];
+  if (value === undefined || !value.trim()) return undefined;
+  return value.trim();
+}
+
+/** Load all entries from an explicit credential file into the process env. */
+export function readCredentialEnv(path: string): Record<string, string> {
+  if (!existsSync(path)) return {};
+  assertCredentialFileMode(path, "credential file");
+  return parseCredentialFile(readFileSync(path, "utf8"));
+}
+
+/**
+ * Save one credential atomically, retaining other entries in the file. This
+ * function is only used after the user explicitly selected file storage.
+ */
+export function writeCredentialFile(
+  path: string,
+  key: string,
+  value: string,
+  label: string,
+): void {
+  if (!key || !/^[A-Z][A-Z0-9_]*$/u.test(key)) {
+    throw new Error(`${label} credential key is invalid`);
+  }
+  if (/\r|\n/u.test(value)) {
+    throw new Error(`${label} credential cannot contain newlines`);
+  }
+  mkdirSync(dirname(path), { recursive: true, mode: 0o700 });
+  if (process.platform !== "win32") chmodSync(dirname(path), 0o700);
+  const original = existsSync(path) ? readFileSync(path, "utf8") : "";
+  const lines = original.split(/\r?\n/u);
+  let replaced = false;
+  const next = lines.map((line) => {
+    const match = line.match(/^\s*([A-Z][A-Z0-9_]*)\s*=/u);
+    if (!match || match[1] !== key) return line;
+    replaced = true;
+    return `${key}=${dotenvValue(value)}`;
+  });
+  if (!replaced) {
+    if (next.at(-1)?.trim()) next.push("");
+    next.push(`${key}=${dotenvValue(value)}`);
+  }
+  const temporary = `${path}.${process.pid}.tmp`;
+  writeFileSync(temporary, `${next.join("\n").replace(/\n+$/u, "")}\n`, {
+    encoding: "utf8",
+    mode: 0o600,
+  });
+  if (process.platform !== "win32") chmodSync(temporary, 0o600);
+  renameSync(temporary, path);
+  if (process.platform !== "win32") chmodSync(path, 0o600);
+}
+
+function parseCredentialFile(text: string): Record<string, string> {
+  const values: Record<string, string> = {};
+  for (const rawLine of text.split(/\r?\n/u)) {
+    const line = rawLine.trim();
+    if (!line || line.startsWith("#") || !line.includes("=")) continue;
+    const separator = line.indexOf("=");
+    const key = line.slice(0, separator).trim();
+    let value = line.slice(separator + 1).trim();
+    if (
+      (value.startsWith('"') && value.endsWith('"')) ||
+      (value.startsWith("'") && value.endsWith("'"))
+    ) {
+      value = value.slice(1, -1);
+    }
+    if (key) values[key] = value;
+  }
+  return values;
+}
+
+function dotenvValue(value: string): string {
+  if (!value.includes('"')) return `"${value}"`;
+  if (!value.includes("'")) return `'${value}'`;
+  throw new Error("Credential values cannot contain both quote styles");
+}
+
+function assertCredentialFileMode(path: string, label: string): void {
+  if (process.platform === "win32") return;
+  let mode: number;
+  try {
+    mode = statSync(path).mode & 0o777;
+  } catch {
+    throw new Error(`Could not inspect the ${label}`);
+  }
+  if (mode !== 0o600) {
+    throw new Error(
+      `The ${label} must have permissions 0600; run chmod 600 ${path}`,
+    );
+  }
+}
diff --git a/agent-host/src/doctor.ts b/agent-host/src/doctor.ts
index b24d258..65fe0e3 100644
--- a/agent-host/src/doctor.ts
+++ b/agent-host/src/doctor.ts
@@ -1,56 +1,419 @@
-import { accessSync, constants, mkdirSync, statSync } from "node:fs";
+import { accessSync, constants, existsSync, statSync } from "node:fs";
+import { homedir } from "node:os";
+import { dirname, join, resolve } from "node:path";
+import { fileURLToPath } from "node:url";
 
 import type { AgentHostConfig } from "./config.js";
+import { defaultAgentConfigPath } from "./config.js";
 import type { HubClient } from "./hub-client.js";
 import { PiAgentEngine } from "./pi-engine.js";
 import { createWebSearchProvider, webSearchStatus } from "./web-search.js";
 
+export interface DoctorOptions {
+  live?: boolean;
+  json?: boolean;
+  hubError?: unknown;
+}
+
+interface DoctorCheck {
+  name: string;
+  status: "ok" | "warn" | "fail";
+  detail: string;
+  fix?: string;
+}
+
 export async function runDoctor(
   config: AgentHostConfig,
   hub?: HubClient,
+  options: DoctorOptions = {},
 ): Promise<void> {
-  if (!statSync(config.workspaceRoot).isDirectory()) {
-    throw new Error("AGENT_WORKSPACE_ROOT must be a directory");
-  }
-  accessSync(config.workspaceRoot, constants.R_OK | constants.W_OK);
-  mkdirSync(config.sessionDir, { recursive: true, mode: 0o700 });
-  accessSync(config.sessionDir, constants.R_OK | constants.W_OK);
-
-  const engine = await PiAgentEngine.create(config, hub);
-  const conversation = await engine.createConversation({
-    cwd: config.workspaceRoot,
-    mode: "diagnostic",
-    persisted: false,
-  });
-  try {
-    const response = await conversation.prompt(
-      "Connection check only. Reply with exactly AGENT_SETUP_OK and do not call tools.",
+  const checks: DoctorCheck[] = [];
+  const add = (
+    name: string,
+    status: DoctorCheck["status"],
+    detail: string,
+    fix?: string,
+  ) => {
+    checks.push({ name, status, detail, ...(fix ? { fix } : {}) });
+  };
+  const expectedConfig = defaultAgentConfigPath();
+  const configPath = config.configPath;
+  const configExists = configPath !== undefined && existsSync(configPath);
+  const legacyConfig =
+    configPath !== undefined &&
+    configExists &&
+    configPath !== expectedConfig &&
+    (configPath.endsWith("/.env.agent") ||
+      configPath.endsWith("/.private/env/agent.env") ||
+      configPath.endsWith("\\.env.agent") ||
+      configPath.endsWith("\\.private\\env\\agent.env"));
+
+  if (configExists && configPath && legacyConfig) {
+    add(
+      "configuration path",
+      "warn",
+      `${configPath} (legacy project config)`,
+      "agent setup",
+    );
+  } else if (configExists && configPath) {
+    const mode = fileMode(configPath);
+    add(
+      "configuration path",
+      mode === "warn" ? "warn" : "ok",
+      `${configPath}${mode === "warn" ? " (expected permissions 0600)" : ""}`,
+      mode === "warn" ? `chmod 600 ${quoteShell(configPath)}` : undefined,
+    );
+  } else {
+    add(
+      "configuration path",
+      "fail",
+      `not configured; expected ${expectedConfig}`,
+      "agent setup --mode local",
+    );
+  }
+
+  const modelConfigured = Boolean(
+    (config.piProvider && config.piModel) ||
+      (config.remoteBaseUrl && config.remoteModel) ||
+      process.env.AGENT_DSH_CONFIG?.trim() ||
+      process.env.AGENT_DSH_RUNTIME_BIN?.trim(),
+  );
+  if (modelConfigured) {
+    const model = config.remoteModel ?? config.piModel ?? config.dshModel ?? "configured runtime";
+    add("model configuration", "ok", `${config.piProvider ?? "remote"} / ${model}`);
+  } else {
+    add(
+      "model configuration",
+      "fail",
+      "no model provider, endpoint, or dsh runtime is configured",
+      "agent setup --mode local",
+    );
+  }
+
+  const backend = config.credentialBackend ?? "keychain";
+  if (backend === "file") {
+    const path = config.credentialFile ?? "configured credential file";
+    const mode = config.credentialFile ? fileMode(config.credentialFile) : "missing";
+    const fileError = config.credentialFileError;
+    add(
+      "credential backend",
+      fileError || mode === "missing" || mode === "warn" ? "fail" : "warn",
+      fileError || mode === "missing"
+        ? fileError ?? `credential file is missing: ${path}`
+        : mode === "warn"
+          ? `credential file has unsafe permissions: ${path}`
+          : `explicit file storage: ${path} (0600; security warning)`,
+      fileError || mode === "missing"
+        ? "agent setup --credentials file"
+        : mode === "warn"
+          ? `chmod 600 ${quoteShell(path)}`
+          : "agent setup --credentials auto  # switch back to the system store",
+    );
+  } else if (backend === "env") {
+    add(
+      "credential backend",
+      "warn",
+      "environment-only; secrets are not persisted",
+      "export AGENT_REMOTE_API_KEY before starting AgentSociety",
+    );
+  } else {
+    add(
+      "credential backend",
+      config.remoteApiKey || config.piProvider ? "ok" : "warn",
+      config.remoteApiKey || config.piProvider
+        ? "system credential store / existing provider"
+        : "system credential store selected, but no model credential was found",
+      config.remoteApiKey || config.piProvider
+        ? undefined
+        : "agent setup",
     );
-    if (!response.text.trim()) {
-      throw new Error("The remote model returned an empty response");
+  }
+
+  if (config.workspaceRoot && existsSync(config.workspaceRoot)) {
+    let writable = true;
+    try {
+      accessSync(config.workspaceRoot, constants.R_OK | constants.W_OK);
+    } catch {
+      writable = false;
+    }
+    add(
+      "workspace",
+      writable ? "ok" : "fail",
+      config.workspaceRoot,
+      writable
+        ? undefined
+        : `chmod u+rwX ${quoteShell(config.workspaceRoot)}`,
+    );
+  } else {
+    add(
+      "workspace",
+      "fail",
+      `missing: ${config.workspaceRoot}`,
+      `mkdir -p ${quoteShell(config.workspaceRoot)}`,
+    );
+  }
+
+  if (existsSync(config.sessionDir)) {
+    let writable = true;
+    try {
+      accessSync(config.sessionDir, constants.R_OK | constants.W_OK);
+    } catch {
+      writable = false;
+    }
+    add(
+      "session directory",
+      writable ? "ok" : "fail",
+      config.sessionDir,
+      writable
+        ? undefined
+        : `chmod u+rwX ${quoteShell(config.sessionDir)}`,
+    );
+  } else {
+    add(
+      "session directory",
+      "warn",
+      `will be created on first run: ${config.sessionDir}`,
+      `mkdir -m 700 -p ${quoteShell(config.sessionDir)}`,
+    );
+  }
+
+  add(
+    "permissions",
+    "ok",
+    `remote=${config.remoteToolPolicy}; dsh=${config.dshPermissionMode ?? "read-only"}`,
+  );
+  add(
+    "worker mode",
+    config.hubReceiveDisabled ? "warn" : "ok",
+    config.hubReceiveDisabled
+      ? "dispatch-only; this host will not receive remote tasks"
+      : config.hubEnabled
+        ? "Hub worker reception enabled"
+        : "local mode; Hub reception disabled",
+    config.hubReceiveDisabled ? "agent setup --mode hub-worker" : undefined,
+  );
+
+  if (config.hubEnabled && config.hubUrl) {
+    add("Hub configuration", "ok", config.hubUrl);
+    if (config.hubNodeToken || config.hubToken) {
+      add("Hub node credential", "ok", "present");
+    } else if (config.hubUsername && config.hubPassword) {
+      add(
+        "Hub node credential",
+        "warn",
+        "account configured; this node has not been registered yet",
+        "agent connect",
+      );
+    } else {
+      add(
+        "Hub node credential",
+        "fail",
+        "Hub URL is present but no account or node credential is available",
+        "agent setup --mode hub-worker",
+      );
     }
-  } finally {
-    await conversation.dispose();
+  } else {
+    add("Hub configuration", "ok", "disabled (local Agent mode)");
   }
 
-  console.log(
-    hub
-      ? `Hub connection:   ok (${config.hubUrl})`
-      : "Hub connection:   disabled (local Agent mode)",
+  const dshHome = resolve(process.env.DSH_HOME?.trim() || join(homedir(), ".dsh"));
+  const dshCommand = config.dshCommand?.[0] ?? "dsh";
+  add(
+    "dsh runtime",
+    executableInPath(dshCommand) ? "ok" : "warn",
+    executableInPath(dshCommand) ? dshCommand : `${dshCommand} not found on PATH`,
+    executableInPath(dshCommand) ? undefined : "agent doctor after installing the combo runtime",
   );
-  console.log(`Agent identity:   ${config.actorId} on ${config.nodeId}`);
-  console.log(`Workspace:        ok (${config.workspaceRoot})`);
-  console.log(`Session storage:  ok (${config.sessionDir})`);
-  console.log(`Remote model:     ok (${config.remoteModel ?? config.piModel})`);
-  console.log(
-    `Web search:       ${webSearchStatus(config.webSearchMode, createWebSearchProvider(config))}`,
+  add(
+    "worker profile",
+    existsSync(join(dshHome, "profiles", config.dshPluginProfile ?? "agent-society-worker", "package.json"))
+      ? "ok"
+      : "warn",
+    existsSync(join(dshHome, "profiles", config.dshPluginProfile ?? "agent-society-worker", "package.json"))
+      ? config.dshPluginProfile ?? "agent-society-worker"
+      : "dsh worker profile is missing",
+    existsSync(join(dshHome, "profiles", config.dshPluginProfile ?? "agent-society-worker", "package.json"))
+      ? undefined
+      : "agent doctor from the combo install root",
   );
-  console.log(
-    config.builtinCapabilitiesEnabled
-      ? "Agent tools:      ok (subagent, plan/todo, memory, LSP, MCP, background)"
-      : "Agent tools:      disabled (AGENT_BUILTIN_CAPABILITIES=0)",
+  add(
+    "web profile",
+    existsSync(join(dshHome, "profiles", config.dshWebProfile ?? "agent-society-web", "package.json"))
+      ? "ok"
+      : "warn",
+    existsSync(join(dshHome, "profiles", config.dshWebProfile ?? "agent-society-web", "package.json"))
+      ? config.dshWebProfile ?? "agent-society-web"
+      : "dsh web profile is missing",
+    existsSync(join(dshHome, "profiles", config.dshWebProfile ?? "agent-society-web", "package.json"))
+      ? undefined
+      : "agent web",
   );
-  console.log(
-    `Worker sessions:  ${config.workerSessionMode}`,
+
+  let searchStatus = "unavailable";
+  try {
+    searchStatus = webSearchStatus(
+      config.webSearchMode,
+      createWebSearchProvider(config),
+    );
+  } catch (error) {
+    searchStatus = error instanceof Error ? error.message : String(error);
+  }
+  add("web search", "ok", searchStatus);
+  add(
+    "built-in tools",
+    "ok",
+    config.builtinCapabilitiesEnabled ? "enabled" : "disabled",
   );
+
+  if (options.live) {
+    if (config.hubEnabled) {
+      if (hub) {
+        try {
+          await hub.listActors();
+          add("Hub live check", "ok", `authenticated as ${config.actorId}`);
+        } catch (error) {
+          add(
+            "Hub live check",
+            "fail",
+            error instanceof Error ? error.message : String(error),
+            "agent connect",
+          );
+        }
+      } else {
+        add(
+          "Hub live check",
+          "fail",
+          formatError(options.hubError ?? "could not create a Hub client"),
+          "agent connect",
+        );
+      }
+    } else {
+      add("Hub live check", "ok", "skipped (local mode)");
+    }
+
+    try {
+      const engine = await PiAgentEngine.create(config, hub);
+      const conversation = await engine.createConversation({
+        cwd: config.workspaceRoot,
+        mode: "diagnostic",
+        persisted: false,
+      });
+      try {
+        const response = await conversation.prompt(
+          "Connection check only. Reply with exactly AGENT_SETUP_OK and do not call tools.",
+        );
+        if (!response.text.trim()) {
+          throw new Error("the remote model returned an empty response");
+        }
+        add("model live check", "ok", "diagnostic response received");
+      } finally {
+        await conversation.dispose();
+      }
+    } catch (error) {
+      add(
+        "model live check",
+        "fail",
+        formatError(error),
+        "agent setup followed by agent doctor --live",
+      );
+    }
+
+    if (config.dshWebEnabled) {
+      const target = process.env.AGENT_DSH_WEB_TARGET?.trim() || "http://127.0.0.1:3080";
+      try {
+        const response = await fetch(target, {
+          signal: AbortSignal.timeout(5_000),
+        });
+        add(
+          "Web Bridge live check",
+          response.ok ? "ok" : "warn",
+          `${target} -> HTTP ${response.status}`,
+          response.ok ? undefined : "agent web",
+        );
+      } catch (error) {
+        add(
+          "Web Bridge live check",
+          "fail",
+          formatError(error),
+          "agent web",
+        );
+      }
+    } else {
+      add("Web Bridge live check", "ok", "disabled");
+    }
+  }
+
+  if (options.json) {
+    console.log(
+      JSON.stringify(
+        {
+          ok: checks.every((check) => check.status !== "fail"),
+          live: Boolean(options.live),
+          checks,
+        },
+        null,
+        2,
+      ),
+    );
+  } else {
+    for (const check of checks) {
+      console.log(
+        `[${check.status}] ${check.name}: ${check.detail}${check.fix ? ` — next: ${check.fix}` : ""}`,
+      );
+    }
+    console.log(
+      checks.some((check) => check.status === "fail")
+        ? "\nDoctor found failures."
+        : "\nDoctor checks passed (warnings may need attention).",
+    );
+  }
+  if (checks.some((check) => check.status === "fail")) process.exitCode = 1;
 }
+
+function fileMode(path: string | undefined): "ok" | "warn" | "missing" {
+  if (!path || !existsSync(path)) return "missing";
+  if (process.platform === "win32") return "ok";
+  try {
+    return (statSync(path).mode & 0o777) === 0o600 ? "ok" : "warn";
+  } catch {
+    return "missing";
+  }
+}
+
+function executableInPath(command: string): boolean {
+  if (command.includes("/") || command.includes("\\")) {
+    return isExecutable(command);
+  }
+  const separator = process.platform === "win32" ? ";" : ":";
+  const candidates =
+    process.platform === "win32" && !/\.(?:cmd|exe|bat)$/iu.test(command)
+      ? [command, `${command}.cmd`, `${command}.exe`, `${command}.bat`]
+      : [command];
+  for (const entry of (process.env.PATH ?? "").split(separator)) {
+    if (entry && candidates.some((candidate) => isExecutable(join(entry, candidate)))) {
+      return true;
+    }
+  }
+  return false;
+}
+
+function isExecutable(path: string): boolean {
+  if (!existsSync(path)) return false;
+  if (process.platform === "win32") return true;
+  try {
+    accessSync(path, constants.X_OK);
+    return true;
+  } catch {
+    return false;
+  }
+}
+
+function quoteShell(value: string): string {
+  return "'" + value.replace(/'/gu, "'\\''") + "'";
+}
+
+function formatError(error: unknown): string {
+  return error instanceof Error ? error.message : String(error);
+}
+
+const _sourceRoot = resolve(dirname(fileURLToPath(import.meta.url)), "..", "..", "..");
diff --git a/agent-host/src/dsh-child.ts b/agent-host/src/dsh-child.ts
index 0075839..7d5b282 100644
--- a/agent-host/src/dsh-child.ts
+++ b/agent-host/src/dsh-child.ts
@@ -19,6 +19,11 @@ export interface DshChildOptions {
    * worker self-update path).
    */
   onExit?: (code: number | null, signal: NodeJS.Signals | null) => boolean | void
+  /**
+   * Pipe the child stdout/stderr so the caller can extract the authenticated
+   * DSH Web URL printed by dsh web. The default remains inherit.
+   */
+  captureWebAuth?: boolean
 }
 
 export interface DshChildResult {
@@ -36,6 +41,8 @@ export interface ManagedDshChild {
   child: ChildProcess
   stop: () => void
   exited: Promise<DshChildResult>
+  /** Resolves with the authenticated DSH Web URL when captureWebAuth is enabled. */
+  authUrl?: Promise<string | undefined>
 }
 
 export function startDshChild(
@@ -44,7 +51,7 @@ export function startDshChild(
   options: DshChildOptions = {},
 ): ManagedDshChild {
   const child: ChildProcess = spawn(command[0]!, command.slice(1), {
-    stdio: 'inherit',
+    stdio: options.captureWebAuth === true ? ['ignore', 'pipe', 'pipe'] : 'inherit',
     env,
     ...(options.cwd !== undefined ? { cwd: options.cwd } : {}),
   })
@@ -75,6 +82,24 @@ export function startDshChild(
       child.kill('SIGTERM')
     }
   }
+  let resolveAuthUrl!: (url: string | undefined) => void
+  const authUrl = new Promise<string | undefined>((resolve) => { resolveAuthUrl = resolve })
+  if (options.captureWebAuth === true) {
+    child.stdout?.setEncoding('utf8')
+    child.stderr?.setEncoding('utf8')
+    const scan = (chunk: string): void => {
+      const match = /https?:\/\/[^\s?)"']+\?token=[^\s?)"']+/u.exec(chunk)
+      if (match !== null) resolveAuthUrl(match[0])
+    }
+    child.stdout?.on('data', scan)
+    child.stderr?.on('data', scan)
+    child.stdout?.on('data', (chunk: string) => process.stdout.write(chunk))
+    child.stderr?.on('data', (chunk: string) => process.stderr.write(chunk))
+    child.once('exit', () => resolveAuthUrl(undefined))
+    child.once('error', () => resolveAuthUrl(undefined))
+  } else {
+    resolveAuthUrl(undefined)
+  }
   process.once('SIGINT', forwardSignal)
   process.once('SIGTERM', forwardSignal)
   child.once('error', (error: Error) => {
@@ -89,7 +114,7 @@ export function startDshChild(
     }
     finish({ started: true, restart })
   })
-  return { child, stop, exited }
+  return { child, stop, exited, authUrl }
 }
 
 /**
diff --git a/agent-host/src/host.ts b/agent-host/src/host.ts
index 7ad5b13..e033124 100644
--- a/agent-host/src/host.ts
+++ b/agent-host/src/host.ts
@@ -105,7 +105,7 @@ export async function registerDshHost(
     ],
     metadata: {
       runtime: "dsh",
-      runtime_version: "0.1.0-rc.5",
+      runtime_version: "0.1.2-alpha.2",
       remote_tool_policy: config.remoteToolPolicy,
       worker_session_mode: config.workerSessionMode,
     },
diff --git a/agent-host/src/web-bridge.ts b/agent-host/src/web-bridge.ts
index 6bbb83d..349b356 100644
--- a/agent-host/src/web-bridge.ts
+++ b/agent-host/src/web-bridge.ts
@@ -20,6 +20,17 @@ export interface WebBridgeOptions {
   nodeId: string;
   /** Local dsh web origin, e.g. http://127.0.0.1:3080. Must be loopback. */
   target: string;
+  /**
+   * DSH Web launch URL printed by the local `dsh web` process. When provided,
+   * the bridge exchanges it for the DSH browser-session cookie and uses that
+   * cookie for all local HTTP and WebSocket requests.
+   */
+  dshAuthUrl?: string;
+  /**
+   * Pre-authenticated DSH session cookie (name=value only). Overrides
+   * dshAuthUrl for deployments that already have a cookie.
+   */
+  dshCookie?: string;
   /** Log line callback, defaults to console.log. */
   log?: (message: string) => void;
   /** Injectable WebSocket constructor, primarily for deterministic tests. */
@@ -34,6 +45,13 @@ const RECONNECT_DELAY_MS = 5_000;
 /** Cap for device-side response bodies, matching the Hub's proxy limit. */
 export const MAX_RESPONSE_BODY = 32 * 1024 * 1024;
 
+/** WebSocket constructor with an optional Node/undici headers option. */
+type LocalWebSocketCtor = new (
+  url: string,
+  protocols?: string | string[],
+  options?: { headers?: Record<string, string> },
+) => WebSocket;
+
 export function assertLoopbackTarget(target: string): string {
   const url = new URL(target);
   const host = url.hostname.replace(/^\[|\]$/gu, "").toLowerCase();
@@ -198,6 +216,7 @@ export class WebBridge {
   private readonly WebSocketImpl: typeof WebSocket;
   private readonly fetchImpl: typeof fetch;
   private readonly delayImpl: WebBridgeDelay;
+  private dshCookie: string | undefined;
   /** Hub-issued event stream id -> local device WebSocket. */
   private readonly eventStreams = new Map<string, WebSocket>();
   /** Tunnel socket which owns each local event stream. */
@@ -234,10 +253,12 @@ export class WebBridge {
     this.WebSocketImpl = options.webSocketImpl ?? WebSocket;
     this.fetchImpl = options.fetchImpl ?? fetch;
     this.delayImpl = options.delayImpl ?? delay;
+    this.dshCookie = options.dshCookie;
   }
 
   /** Run until stop() is called, reconnecting with backoff on failure. */
   async run(): Promise<void> {
+    await this.authenticate();
     while (!this.stopped) {
       try {
         await this.connectOnce();
@@ -413,6 +434,30 @@ export class WebBridge {
       }
       return;
     }
+    if (message.type === "ws-client-frame") {
+      const streamId = String(message.id ?? "");
+      const local = this.eventStreams.get(streamId);
+      if (
+        local === undefined ||
+        this.eventStreamOwners.get(streamId) !== ws
+      ) {
+        return;
+      }
+      try {
+        const payload = Buffer.from(
+          String(message.payload_b64 ?? ""),
+          "base64",
+        );
+        if (Number(message.opcode ?? 1) === 0x2) {
+          local.send(payload);
+        } else {
+          local.send(payload.toString("utf8"));
+        }
+      } catch {
+        // The local socket is already closing.
+      }
+      return;
+    }
     if (message.type !== "http") return;
     const requestId = String(message.id ?? "");
     if (!requestId) return;
@@ -430,7 +475,7 @@ export class WebBridge {
         // Ask the local server for an identity body: Node's fetch transparently
         // decompresses gzip while keeping the content-encoding header, which
         // would make the Hub/browser try to decode plaintext.
-        headers: { ...headers, "accept-encoding": "identity" },
+        headers: this.localHeaders({ ...headers, "accept-encoding": "identity" }),
         ...(body !== undefined ? { body } : {}),
         signal: this.abortController.signal,
       });
@@ -472,12 +517,12 @@ export class WebBridge {
     });
   }
 
-  /** Open one device-local DSH event downlink and relay its frames to the Hub. */
+  /** Open one device-local DSH remote.mux and relay frames in both directions. */
   private openEventStream(ws: WebSocket, message: Record<string, unknown>): void {
     const streamId = String(message.id ?? "");
     if (!streamId) return;
     const path = String(message.path ?? "/");
-    if (!/^\/api\/events\.(mux|host)$/u.test(path)) {
+    if (path !== "/api/remote.mux") {
       this.sendTunnel(ws, {
         type: "ws-open-ack",
         id: streamId,
@@ -488,7 +533,13 @@ export class WebBridge {
     }
     let local: WebSocket;
     try {
-      local = new this.WebSocketImpl(`${this.target}${path}`);
+      local = new (this.WebSocketImpl as LocalWebSocketCtor)(
+          `${this.target}${path}`,
+          undefined,
+          this.dshCookie === undefined
+            ? undefined
+            : { headers: { cookie: this.dshCookie } },
+        );
     } catch (error) {
       this.sendTunnel(ws, {
         type: "ws-open-ack",
@@ -584,6 +635,35 @@ export class WebBridge {
     }, { once: true });
   }
 
+  async authenticate(): Promise<string | undefined> {
+    if (this.dshCookie !== undefined) return this.dshCookie;
+    if (this.options.dshAuthUrl === undefined) {
+      this.log("web-bridge no DSH auth URL; continuing without a local session cookie");
+      return undefined;
+    }
+    const response = await this.fetchImpl(this.options.dshAuthUrl, {
+      method: "GET",
+      redirect: "manual",
+      signal: this.abortController.signal,
+    });
+    const headers = response.headers as unknown as {
+      getSetCookie?: () => string[];
+    };
+    const raw = headers.getSetCookie?.()[0] ?? response.headers.get("set-cookie");
+    if (!raw) {
+      this.log("web-bridge DSH auth exchange did not set a session cookie");
+      return undefined;
+    }
+    this.dshCookie = raw.split(";", 1)[0]!.trim();
+    this.log("web-bridge authenticated local dsh web");
+    return this.dshCookie;
+  }
+
+  private localHeaders(headers: Record<string, string>): Record<string, string> {
+    if (this.dshCookie === undefined) return headers;
+    return { ...headers, cookie: this.dshCookie };
+  }
+
   private async fetchTicket(signal: AbortSignal): Promise<string> {
     const response = await this.fetchImpl(
       `${this.options.hubUrl}/v1/hub/nodes/web/tunnel`,
diff --git a/agent-host/src/worker-session-registry.ts b/agent-host/src/worker-session-registry.ts
index 69695ed..d55c080 100644
--- a/agent-host/src/worker-session-registry.ts
+++ b/agent-host/src/worker-session-registry.ts
@@ -6,6 +6,7 @@ import {
   readFileSync,
   renameSync,
   writeFileSync,
+  unlinkSync,
 } from "node:fs";
 import { join, relative, resolve } from "node:path";
 
@@ -103,12 +104,16 @@ export class WorkerSessionRegistry {
     chmodSync(this.recordsDir, 0o700);
     const target = this.recordPath(record.key);
     const temporary = `${target}.${process.pid}.${randomUUID()}.tmp`;
-    writeFileSync(temporary, `${JSON.stringify(record, null, 2)}\n`, {
-      encoding: "utf8",
-      mode: 0o600,
-    });
-    chmodSync(temporary, 0o600);
-    renameSync(temporary, target);
+    try {
+      writeFileSync(temporary, `${JSON.stringify(record, null, 2)}\n`, {
+        encoding: "utf8",
+        mode: 0o600,
+      });
+      chmodSync(temporary, 0o600);
+      renameSync(temporary, target);
+    } finally {
+      try { unlinkSync(temporary); } catch { /* rename completed or temp was never created */ }
+    }
   }
 }
 
diff --git a/agent-host/src/worker.ts b/agent-host/src/worker.ts
index 563ce60..1cfbaa2 100644
--- a/agent-host/src/worker.ts
+++ b/agent-host/src/worker.ts
@@ -24,6 +24,20 @@ import {
   type WorkerSessionScope,
 } from "./worker-session-registry.js";
 
+function isMissingSessionError(error: unknown): boolean {
+  const code = (error as NodeJS.ErrnoException | null)?.code;
+  if (code === "EACCES" || code === "EPERM" || code === "EIO") return false;
+  if (code === "ENOENT") return true;
+  const text = errorMessage(error);
+  if (/(?:corrupt|invalid|malformed|permission|access denied|i\/o|input\/output|writer lock|write conflict)/iu.test(text)) {
+    return false;
+  }
+  return [
+    /(?:session|artifact|file).*?(?:not found|does not exist|no such file)/iu,
+    /(?:not found|does not exist|no such file).*?(?:session|artifact|file)/iu,
+  ].some((pattern) => pattern.test(text));
+}
+
 type WorkerHub = Pick<
   HubClient,
   "claimTask" | "updateTask" | "updateRun" | "heartbeat"
@@ -463,8 +477,9 @@ export class TaskWorker {
           `Resumed continuous ${this.engineProfile.label} session ${conversation.sessionId} for worker ${this.workerSlot + 1}`,
         );
       } catch (error) {
+        if (!isMissingSessionError(error)) throw error;
         this.output(
-          `Continuous ${this.engineProfile.label} session recovery failed; creating a new session: ${errorMessage(error)}`,
+          `Continuous ${this.engineProfile.label} session is unavailable; starting a replacement: ${errorMessage(error)}`,
         );
       }
     }
diff --git a/agent-host/test/worker.test.ts b/agent-host/test/worker.test.ts
index bfb3d7d..e8694e3 100644
--- a/agent-host/test/worker.test.ts
+++ b/agent-host/test/worker.test.ts
@@ -1,8 +1,7 @@
 import assert from "node:assert/strict";
-import { Entry } from "@napi-rs/keyring";
 import { spawnSync } from "node:child_process";
-import { randomUUID } from "node:crypto";
 import {
+  existsSync,
   mkdirSync,
   mkdtempSync,
   readFileSync,
@@ -15,7 +14,9 @@ import { basename, join, resolve } from "node:path";
 import { afterEach, test } from "node:test";
 
 import {
+  agentEnvPath,
   assertRemoteUrl,
+  defaultAgentConfigPath,
   discoverProjectEnv,
   resolveHubConfig,
   type AgentHostConfig,
@@ -31,18 +32,10 @@ import {
 import { resolveTaskWorkspace, TaskWorker } from "../src/worker.js";
 
 const temporaryDirectories: string[] = [];
-const temporaryCredentials: Entry[] = [];
 afterEach(() => {
   for (const path of temporaryDirectories.splice(0)) {
     rmSync(path, { recursive: true, force: true });
   }
-  for (const entry of temporaryCredentials.splice(0)) {
-    try {
-      entry.deletePassword();
-    } catch {
-      // A failed setup may not have created the credential.
-    }
-  }
 });
 
 function temporaryDirectory(): string {
@@ -51,22 +44,6 @@ function temporaryDirectory(): string {
   return path;
 }
 
-function temporaryCredentialNamespace(): {
-  namespace: string;
-  account: string;
-} {
-  return {
-    namespace: `AgentSociety Test ${randomUUID()}`,
-    account: `test-${randomUUID()}`,
-  };
-}
-
-function trackedCredential(service: string, account: string): Entry {
-  const entry = new Entry(service, account);
-  temporaryCredentials.push(entry);
-  return entry;
-}
-
 function config(workspaceRoot: string): AgentHostConfig {
   return {
     hubEnabled: true,
@@ -708,23 +685,35 @@ test("agent-specific environment takes precedence over the legacy project env",
   assert.equal(discoverProjectEnv(host), join(repository, ".env"));
 });
 
+test("central AgentSociety configuration path follows the platform config home", () => {
+  const configHome = temporaryDirectory();
+  const previous = process.env.XDG_CONFIG_HOME;
+  process.env.XDG_CONFIG_HOME = configHome;
+  try {
+    assert.equal(
+      defaultAgentConfigPath(),
+      join(configHome, "agentsociety", "agent.env"),
+    );
+    assert.equal(agentEnvPath(temporaryDirectory()), defaultAgentConfigPath());
+  } finally {
+    if (previous === undefined) delete process.env.XDG_CONFIG_HOME;
+    else process.env.XDG_CONFIG_HOME = previous;
+  }
+});
+
 test("setup stores the model key outside its non-secret configuration", () => {
   const workspace = temporaryDirectory();
   const configPath = join(workspace, ".env.agent");
   const script = resolve(process.cwd(), "scripts/setup.mjs");
-  const credential = temporaryCredentialNamespace();
-  const modelEntry = trackedCredential(
-    `${credential.namespace} Remote LLM API`,
-    credential.account,
-  );
-  const result = spawnSync(process.execPath, [script, "--configure-only"], {
+  const result = spawnSync(
+    process.execPath,
+    [script, "--configure-only", "--credentials", "file"],
+    {
     cwd: workspace,
     env: {
       ...process.env,
       AGENT_SETUP_CONFIG_PATH: configPath,
       AGENT_SETUP_WORKSPACE: workspace,
-      AGENT_CREDENTIAL_NAMESPACE: credential.namespace,
-      AGENT_CREDENTIAL_ACCOUNT: credential.account,
       AGENT_HUB_URL: "",
       AGENT_HUB_TOKEN: "",
       AGENT_REMOTE_BASE_URL: "https://model.test.invalid/v1",
@@ -732,28 +721,29 @@ test("setup stores the model key outside its non-secret configuration", () => {
       AGENT_REMOTE_API_KEY: "test-model-key",
     },
     encoding: "utf8",
-  });
+    },
+  );
   assert.equal(result.status, 0, result.stderr);
   const contents = readFileSync(configPath, "utf8");
   assert.doesNotMatch(contents, /AGENT_HUB_/u);
   assert.doesNotMatch(contents, /test-model-key/u);
   assert.doesNotMatch(contents, /^AGENT_REMOTE_API_KEY=/mu);
   assert.match(contents, /AGENT_REMOTE_MODEL="test-model"/u);
-  assert.ok(
-    contents.includes(
-      `AGENT_REMOTE_API_KEY_CREDENTIAL_SERVICE="${credential.namespace} Remote LLM API"`,
-    ),
+  assert.match(contents, /AGENT_CREDENTIAL_BACKEND="file"/u);
+  assert.match(
+    readFileSync(join(workspace, "credentials.env"), "utf8"),
+    /AGENT_REMOTE_API_KEY="test-model-key"/u,
   );
-  assert.equal(modelEntry.getPassword(), "test-model-key");
   assert.doesNotMatch(`${result.stdout}${result.stderr}`, /test-model-key/u);
-  const repeated = spawnSync(process.execPath, [script, "--configure-only"], {
+  const repeated = spawnSync(
+    process.execPath,
+    [script, "--configure-only", "--credentials", "file"],
+    {
     cwd: workspace,
     env: {
       ...process.env,
       AGENT_SETUP_CONFIG_PATH: configPath,
       AGENT_SETUP_WORKSPACE: workspace,
-      AGENT_CREDENTIAL_NAMESPACE: credential.namespace,
-      AGENT_CREDENTIAL_ACCOUNT: credential.account,
       AGENT_HUB_URL: "",
       AGENT_HUB_TOKEN: "",
       AGENT_REMOTE_BASE_URL: "",
@@ -761,7 +751,8 @@ test("setup stores the model key outside its non-secret configuration", () => {
       AGENT_REMOTE_API_KEY: "",
     },
     encoding: "utf8",
-  });
+    },
+  );
   assert.equal(repeated.status, 0, repeated.stderr);
   assert.doesNotMatch(
     readFileSync(configPath, "utf8"),
@@ -770,30 +761,23 @@ test("setup stores the model key outside its non-secret configuration", () => {
   assert.match(contents, new RegExp(`AGENT_WORKSPACE_ROOT="${workspace}"`, "u"));
   if (process.platform !== "win32") {
     assert.equal(statSync(configPath).mode & 0o777, 0o600);
+    assert.equal(statSync(join(workspace, "credentials.env")).mode & 0o777, 0o600);
   }
 });
 
-test("setup stores model and Hub secrets only in the system credential store", () => {
+test("setup stores model and Hub secrets only in an explicit 0600 credential file", () => {
   const workspace = temporaryDirectory();
   const configPath = join(workspace, ".env.agent");
   const script = resolve(process.cwd(), "scripts/setup.mjs");
-  const credential = temporaryCredentialNamespace();
-  const modelEntry = trackedCredential(
-    `${credential.namespace} Remote LLM API`,
-    credential.account,
-  );
-  const hubEntry = trackedCredential(
-    `${credential.namespace} Hub`,
-    credential.account,
-  );
-  const result = spawnSync(process.execPath, [script, "--configure-only"], {
+  const result = spawnSync(
+    process.execPath,
+    [script, "--configure-only", "--credentials", "file"],
+    {
     cwd: workspace,
     env: {
       ...process.env,
       AGENT_SETUP_CONFIG_PATH: configPath,
       AGENT_SETUP_WORKSPACE: workspace,
-      AGENT_CREDENTIAL_NAMESPACE: credential.namespace,
-      AGENT_CREDENTIAL_ACCOUNT: credential.account,
       AGENT_HUB_URL: "https://hub.test.invalid",
       AGENT_HUB_TOKEN: "test-hub-token-with-24-characters",
       AGENT_REMOTE_BASE_URL: "https://model.test.invalid/v1",
@@ -801,19 +785,26 @@ test("setup stores model and Hub secrets only in the system credential store", (
       AGENT_REMOTE_API_KEY: "test-model-key",
     },
     encoding: "utf8",
-  });
+    },
+  );
   assert.equal(result.status, 0, result.stderr);
   const contents = readFileSync(configPath, "utf8");
   assert.match(contents, /AGENT_HUB_URL="https:\/\/hub\.test\.invalid"/u);
   assert.doesNotMatch(contents, /test-model-key|test-hub-token-with-24-characters/u);
   assert.doesNotMatch(contents, /^AGENT_(?:REMOTE_API_KEY|HUB_TOKEN)=/mu);
-  assert.ok(
-    contents.includes(
-      `AGENT_HUB_TOKEN_CREDENTIAL_SERVICE="${credential.namespace} Hub"`,
-    ),
+  assert.match(contents, /AGENT_CREDENTIAL_BACKEND="file"/u);
+  const credentialContents = readFileSync(
+    join(workspace, "credentials.env"),
+    "utf8",
+  );
+  assert.match(credentialContents, /AGENT_REMOTE_API_KEY="test-model-key"/u);
+  assert.match(
+    credentialContents,
+    /AGENT_HUB_TOKEN="test-hub-token-with-24-characters"/u,
   );
-  assert.equal(modelEntry.getPassword(), "test-model-key");
-  assert.equal(hubEntry.getPassword(), "test-hub-token-with-24-characters");
+  if (process.platform !== "win32") {
+    assert.equal(statSync(join(workspace, "credentials.env")).mode & 0o777, 0o600);
+  }
   assert.doesNotMatch(
     `${result.stdout}${result.stderr}`,
     /test-model-key|test-hub-token-with-24-characters/u,
@@ -824,15 +815,6 @@ test("setup migrates legacy plaintext secrets out of an existing config", () =>
   const workspace = temporaryDirectory();
   const configPath = join(workspace, ".env.agent");
   const script = resolve(process.cwd(), "scripts/setup.mjs");
-  const credential = temporaryCredentialNamespace();
-  const modelEntry = trackedCredential(
-    `${credential.namespace} Remote LLM API`,
-    credential.account,
-  );
-  const hubEntry = trackedCredential(
-    `${credential.namespace} Hub`,
-    credential.account,
-  );
   writeFileSync(
     configPath,
     [
@@ -841,17 +823,19 @@ test("setup migrates legacy plaintext secrets out of an existing config", () =>
       "AGENT_REMOTE_API_KEY=legacy-model-secret",
       "AGENT_HUB_URL=https://hub.test.invalid",
       "AGENT_HUB_TOKEN=legacy-hub-token-with-24-characters",
+      "AGENT_HUB_NODE_TOKEN=legacy-node-token",
       "",
     ].join("\n"),
   );
-  const result = spawnSync(process.execPath, [script, "--configure-only"], {
+  const result = spawnSync(
+    process.execPath,
+    [script, "--configure-only", "--credentials", "file"],
+    {
     cwd: workspace,
     env: {
       ...process.env,
       AGENT_SETUP_CONFIG_PATH: configPath,
       AGENT_SETUP_WORKSPACE: workspace,
-      AGENT_CREDENTIAL_NAMESPACE: credential.namespace,
-      AGENT_CREDENTIAL_ACCOUNT: credential.account,
       AGENT_HUB_URL: "",
       AGENT_HUB_TOKEN: "",
       AGENT_REMOTE_BASE_URL: "",
@@ -860,13 +844,166 @@ test("setup migrates legacy plaintext secrets out of an existing config", () =>
       LLM_API_KEY: "",
     },
     encoding: "utf8",
-  });
+    },
+  );
   assert.equal(result.status, 0, result.stderr);
   const contents = readFileSync(configPath, "utf8");
-  assert.doesNotMatch(contents, /legacy-model-secret|legacy-hub-token/u);
-  assert.doesNotMatch(contents, /^AGENT_(?:REMOTE_API_KEY|HUB_TOKEN)=/mu);
-  assert.equal(modelEntry.getPassword(), "legacy-model-secret");
-  assert.equal(hubEntry.getPassword(), "legacy-hub-token-with-24-characters");
+  assert.doesNotMatch(
+    contents,
+    /legacy-model-secret|legacy-hub-token|legacy-node-token/u,
+  );
+  assert.doesNotMatch(
+    contents,
+    /^AGENT_(?:REMOTE_API_KEY|HUB_TOKEN|HUB_NODE_TOKEN)=/mu,
+  );
+  const credentialContents = readFileSync(
+    join(workspace, "credentials.env"),
+    "utf8",
+  );
+  assert.match(credentialContents, /AGENT_REMOTE_API_KEY="legacy-model-secret"/u);
+  assert.match(
+    credentialContents,
+    /AGENT_HUB_TOKEN="legacy-hub-token-with-24-characters"/u,
+  );
+  assert.match(credentialContents, /AGENT_HUB_NODE_TOKEN="legacy-node-token"/u);
+});
+
+test("setup supports explicit environment-only credentials", () => {
+  const workspace = temporaryDirectory();
+  const configPath = join(workspace, ".env.agent");
+  const script = resolve(process.cwd(), "scripts/setup.mjs");
+  const result = spawnSync(
+    process.execPath,
+    [script, "--configure-only", "--credentials", "env", "--non-interactive"],
+    {
+      cwd: workspace,
+      env: {
+        ...process.env,
+        AGENT_SETUP_CONFIG_PATH: configPath,
+        AGENT_SETUP_WORKSPACE: workspace,
+        AGENT_SETUP_MODE: "local",
+        AGENT_HUB_URL: "",
+        AGENT_REMOTE_BASE_URL: "https://model.test.invalid/v1",
+        AGENT_REMOTE_MODEL: "test-model",
+        AGENT_REMOTE_API_KEY: "env-only-model-key",
+      },
+      encoding: "utf8",
+    },
+  );
+  assert.equal(result.status, 0, result.stderr);
+  const contents = readFileSync(configPath, "utf8");
+  assert.match(contents, /AGENT_CREDENTIAL_BACKEND="env"/u);
+  assert.doesNotMatch(contents, /env-only-model-key/u);
+  assert.equal(existsSync(join(workspace, "credentials.env")), false);
+  assert.doesNotMatch(`${result.stdout}${result.stderr}`, /env-only-model-key/u);
+});
+
+test("setup preserves a legacy remote permission while filling the DSH value", () => {
+  const workspace = temporaryDirectory();
+  const configPath = join(workspace, ".env.agent");
+  const script = resolve(process.cwd(), "scripts/setup.mjs");
+  writeFileSync(
+    configPath,
+    [
+      "AGENT_REMOTE_BASE_URL=https://model.test.invalid/v1",
+      "AGENT_REMOTE_MODEL=test-model",
+      "AGENT_REMOTE_API_KEY=legacy-model-secret",
+      "AGENT_REMOTE_TOOL_POLICY=read_only",
+      "",
+    ].join("\n"),
+  );
+  const result = spawnSync(
+    process.execPath,
+    [script, "--mode", "local", "--credentials", "file", "--non-interactive"],
+    {
+      cwd: workspace,
+      env: {
+        ...process.env,
+        AGENT_SETUP_CONFIG_PATH: configPath,
+        AGENT_SETUP_WORKSPACE: workspace,
+        AGENT_REMOTE_BASE_URL: "",
+        AGENT_REMOTE_MODEL: "",
+        AGENT_REMOTE_API_KEY: "",
+        AGENT_HUB_URL: "",
+      },
+      encoding: "utf8",
+    },
+  );
+  assert.equal(result.status, 0, result.stderr);
+  const contents = readFileSync(configPath, "utf8");
+  assert.match(contents, /AGENT_REMOTE_TOOL_POLICY="read_only"/u);
+  assert.match(contents, /AGENT_DSH_PERMISSION_MODE="read-only"/u);
+  assert.doesNotMatch(contents, /legacy-model-secret/u);
+});
+
+test("setup marks dispatch-only mode without creating a credential file", () => {
+  const workspace = temporaryDirectory();
+  const configPath = join(workspace, ".env.agent");
+  const script = resolve(process.cwd(), "scripts/setup.mjs");
+  const result = spawnSync(
+    process.execPath,
+    [script, "--mode", "dispatch-only", "--credentials", "env", "--non-interactive", "--configure-only"],
+    {
+      cwd: workspace,
+      env: {
+        ...process.env,
+        AGENT_SETUP_CONFIG_PATH: configPath,
+        AGENT_SETUP_WORKSPACE: workspace,
+        AGENT_REMOTE_BASE_URL: "https://model.test.invalid/v1",
+        AGENT_REMOTE_MODEL: "test-model",
+        AGENT_REMOTE_API_KEY: "env-model-key",
+        AGENT_HUB_URL: "https://hub.test.invalid",
+        AGENT_HUB_USERNAME: "worker",
+        AGENT_HUB_PASSWORD: "hub-password",
+      },
+      encoding: "utf8",
+    },
+  );
+  assert.equal(result.status, 0, result.stderr);
+  const contents = readFileSync(configPath, "utf8");
+  assert.match(contents, /AGENT_HUB_RECEIVE_DISABLED="1"/u);
+  assert.match(contents, /AGENT_HUB_URL="https:\/\/hub\.test\.invalid"/u);
+  assert.match(contents, /AGENT_CREDENTIAL_BACKEND="env"/u);
+  assert.equal(existsSync(join(workspace, "credentials.env")), false);
+  assert.doesNotMatch(`${result.stdout}${result.stderr}`, /env-model-key|hub-password/u);
+});
+
+test("static doctor does not execute a model request", () => {
+  const workspace = temporaryDirectory();
+  const configPath = join(workspace, "agent.env");
+  writeFileSync(
+    configPath,
+    [
+      `AGENT_REMOTE_BASE_URL="https://model.test.invalid/v1"`,
+      `AGENT_REMOTE_MODEL="test-model"`,
+      `AGENT_WORKSPACE_ROOT="${workspace}"`,
+      `AGENT_SESSION_DIR="${join(workspace, "sessions")}"`,
+      `AGENT_REMOTE_TOOL_POLICY="full"`,
+      `AGENT_DSH_PERMISSION_MODE="workspace-write"`,
+      "",
+    ].join("\n"),
+  );
+  const result = spawnSync(
+    process.execPath,
+    [resolve(process.cwd(), "dist/src/cli.js"), "doctor", "--json"],
+    {
+      cwd: process.cwd(),
+      env: {
+        ...process.env,
+        AGENT_ENV_FILE: configPath,
+        AGENT_SESSION_DIR: join(workspace, "sessions"),
+        DSH_HOME: join(workspace, "dsh"),
+      },
+      encoding: "utf8",
+    },
+  );
+  assert.equal(result.status, 0, result.stderr);
+  const report = JSON.parse(result.stdout) as {
+    live: boolean;
+    checks: Array<{ name: string }>;
+  };
+  assert.equal(report.live, false);
+  assert.equal(report.checks.some((check) => check.name === "model live check"), false);
 });
 
 test("run registry resolves run, task, and session identifiers", () => {
diff --git a/agent.ps1 b/agent.ps1
index e90f5c0..481f627 100644
--- a/agent.ps1
+++ b/agent.ps1
@@ -17,24 +17,47 @@ $NpmCommand = if (Get-Command npm.cmd -ErrorAction SilentlyContinue) {
     throw "Node.js 22.19 or newer and npm are required."
 }
 
-if ($Command -eq "setup") {
+$Modules = Join-Path $AgentHost "node_modules"
+$Entrypoint = Join-Path $AgentHost "dist/src/cli.js"
+
+function Get-AgentConfigPath {
+    if ($env:AGENT_ENV_FILE) { return $env:AGENT_ENV_FILE }
+    if ($env:XDG_CONFIG_HOME) { return Join-Path $env:XDG_CONFIG_HOME "agentsociety\agent.env" }
+    if ($env:APPDATA) { return Join-Path $env:APPDATA "AgentSociety\agent.env" }
+    return Join-Path $HOME ".config\agentsociety\agent.env"
+}
+
+function Test-AgentConfig {
+    return (
+        (Test-Path (Get-AgentConfigPath)) -or
+        (Test-Path (Join-Path $RepositoryRoot ".private\env\agent.env")) -or
+        (Test-Path (Join-Path $RepositoryRoot ".env.agent"))
+    )
+}
+
+function Invoke-AgentBootstrap {
+    param([switch]$Force)
+    if ($Force -or -not (Test-Path $Modules) -or -not (Test-Path $Entrypoint)) {
+        & $NpmCommand --prefix $AgentHost run bootstrap
+        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
+    }
+}
+
+if ($Command -in "bootstrap", "repair") {
+    Invoke-AgentBootstrap -Force
+    exit $LASTEXITCODE
+}
+
+if ($Command -in "setup", "configure") {
+    Invoke-AgentBootstrap
     & $NpmCommand --prefix $AgentHost run setup -- @Rest
     exit $LASTEXITCODE
 }
 
-$Config = Join-Path $RepositoryRoot ".env.agent"
-$PrivateConfig = Join-Path $RepositoryRoot ".private\env\agent.env"
-$Modules = Join-Path $AgentHost "node_modules"
-$Entrypoint = Join-Path $AgentHost "dist/src/cli.js"
-$CompletionMarker = Join-Path $AgentHost ".setup-complete"
-$DidSetup = $false
-if (-not (Test-Path $CompletionMarker) -or (-not (Test-Path $Config) -and -not (Test-Path $PrivateConfig)) -or -not (Test-Path $Modules) -or -not (Test-Path $Entrypoint)) {
+Invoke-AgentBootstrap
+if ($Command -ne "doctor" -and $Command -ne "sessions" -and -not (Test-AgentConfig)) {
     & $NpmCommand --prefix $AgentHost run setup
     if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
-    $DidSetup = $true
-}
-if ($Command -eq "doctor" -and $DidSetup) {
-    exit 0
 }
 
 switch ($Command) {
@@ -43,14 +66,15 @@ switch ($Command) {
         break
     }
     "worker" { & $NpmCommand --prefix $AgentHost run worker; break }
-    "web" { & $NpmCommand --prefix $AgentHost run start -- "web" @Rest; break }
-    { $_ -in "doctor", "sessions", "dsh-doctor" } { & $NpmCommand --prefix $AgentHost run $Command; break }
-    { $_ -in "register", "once", "observe", "attach", "bridge", "dsh-worker", "dsh-once", "dsh-dispatch", "dsh-doctor" } {
+    { $_ -in "web", "web-bridge" } { & $NpmCommand --prefix $AgentHost run start -- $Command @Rest; break }
+    "doctor" { & node $Entrypoint "doctor" @Rest; break }
+    { $_ -in "sessions", "dsh-doctor" } { & $NpmCommand --prefix $AgentHost run start -- $Command @Rest; break }
+    { $_ -in "register", "once", "observe", "attach", "bridge", "connect", "dsh-worker", "dsh-once", "dsh-dispatch" } {
         & $NpmCommand --prefix $AgentHost run start -- $Command @Rest
         break
     }
     default {
-        throw "Usage: .\\agent.ps1 [setup|tui|web|local|worker|connect|bridge --adapter ID|doctor|dsh-doctor|sessions|register|once|observe ID|attach ID|steer|follow-up|cancel|control|dsh-worker|dsh-once|dsh-dispatch]"
+        throw "Usage: .\agent.ps1 [setup|configure|bootstrap|repair|tui|web|web-bridge|local|worker|connect|bridge --adapter ID|doctor [--live] [--json]|dsh-doctor|sessions|register|once|observe ID|attach ID|steer|follow-up|cancel|control|dsh-worker|dsh-once|dsh-dispatch]"
     }
 }
 exit $LASTEXITCODE
diff --git a/deploy/launchd/README.md b/deploy/launchd/README.md
index c72db69..7fa243e 100644
--- a/deploy/launchd/README.md
+++ b/deploy/launchd/README.md
@@ -17,8 +17,8 @@ the real plist contains absolute machine paths and must be generated locally
 mkdir -p ~/Library/LaunchAgents
 sed -e 's|__NODE__|/opt/homebrew/bin/node|g' \
     -e 's|__CLI__|/Users/YOU/Documents/AgentSociety/agent-host/dist/src/cli.js|g' \
-    -e 's|__ENV_FILE__|/Users/YOU/Documents/AgentSociety/.private/env/agent.env|g' \
-    -e 's|__WORKSPACE__|/Users/YOU/Documents/AgentSociety|g' \
+    -e 's|__ENV_FILE__|/Users/YOU/.config/agentsociety/agent.env|g' \
+    -e 's|__WORKSPACE__|/Users/YOU|g' \
     -e 's|__LOG_DIR__|/Users/YOU/.pi/agent/logs|g' \
     deploy/launchd/ai.agentsociety.agent-worker.plist.example \
     > ~/Library/LaunchAgents/ai.agentsociety.agent-worker.plist
@@ -34,9 +34,10 @@ launchctl bootout gui/$(id -u)/ai.agentsociety.agent-worker
   `ProgramArguments` back to the `agent` shell wrapper: launchd cannot exec
   scripts located under `~/Documents` on this machine (EPERM), while node +
   `dist/src/cli.js` works.
-- The worker loads `.private/env/agent.env` (legacy `.env.agent` still works)
-  through `AGENT_ENV_FILE`; the Hub token is
-  stored in the macOS keychain under the configured credential service.
+- The worker loads the user-level AgentSociety config through `AGENT_ENV_FILE`
+  (legacy `.private/env/agent.env` / `.env.agent` still works). Run
+  `agent setup --mode hub-worker` first; the Hub node credential is stored in
+  the macOS keychain under the configured credential service.
 - `AGENT_SELF_UPDATE` defaults to on, so the worker follows `origin/main`.
   Set it to `0` only when pinning a specific build (e.g. while a newer main
   is known-broken); remove the pin once main is healthy again.
diff --git a/docs/agent-platform.md b/docs/agent-platform.md
index 9b88f8a..a016579 100644
--- a/docs/agent-platform.md
+++ b/docs/agent-platform.md
@@ -194,29 +194,31 @@ override 无法越过该 shrinkwrap。因此 setup 直接锁定安全版 5.0.9
 Pi 的这一份嵌套包；安全检查会验证实际运行版本。这里刻意禁用第三方安装脚本，补丁也不通过
 隐式 `postinstall` 执行。
 
-setup 把非敏感配置和凭据引用写入 `.private/env/agent.env`（旧的根目录
-`.env.agent` 仍会被兼容读取），Agent Host 会优先自动读取它；
-旧 `.env` 仅作为兼容回退。模型地址必须是远程 HTTP(S) 地址，loopback 会被拒绝。Principal 默认为
-`human-<登录用户名>`，Actor/Node 根据主机名生成；workspace 自动设为 clone 后的仓库根目录。
-可以参照 `.env.agent.example` 覆盖这些高级默认值。
+setup 把非敏感配置和凭据引用写入用户级
+`~/.config/agentsociety/agent.env`（Windows 为 `%APPDATA%\\AgentSociety\\agent.env`）；
+旧项目的 `.private/env/agent.env` 和 `.env.agent` 仍会被自动发现并非破坏性迁移，
+旧 `.env` 仅作为兼容回退。也可以用 `--config` 或 `AGENT_ENV_FILE` 指定中心配置。
+模型地址必须是远程 HTTP(S) 地址，loopback 会被拒绝。Principal 默认为
+`human-<登录用户名>`，Actor/Node 根据主机名生成；新配置的 workspace 默认为用户 home，
+不会指向 AgentSociety checkout。可以参照 `.env.agent.example` 覆盖高级默认值。
 
 没有 `AGENT_HUB_URL`/`AGENT_HUB_TOKEN` 时，本地 TUI、session 和模型完全独立运行，也不会向
 Pi 暴露 Hub 工具。`worker`、`once`、`observe`、`attach`、`register` 是显式协作命令，缺少
-Hub 配置时会给出清晰错误。以后需要协作时重新运行 `./agent setup`，补入 Hub 两项即可；
+Hub 配置时会给出清晰错误。以后需要协作时重新运行 `agent setup --mode hub-worker`；只派发任务则使用
+`agent setup --mode dispatch-only`；
 已经配置 Hub 时仍可用 `./agent local` 临时强制脱离 Hub。
 
-setup 输入的 Model API key 和可选 Hub token 会直接写入操作系统安全凭据库：
+setup 输入的 Model API key 和可选 Hub token 会优先写入操作系统安全凭据库：
 
 - macOS 使用 Login Keychain。
 - Windows 使用 Credential Manager。
 - Linux 使用 Secret Service，需要当前用户会话提供可用且已解锁的 keyring。
 
-`.private/env/agent.env` 只记录 credential service/account，不记录秘密本身；
-凭据不会进入 Hub、日志或
-SQLite。系统凭据库不可用或锁定时 setup 会失败，不提供明文文件回退。重新运行 setup 会把
-旧 `.env.agent` 中的 `AGENT_REMOTE_API_KEY` / `AGENT_HUB_TOKEN` 迁移到系统凭据库，并从配置
-文件中删除。CI 或外部 secret manager 仍可在进程环境中临时注入这些变量，但 setup 永远不会
-把它们写入本地文本文件。
+`agent.env` 只记录 credential service/account，不记录秘密本身；凭据不会进入 Hub、日志或
+SQLite。系统凭据库不可用或锁定时，setup 默认退回只读环境变量，不自动创建明文文件；
+用户显式运行 `agent setup --credentials file` 才会写入权限 0600 的用户级凭据文件，
+并由 doctor 持续提示风险。重新运行 setup 会把旧配置中的明文密钥迁移到所选后端，
+从当前生效配置删除。CI 或外部 secret manager 仍可在进程环境中临时注入这些变量。
 
 ```bash
 # 本地用户使用 Pi 原生完整 TUI 直接操作
diff --git a/docs/authentication.md b/docs/authentication.md
index dd95dc5..3cf1c4a 100644
--- a/docs/authentication.md
+++ b/docs/authentication.md
@@ -21,15 +21,14 @@
 - Agent 连接：`agent connect` 交互输入自己的用户名/密码 → Hub 校验后
   注册/绑定 principal/actor/node，并签发**节点短期凭据**（默认 7 天）；
   密码不落盘，节点凭据存入系统钥匙串，worker 启动时自动登录/续期。
-- 无系统钥匙串的主机（如无头 Linux）：`agent connect` 会提示把
-  `AGENT_HUB_NODE_TOKEN` 写入 `.private/env/agent.env`（兼容旧 `.env.agent`，
-  文件权限 600）；该凭据由密码
-  登录签发、可单独吊销，不等同于旧的共享 bootstrap token。
-- Codex MCP 客户端通过 `agent-host/scripts/mcp-hub-wrapper.mjs` 从系统
-  钥匙串读取**本机节点凭据**再桥接 mcp-remote，配置文件里不落任何 token；
-  运行 `agent connect` 之后 MCP 工具即恢复可用。wrapper 会自动读取
-  `.private/env/agent.env` 中的 `AGENT_HUB_URL` 与钥匙串凭据名（`AGENT_HUB_NODE_TOKEN_CREDENTIAL_*`），
-  无需额外环境变量；`AGENT_HUB_MCP_URL` 仅用于覆盖默认的 `<AGENT_HUB_URL>/mcp`。
+- 无系统钥匙串的主机（如无头 Linux）：默认只从环境变量读取节点凭据，不会自动
+  落盘；用户显式选择 `agent setup --credentials file` 后，节点凭据才会写入用户
+  配置目录下权限 600 的凭据文件。该凭据由密码登录签发、可单独吊销，不等同于
+  旧的共享 bootstrap token。
+- Codex MCP 客户端通过 `agent-host/scripts/mcp-hub-wrapper.mjs` 从系统凭据库、
+  显式凭据文件或环境变量读取**本机节点凭据**再桥接 mcp-remote，配置文件里不落
+  任何 token；wrapper 会自动读取用户级 AgentSociety 配置（旧项目配置仍兼容）。
+  `AGENT_HUB_MCP_URL` 仅用于覆盖默认的 `<AGENT_HUB_URL>/mcp`。
 
 ## 2. 数据模型（新增表）
 
@@ -90,12 +89,15 @@
 ## 5. agent-host 连接流程
 
 ```
-agent connect
+agent setup --mode hub-worker
   ├─ 输入 username + password（隐藏输入）
   ├─ POST /v1/auth/agent-login
-  ├─ 将 node_token 写入系统钥匙串（AGENT_HUB_NODE_TOKEN_CREDENTIAL_*）
-  ├─ 将 username 引用写入 .private/env/agent.env（不写密码明文；兼容旧 .env.agent）
-  └─ 打印节点/租户信息
+  ├─ 将 node_token 写入系统凭据库（或用户显式选择的 0600 凭据文件）
+  ├─ 将非敏感配置写入用户级 agent.env（旧项目配置保留）
+  └─ 打印节点/租户信息（不打印 secret）
+
+agent connect
+  └─ 重新注册/轮换当前节点凭据
 
 worker 启动
   ├─ 读钥匙串 node_token（或 AGENT_HUB_NODE_TOKEN 兼容项）
diff --git a/docs/public-hub.md b/docs/public-hub.md
index a283e8b..bbb108d 100644
--- a/docs/public-hub.md
+++ b/docs/public-hub.md
@@ -147,7 +147,7 @@ Web 会话使用 `HttpOnly + SameSite=Strict` Cookie 和 CSRF 防护；登录 to
 正式域名模式下，直接运行：
 
 ```bash
-./agent setup
+agent setup --mode hub-worker
 ```
 
 Hub URL 填 `https://hub.example.com` 或 Quick Tunnel 的
@@ -156,7 +156,7 @@ token。Cloudflare 两种模式都不需要信任 CA；仅直连 Caddy 自签模
 
 ```bash
 export NODE_EXTRA_CA_CERTS=/path/to/certs/ca.pem
-./agent setup
+agent setup --mode hub-worker
 ```
 
 macOS/Windows/Linux 后台服务也要在启动环境里设置 `NODE_EXTRA_CA_CERTS`。
diff --git a/dsh-plugin/lib/worker-plugin.js b/dsh-plugin/lib/worker-plugin.js
index ffc9c6d..cc2dd1c 100644
--- a/dsh-plugin/lib/worker-plugin.js
+++ b/dsh-plugin/lib/worker-plugin.js
@@ -11,7 +11,7 @@ import { createUserMessage } from '@deepseek-ai/dsh-llm';
 import { SessionId } from '@deepseek-ai/dsh-session';
 import { setSandboxMode } from '@deepseek-ai/dsh-sandbox-policy';
 import { hostname, userInfo } from 'node:os';
-import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs';
+import { existsSync, mkdirSync, readFileSync, renameSync, unlinkSync, writeFileSync } from 'node:fs';
 import { randomUUID } from 'node:crypto';
 import { basename, dirname, isAbsolute, relative, resolve } from 'node:path';
 import { fileURLToPath, pathToFileURL } from 'node:url';
@@ -989,7 +989,7 @@ class WorkerLoop {
         return candidate;
     }
     scopeKey(task, cwd) {
-        return [task.principal_id, cwd].join('\u0000');
+        return [task.principal_id, this.options.actorId, this.options.nodeId, cwd].join('\u0000');
     }
     async dispose() {
         this.disposed = true;
@@ -1100,10 +1100,20 @@ function readState(path) {
 }
 function writeState(path, state) {
     mkdirSync(dirname(path), { recursive: true, mode: 0o700 });
-    writeFileSync(path, `${JSON.stringify(state, null, 2)}\n`, {
-        encoding: 'utf8',
-        mode: 0o600,
-    });
+    const temporary = `${path}.${process.pid}.${randomUUID()}.tmp`;
+    try {
+        writeFileSync(temporary, `${JSON.stringify(state, null, 2)}\n`, {
+            encoding: 'utf8',
+            mode: 0o600,
+        });
+        renameSync(temporary, path);
+    }
+    finally {
+        try {
+            unlinkSync(temporary);
+        }
+        catch { /* rename completed or temp was never created */ }
+    }
 }
 function stableSlug(value) {
     const slug = value.toLowerCase().replace(/[^a-z0-9._-]+/gu, '-');
diff --git a/dsh-plugin/lib/worker-plugin.js.map b/dsh-plugin/lib/worker-plugin.js.map
index 9878cf0..a87b4b6 100644
--- a/dsh-plugin/lib/worker-plugin.js.map
+++ b/dsh-plugin/lib/worker-plugin.js.map
@@ -1 +1 @@
-{"version":3,"file":"worker-plugin.js","sourceRoot":"","sources":["../src/worker-plugin.ts"],"names":[],"mappings":"AAAA;;;;;;;GAOG;AAIH,OAAO,MAAM,MAAM,0BAA0B,CAAA;AAE7C,OAAO,EAAE,iBAAiB,EAAE,MAAM,sBAAsB,CAAA;AACxD,OAAO,EAAE,SAAS,EAAmC,MAAM,0BAA0B,CAAA;AAErF,OAAO,EAAE,cAAc,EAAE,MAAM,iCAAiC,CAAA;AAEhE,OAAO,EAAE,QAAQ,EAAE,QAAQ,EAAE,MAAM,SAAS,CAAA;AAC5C,OAAO,EAAE,UAAU,EAAE,SAAS,EAAE,YAAY,EAAE,aAAa,EAAE,MAAM,SAAS,CAAA;AAC5E,OAAO,EAAE,UAAU,EAAE,MAAM,aAAa,CAAA;AACxC,OAAO,EAAE,QAAQ,EAAE,OAAO,EAAE,UAAU,EAAE,QAAQ,EAAE,OAAO,EAAE,MAAM,WAAW,CAAA;AAC5E,OAAO,EAAE,aAAa,EAAE,aAAa,EAAE,MAAM,UAAU,CAAA;AACvD,OAAO,EACL,SAAS,GAIV,MAAM,iBAAiB,CAAA;AACxB,OAAO,EAAE,kBAAkB,EAAwB,MAAM,aAAa,CAAA;AACtE,OAAO,EAAE,cAAc,EAAE,MAAM,aAAa,CAAA;AAC5C,OAAO,EACL,UAAU,EACV,eAAe,EACf,UAAU,EACV,UAAU,GACX,MAAM,gBAAgB,CAAA;AACvB,OAAO,EACL,gBAAgB,EAChB,mBAAmB,EACnB,qBAAqB,GACtB,MAAM,kBAAkB,CAAA;AAEzB,MAAM,CAAC,MAAM,IAAI,GAAG,sBAAsB,CAAA;AAC1C,MAAM,CAAC,MAAM,MAAM,GAAG,CAAC,OAAO,EAAE,QAAQ,EAAE,UAAU,EAAE,oBAAoB,CAAC,CA
OAAO,IAAI,OAAO,CAAC,CAAC,OAAO,EAAE,EAAE,CAAC,UAAU,CAAC,OAAO,EAAE,EAAE,CAAC,CAAC,CAAA;AAC1D,CAAC;AAED,SAAS,OAAO,CAAC,KAAc;IAC7B,OAAO,KAAK,YAAY,KAAK,CAAC,CAAC,CAAC,KAAK,CAAC,OAAO,CAAC,CAAC,CAAC,MAAM,CAAC,KAAK,CAAC,CAAA;AAC/D,CAAC"}
\ No newline at end of file
diff --git a/dsh-plugin/package.json b/dsh-plugin/package.json
index ddcdddc..fd504ee 100644
--- a/dsh-plugin/package.json
+++ b/dsh-plugin/package.json
@@ -59,25 +59,31 @@
     "test": "npm run build && node --test test/*.test.mjs"
   },
   "peerDependencies": {
-    "@deepseek-ai/cordis": "^4.0.1",
-    "@deepseek-ai/cordis-plugin-timer": "^1.1.3",
-    "@deepseek-ai/dsh-agent": "^0.0.1-rc.5",
-    "@deepseek-ai/dsh-llm": "^0.0.1-rc.5",
-    "@deepseek-ai/dsh-sandbox-policy": "^0.0.1-rc.5",
-    "@deepseek-ai/dsh-session": "^0.0.1-rc.5",
-    "@deepseek-ai/dsh-session-persistence": "^0.0.1-rc.5",
-    "@deepseek-ai/schemastery": "^3.18.1"
+    "@deepseek-ai/cordis": "^4.0.2",
+    "@deepseek-ai/cordis-plugin-timer": "^1.1.4",
+    "@deepseek-ai/dsh-agent": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-llm": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-sandbox-policy": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-session": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-session-persistence": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-system-prompt": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-mcp-client": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-scope": "^0.1.2-alpha.2",
+    "@deepseek-ai/schemastery": "^3.18.2"
   },
   "devDependencies": {
-    "@deepseek-ai/cordis": "4.0.1",
-    "@deepseek-ai/cordis-plugin-timer": "1.1.3",
-    "@deepseek-ai/dsh-agent": "0.0.1-rc.5",
-    "@deepseek-ai/dsh-llm": "0.0.1-rc.5",
-    "@deepseek-ai/dsh-sandbox-policy": "^0.0.1-rc.5",
-    "@deepseek-ai/dsh-session": "0.0.1-rc.5",
-    "@deepseek-ai/dsh-session-persistence": "0.0.1-rc.5",
-    "@deepseek-ai/dsh-session-title": "^0.0.1-rc.5",
-    "@deepseek-ai/schemastery": "3.18.1",
+    "@deepseek-ai/cordis": "4.0.2",
+    "@deepseek-ai/cordis-plugin-timer": "1.1.4",
+    "@deepseek-ai/dsh-agent": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-llm": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-mcp-client": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-sandbox-policy": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-scope": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-session": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-session-persistence": "0.1.2-alpha.2",
+    "@deepseek-ai/dsh-session-title": "^0.1.2-alpha.2",
+    "@deepseek-ai/dsh-system-prompt": "0.1.2-alpha.2",
+    "@deepseek-ai/schemastery": "3.18.2",
     "@types/node": "^22.0.0",
     "typescript": "^5.9.3"
   },
diff --git a/dsh-plugin/src/worker-plugin.ts b/dsh-plugin/src/worker-plugin.ts
index 4bcedc9..4081ee5 100644
--- a/dsh-plugin/src/worker-plugin.ts
+++ b/dsh-plugin/src/worker-plugin.ts
@@ -17,7 +17,7 @@ import type { SessionTitleService } from '@deepseek-ai/dsh-session-title'
 import { setSandboxMode } from '@deepseek-ai/dsh-sandbox-policy'
 import type {} from '@deepseek-ai/dsh-session-persistence'
 import { hostname, userInfo } from 'node:os'
-import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs'
+import { existsSync, mkdirSync, readFileSync, renameSync, unlinkSync, writeFileSync } from 'node:fs'
 import { randomUUID } from 'node:crypto'
 import { basename, dirname, isAbsolute, relative, resolve } from 'node:path'
 import { fileURLToPath, pathToFileURL } from 'node:url'
@@ -1197,7 +1197,7 @@ class WorkerLoop {
   }
 
   private scopeKey(task: HubTask, cwd: string): string {
-    return [task.principal_id, cwd].join('\u0000')
+    return [task.principal_id, this.options.actorId, this.options.nodeId, cwd].join('\u0000')
   }
 
   async dispose(): Promise<void> {
@@ -1321,10 +1321,16 @@ function readState(path: string): Record<string, string> {
 
 function writeState(path: string, state: Record<string, string>): void {
   mkdirSync(dirname(path), { recursive: true, mode: 0o700 })
-  writeFileSync(path, `${JSON.stringify(state, null, 2)}\n`, {
-    encoding: 'utf8',
-    mode: 0o600,
-  })
+  const temporary = `${path}.${process.pid}.${randomUUID()}.tmp`
+  try {
+    writeFileSync(temporary, `${JSON.stringify(state, null, 2)}\n`, {
+      encoding: 'utf8',
+      mode: 0o600,
+    })
+    renameSync(temporary, path)
+  } finally {
+    try { unlinkSync(temporary) } catch { /* rename completed or temp was never created */ }
+  }
 }
 
 function stableSlug(value: string): string {
diff --git a/tests/hub/test_tunnel.py b/tests/hub/test_tunnel.py
index 17b6769..e551af3 100644
--- a/tests/hub/test_tunnel.py
+++ b/tests/hub/test_tunnel.py
@@ -29,7 +29,7 @@ class TunnelRegistryTests(unittest.TestCase):
         self.assertTrue(self.registry.is_online("node-a"))
         self.assertTrue(self.registry.send_to("node-a", {"type": "ping"}))
         self.assertEqual(received, [{"type": "ping"}])
-        self.registry.detach("node-a", close)
+        self.assertTrue(self.registry.detach("node-a", close))
         self.assertFalse(self.registry.is_online("node-a"))
 
     def test_send_to_offline_node_fails(self) -> None:
@@ -41,6 +41,16 @@ class TunnelRegistryTests(unittest.TestCase):
         self.registry.attach("node-a", lambda message: None, lambda: None)
         self.assertEqual(closed, [True])
 
+    def test_stale_detach_cannot_remove_replacement(self) -> None:
+        old_close = lambda: None
+        new_close = lambda: None
+        self.registry.attach("node-a", lambda message: None, old_close)
+        self.registry.attach("node-a", lambda message: None, new_close)
+        self.assertFalse(self.registry.detach("node-a", old_close))
+        self.assertTrue(self.registry.is_online("node-a"))
+        self.assertTrue(self.registry.detach("node-a", new_close))
+        self.assertFalse(self.registry.is_online("node-a"))
+
     def test_send_failure_reports_offline(self) -> None:
         def broken(message: dict) -> None:
             raise RuntimeError("send failed")
diff --git a/tests/hub/test_web_tunnel.py b/tests/hub/test_web_tunnel.py
index 008b3af..f54266f 100644
--- a/tests/hub/test_web_tunnel.py
+++ b/tests/hub/test_web_tunnel.py
@@ -7,6 +7,7 @@ import secrets
 import socket
 import struct
 import threading
+import queue
 from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
 from pathlib import Path
 from tempfile import TemporaryDirectory
@@ -19,7 +20,7 @@ from agent_hub.api import AgentHubApi
 from agent_hub.server import HubHttpServer
 from agent_hub.store import AgentHubStore
 from agent_hub.websocket import WebSocket, accept_key
-from agent_hub.web_proxy import validate_proxy_path
+from agent_hub.web_proxy import WebTunnelCoordinator, validate_proxy_path
 
 
 class FakeDshWebServer:
@@ -30,7 +31,7 @@ class FakeDshWebServer:
 
         class Handler(BaseHTTPRequestHandler):
             def do_GET(self) -> None:  # noqa: N802
-                if self.path == "/api/events.mux":
+                if self.path == "/api/remote.mux":
                     self._serve_events()
                     return
                 self._answer()
@@ -65,15 +66,30 @@ class FakeDshWebServer:
                 try:
                     for index in range(3):
                         frame = {
-                            "type": "server-request",
-                            "rpcId": f"fx{index}",
-                            "method": "mux/update",
-                            "payload": {"seq": index},
+                            "type": "item",
+                            "streamId": "fx",
+                            "value": {
+                                "seq": index,
+                                "label": f"frame-{index}",
+                            },
                         }
                         ws.send_text(json.dumps(frame))
                         time.sleep(0.05)
                     while True:
-                        ws.recv_message()
+                        opcode, payload = ws.recv_message()
+                        if opcode != 0x1:
+                            continue
+                        try:
+                            client = json.loads(payload.decode("utf-8"))
+                        except (UnicodeDecodeError, json.JSONDecodeError):
+                            continue
+                        stream_id = client.get("streamId", "echo")
+                        endpoint = client.get("endpoint", "echo")
+                        ws.send_text(json.dumps({
+                            "type": "item",
+                            "streamId": stream_id,
+                            "value": {"echo": endpoint},
+                        }))
                 except Exception:  # noqa: BLE001
                     pass
 
@@ -198,6 +214,57 @@ class MiniWebSocketClient:
             pass
 
 
+class WebTunnelCoordinatorIsolationTests(unittest.TestCase):
+    def test_fail_node_only_releases_that_nodes_channels(self) -> None:
+        from agent_hub.tunnel import TunnelRegistry
+
+        registry = TunnelRegistry()
+        coordinator = WebTunnelCoordinator(registry)
+        pending_a: queue.Queue[dict] = queue.Queue(maxsize=1)
+        pending_b: queue.Queue[dict] = queue.Queue(maxsize=1)
+        stream_a: queue.Queue[dict] = queue.Queue(maxsize=1)
+        stream_b: queue.Queue[dict] = queue.Queue(maxsize=1)
+        coordinator._pending = {
+            "request-a": ("node-a", pending_a),
+            "request-b": ("node-b", pending_b),
+        }
+        coordinator._ws_channels = {
+            "stream-a": ("node-a", stream_a),
+            "stream-b": ("node-b", stream_b),
+        }
+
+        coordinator._fail_node("node-a")
+
+        self.assertEqual(pending_a.get_nowait()["status"], 502)
+        self.assertTrue(pending_b.empty())
+        self.assertEqual(stream_a.get_nowait()["code"], 1006)
+        self.assertTrue(stream_b.empty())
+        self.assertEqual(set(coordinator._pending), {"request-b"})
+        self.assertEqual(set(coordinator._ws_channels), {"stream-b"})
+
+    def test_old_tunnel_teardown_does_not_fail_replacement(self) -> None:
+        from agent_hub.tunnel import TunnelRegistry
+
+        registry = TunnelRegistry()
+        coordinator = WebTunnelCoordinator(registry)
+        old_close = lambda: None
+        new_close = lambda: None
+        registry.attach("node-a", lambda message: None, old_close)
+        registry.attach("node-a", lambda message: None, new_close)
+        coordinator._pending = {
+            "request-a": ("node-a", queue.Queue(maxsize=1)),
+        }
+
+        # This is the old handler's finally path. It must not call _fail_node
+        # after attach() has installed the replacement tunnel.
+        if registry.detach("node-a", old_close):
+            coordinator._fail_node("node-a")
+
+        self.assertTrue(registry.is_online("node-a"))
+        self.assertIn("request-a", coordinator._pending)
+
+
+
 class WebTunnelIntegrationTests(unittest.TestCase):
     def setUp(self) -> None:
         self._temporary = TemporaryDirectory()
@@ -358,7 +425,7 @@ class WebTunnelIntegrationTests(unittest.TestCase):
         thread.start()
         try:
             browser = MiniWebSocketClient(
-                f"ws://127.0.0.1:{self.port}/v1/web/node-device/api/events.mux",
+                f"ws://127.0.0.1:{self.port}/v1/web/node-device/api/remote.mux",
                 headers={"Authorization": f"Bearer {self.token}"},
             )
             try:
@@ -504,7 +571,7 @@ class WebTunnelIntegrationTests(unittest.TestCase):
         thread.start()
         try:
             browser = MiniWebSocketClient(
-                f"ws://127.0.0.1:{self.port}/v1/web/node-device/api/events.mux",
+                f"ws://127.0.0.1:{self.port}/v1/web/node-device/api/remote.mux",
                 headers={"Authorization": f"Bearer {self.token}"},
             )
             received = []
@@ -515,14 +582,85 @@ class WebTunnelIntegrationTests(unittest.TestCase):
             finally:
                 browser.close()
             self.assertEqual(
-                [frame["payload"]["seq"] for frame in received], [0, 1, 2]
+                [frame["value"]["seq"] for frame in received], [0, 1, 2]
             )
-            self.assertEqual(received[0]["rpcId"], "fx0")
-            self.assertEqual(received[0]["method"], "mux/update")
+            self.assertEqual(received[0]["streamId"], "fx")
+            self.assertEqual(received[0]["type"], "item")
         finally:
             ws.close()
         self.assertEqual(relay_error, [])
 
+    def test_browser_remote_mux_forwards_both_directions(self) -> None:
+        """Browser->device frames are relayed and device replies return."""
+        ws = self._open_tunnel()
+        relay_error: list[Exception] = []
+
+        def device_loop() -> None:
+            try:
+                while True:
+                    message = json.loads(ws.recv_text(timeout=5))
+                    if message["type"] == "ws-open":
+                        local = MiniWebSocketClient(
+                            f"ws://127.0.0.1:{self.fake_web.server.server_address[1]}{message['path']}"
+                        )
+                        ws.send_text(json.dumps({
+                            "type": "ws-open-ack",
+                            "id": message["id"],
+                            "ok": True,
+                        }))
+                        # Consume the fake device's initial remote.mux items.
+                        for _ in range(3):
+                            local.recv_text(timeout=5)
+                        while True:
+                            client = json.loads(ws.recv_text(timeout=5))
+                            if client["type"] == "ws-client-frame":
+                                text = base64.b64decode(client["payload_b64"]).decode("utf-8")
+                                local.send_text(text)
+                                reply = local.recv_text(timeout=5)
+                                ws.send_text(json.dumps({
+                                    "type": "ws-frame",
+                                    "id": client["id"],
+                                    "opcode": 1,
+                                    "payload_b64": base64.b64encode(
+                                        reply.encode("utf-8")
+                                    ).decode("ascii"),
+                                }))
+                                local.close()
+                                return
+                            elif client["type"] == "ws-close":
+                                local.close()
+                                return
+                    elif message["type"] == "ws-close":
+                        return
+            except Exception as exc:  # noqa: BLE001
+                relay_error.append(exc)
+
+        thread = threading.Thread(target=device_loop, daemon=True)
+        thread.start()
+        try:
+            browser = MiniWebSocketClient(
+                f"ws://127.0.0.1:{self.port}/v1/web/node-device/api/remote.mux",
+                headers={"Authorization": f"Bearer {self.token}"},
+            )
+            try:
+                browser.send_text(json.dumps({
+                    "type": "open",
+                    "streamId": "s1",
+                    "endpoint": "$events",
+                    "payload": {"args": {}},
+                }))
+                reply = json.loads(browser.recv_text(timeout=10))
+                self.assertEqual(reply["type"], "item")
+                self.assertEqual(reply["streamId"], "s1")
+                self.assertEqual(reply["value"], {"echo": "$events"})
+            finally:
+                browser.close()
+        finally:
+            ws.close()
+            thread.join(timeout=5)
+        self.assertEqual(relay_error, [])
+
+
     def test_event_ws_rejects_disabled_node_web(self) -> None:
         # A second node without the dsh_web capability must be denied.
         self.store.register_node(
@@ -533,7 +671,7 @@ class WebTunnelIntegrationTests(unittest.TestCase):
         )
         try:
             browser = MiniWebSocketClient(
-                f"ws://127.0.0.1:{self.port}/v1/web/node-plain/api/events.mux",
+                f"ws://127.0.0.1:{self.port}/v1/web/node-plain/api/remote.mux",
                 headers={"Authorization": f"Bearer {self.token}"},
             )
             browser.close()
