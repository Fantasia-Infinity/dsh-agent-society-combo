diff --git a/apps/web/index.html b/apps/web/index.html
index 75c4e5bac6..27024bbef4 100644
--- a/apps/web/index.html
+++ b/apps/web/index.html
@@ -3,12 +3,12 @@
   <head>
     <meta charset="utf-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1" />
-    <link rel="manifest" href="/manifest.webmanifest" />
-    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
+    <link rel="manifest" href="./manifest.webmanifest" />
+    <link rel="icon" type="image/svg+xml" href="./favicon.svg" />
     <title>DSH Local Build</title>
   </head>
   <body>
     <div id="root"></div>
-    <script type="module" src="/src/main.ts"></script>
+    <script type="module" src="./src/main.ts"></script>
   </body>
 </html>
diff --git a/apps/web/public/manifest.webmanifest b/apps/web/public/manifest.webmanifest
index 20a428fee6..583a46791d 100644
--- a/apps/web/public/manifest.webmanifest
+++ b/apps/web/public/manifest.webmanifest
@@ -1,13 +1,13 @@
 {
-  "id": "/",
+  "id": "./",
   "name": "DeepSeek Harness",
   "short_name": "DSH",
-  "start_url": "/",
-  "scope": "/",
+  "start_url": "./",
+  "scope": "./",
   "display": "fullscreen",
   "icons": [
     {
-      "src": "/favicon.svg",
+      "src": "./favicon.svg",
       "sizes": "any",
       "type": "image/svg+xml",
       "purpose": "any"
diff --git a/apps/web/tests/pwa-manifest.e2e.ts b/apps/web/tests/pwa-manifest.e2e.ts
index 08e210fa26..492431b534 100644
--- a/apps/web/tests/pwa-manifest.e2e.ts
+++ b/apps/web/tests/pwa-manifest.e2e.ts
@@ -11,14 +11,14 @@ it('ships install metadata with the built web application', async () => {
 
   const manifest: unknown = JSON.parse(await readFile(join(DIST_ROOT, 'manifest.webmanifest'), 'utf8'))
   expect(manifest).toEqual({
-    id: '/',
+    id: './',
     name: 'DeepSeek Harness',
     short_name: 'DSH',
-    start_url: '/',
-    scope: '/',
+    start_url: './',
+    scope: './',
     display: 'fullscreen',
     icons: [{
-      src: '/favicon.svg',
+      src: './favicon.svg',
       sizes: 'any',
       type: 'image/svg+xml',
       purpose: 'any',
diff --git a/packages/api/gateway/src/client/stream-client.ts b/packages/api/gateway/src/client/stream-client.ts
index 2fe84ec94f..80bb4d8700 100644
--- a/packages/api/gateway/src/client/stream-client.ts
+++ b/packages/api/gateway/src/client/stream-client.ts
@@ -305,7 +305,13 @@ function remoteStreamUrl(): string {
   const location = (globalThis as { location?: { origin?: string } }).location
   const transport = (globalThis as { __DSH_TRANSPORT__?: { streamBaseUrl?: string } }).__DSH_TRANSPORT__
   const base = transport?.streamBaseUrl ?? (location?.origin !== undefined && location.origin !== 'null' ? location.origin : INTERNAL_BASE)
-  const url = new URL(REMOTE_STREAM_MUX_PATH, base)
+  const page = (globalThis as { location?: { href?: string } }).location
+  const resolvedBase = transport?.streamBaseUrl !== undefined
+    ? base.replace(/\/+$/u, '') + '/'
+    : location?.origin !== undefined && location.origin !== 'null'
+      ? new URL('.', page?.href ?? location.origin + '/').toString()
+      : INTERNAL_BASE + '/'
+  const url = new URL(REMOTE_STREAM_MUX_PATH.replace(/^\/+/, ''), resolvedBase)
   url.protocol = url.protocol === 'https:' ? 'wss:' : 'ws:'
   return url.href
 }
diff --git a/packages/api/gateway/tests/gateway.client.spec.ts b/packages/api/gateway/tests/gateway.client.spec.ts
index 55e0b1d137..86d1762ff4 100644
--- a/packages/api/gateway/tests/gateway.client.spec.ts
+++ b/packages/api/gateway/tests/gateway.client.spec.ts
@@ -2343,7 +2343,7 @@ describe('Client Typert API', () => {
     ;(globalThis as WebSocketGlobal).WebSocket = FakeWebSocket as unknown as typeof WebSocket
     Object.defineProperty(globalThis, 'location', {
       configurable: true,
-      value: { origin: 'https://harness.example' },
+      value: { origin: 'https://harness.example', href: 'https://harness.example/v1/web/node/' },
     })
     FakeWebSocket.sockets.length = 0
     const call = vi.fn<ConnectionHandle['rpc']['call']>()
@@ -2355,7 +2355,7 @@ describe('Client Typert API', () => {
       const firstItem = first.next()
       await vi.waitFor(() => { expect(FakeWebSocket.sockets[0]?.sent).toHaveLength(1) })
       const socket = FakeWebSocket.sockets[0]!
-      expect(socket.url).toBe('wss://harness.example/api/remote.mux')
+      expect(socket.url).toBe('wss://harness.example/v1/web/node/api/remote.mux')
       const opened = JSON.parse(socket.sent[0]!) as { streamId: string }
       expect(opened).toMatchObject({
         type: 'open',
diff --git a/packages/client/connection/src/client/rpc.ts b/packages/client/connection/src/client/rpc.ts
index 2c3f7d3e28..90f90aff9f 100644
--- a/packages/client/connection/src/client/rpc.ts
+++ b/packages/client/connection/src/client/rpc.ts
@@ -41,7 +41,7 @@ export function createWebConnectionRpc(doFetch?: RpcFetch, openStream?: RpcStrea
         payload,
       }
       const response = await send(
-        new URL(`${channel}/${endpoint}`, resolveBase()),
+        resolvePath(`${channel}/${endpoint}`),
         {
           method: 'POST',
           headers: { 'content-type': 'application/json' },
@@ -106,8 +106,15 @@ function isRecord(value: unknown): value is Record<PropertyKey, unknown> {
 }
 
 function resolveBase(): string {
-  const location = (globalThis as { location?: { origin?: string } }).location
-  return location?.origin !== undefined && location.origin !== 'null' ? location.origin : INTERNAL_BASE
+  const location = (globalThis as { location?: { origin?: string; href?: string } }).location
+  if (location?.origin !== undefined && location.origin !== 'null') {
+    return new URL('.', location.href ?? `${location.origin}/`).toString()
+  }
+  return `${INTERNAL_BASE}/`
+}
+
+function resolvePath(path: string): URL {
+  return new URL(path.replace(/^\/+/, ''), resolveBase())
 }
 
 function assertTarget(channel: string, endpoint: string): void {
diff --git a/packages/client/connection/tests/client-apply.client.spec.ts b/packages/client/connection/tests/client-apply.client.spec.ts
index 084f037135..40b94d9ab1 100644
--- a/packages/client/connection/tests/client-apply.client.spec.ts
+++ b/packages/client/connection/tests/client-apply.client.spec.ts
@@ -15,7 +15,7 @@ import {
 } from '../src/client/index.ts'
 
 type Win = {
-  location?: { hostname: string; origin?: string }
+  location?: { hostname: string; origin?: string; href?: string }
   __DSH_TRANSPORT__?: ClientTransportHooks
 }
 
@@ -532,6 +532,7 @@ describe('connection client apply', () => {
   it('validates generic RPC transport failures, correlation, and targets', async () => {
     ;(globalThis as Win).location = {
       hostname: 'harness.example', origin: 'https://harness.example',
+      href: 'https://harness.example/v1/web/node/',
     }
     const handle = await mount()
     const original = globalThis.fetch
@@ -541,7 +542,7 @@ describe('connection client apply', () => {
       await expect(handle.rpc.call('/api', 'goals/create', {}, abort.signal))
         .rejects.toThrow('HTTP 503')
       expect(globalThis.fetch).toHaveBeenCalledWith(
-        new URL('https://harness.example/api/goals/create'),
+        new URL('https://harness.example/v1/web/node/api/goals/create'),
         expect.objectContaining({ signal: abort.signal }),
       )
 
diff --git a/packages/client/hmr/src/client/index.ts b/packages/client/hmr/src/client/index.ts
index 829ea3003d..354824d957 100644
--- a/packages/client/hmr/src/client/index.ts
+++ b/packages/client/hmr/src/client/index.ts
@@ -1,10 +1,10 @@
 /** Web SSE transport for page-owned client entry reconciliation and rebuilt code replacement. */
 import type { Context } from '@deepseek-ai/cordis'
 import type { PluginsEventParseResult } from '../events.ts'
-import { EVENTS_ENDPOINT, parsePluginsEventFrame } from '../events.ts'
+import { CLIENT_EVENTS_ENDPOINT, parsePluginsEventFrame } from '../events.ts'
 
 export type { PluginsEventFrame } from '../events.ts'
-export { EVENTS_ENDPOINT } from '../events.ts'
+export { CLIENT_EVENTS_ENDPOINT, EVENTS_ENDPOINT } from '../events.ts'
 
 /** Cordis plugin name. */
 export const name = 'client-hmr'
@@ -26,7 +26,7 @@ export function apply(ctx: Context): void {
   }
 
   ctx.effect(() => {
-    const source = new EventSource(EVENTS_ENDPOINT)
+    const source = new EventSource(CLIENT_EVENTS_ENDPOINT)
     source.addEventListener('message', (event: MessageEvent<string>) => {
       let value: unknown
       try {
diff --git a/packages/client/hmr/src/events.ts b/packages/client/hmr/src/events.ts
index 26609d2dc3..bb92a5ec45 100644
--- a/packages/client/hmr/src/events.ts
+++ b/packages/client/hmr/src/events.ts
@@ -42,3 +42,6 @@ export function parsePluginsEventFrame(value: unknown): PluginsEventParseResult
 
 /** System SSE endpoint pushing graph/rebuilt frames (wire protocol constant). */
 export const EVENTS_ENDPOINT = '/plugins/events'
+
+/** Browser-relative form; survives mounting the page below a path prefix. */
+export const CLIENT_EVENTS_ENDPOINT = './plugins/events'
diff --git a/packages/client/modules/src/index.ts b/packages/client/modules/src/index.ts
index 4b59882f13..aa852e3945 100644
--- a/packages/client/modules/src/index.ts
+++ b/packages/client/modules/src/index.ts
@@ -159,9 +159,14 @@ interface LazyResponse {
 /** Fields shared by every generated combo plan. */
 interface ComboArtifact {
   url: string
+  /** Server-absolute combo key used by the registered /plugins route. */
+  serverUrl: string
   rev: string
   entries: string[]
+  /** Server-absolute source-map key used by the registered /plugins route. */
   sourceMapUrl: string
+  /** Browser-relative source-map URL embedded in the script. */
+  scriptSourceMapUrl: string
   scriptBody: () => Promise<Buffer>
   sourceMapBody: () => Promise<Buffer>
 }
@@ -213,10 +218,25 @@ function artifactRevision(bundle: Buffer, baseline: ClientArtifactBaseline): str
   return framedHash('plugin-artifact', [bundle, Buffer.from(String(baseline.mtimeMs))])
 }
 
-/** Address one ordered plugin-file list through the shared combo route. */
-function comboUrl(ids: readonly string[], rev: string, sourceMap = false): string {
+/** Resource query shared by browser-relative and server-absolute combo URLs. */
+function comboResources(ids: readonly string[], sourceMap = false): string {
   const resources = ids.map(id => `${id}/client.js${sourceMap ? '.map' : ''}`).join(',')
-  return `/plugins/??${resources}&rev=${rev}`
+  return resources
+}
+
+/** Browser-relative combo URL; survives mounting the page below a path prefix. */
+function browserComboUrl(ids: readonly string[], rev: string, sourceMap = false): string {
+  return `./plugins/??${comboResources(ids, sourceMap)}&rev=${rev}`
+}
+
+/** Server-absolute route key used by the registered /plugins handler. */
+function serverComboUrl(ids: readonly string[], rev: string, sourceMap = false): string {
+  return `/plugins/??${comboResources(ids, sourceMap)}&rev=${rev}`
+}
+
+/** Source-map URL relative to the combo script itself, not to the document. */
+function scriptRelativeComboMapUrl(ids: readonly string[], rev: string): string {
+  return `??${comboResources(ids, true)}&rev=${rev}`
 }
 
 /** Address one package-local chunk through the same revision as its entry. */
@@ -224,9 +244,14 @@ function chunkUrl(id: string, fileName: string, rev: string, sourceMap = false):
   return `/plugins/${id}/${fileName}${sourceMap ? '.map' : ''}?rev=${rev}`
 }
 
+/** Source-map URL relative to a package-local chunk script. */
+function scriptRelativeChunkMapUrl(fileName: string, rev: string): string {
+  return `${fileName}.map?rev=${rev}`
+}
+
 /** Measure the longer map-form URL used to partition a startup resource list. */
 function projectedComboUrlBytes(records: readonly WebPluginRecord[]): number {
-  return Buffer.byteLength(comboUrl(
+  return Buffer.byteLength(browserComboUrl(
     records.map(record => record.entry.id),
     COMBO_REVISION_PLACEHOLDER,
     true,
@@ -410,14 +435,18 @@ function buildCombo(
   }))
   const rev = revision ?? comboRevision(resources)
   const entries = resources.map(resource => resource.id)
-  const url = comboUrl(entries, rev)
-  const sourceMapUrl = comboUrl(entries, rev, true)
+  const url = browserComboUrl(entries, rev)
+  const serverUrl = serverComboUrl(entries, rev)
+  const sourceMapUrl = serverComboUrl(entries, rev, true)
+  const scriptSourceMapUrl = scriptRelativeComboMapUrl(entries, rev)
   return {
     url,
+    serverUrl,
     rev,
     entries,
     sourceMapUrl,
-    scriptBody: lazyBody(() => buildComboScript(resources, sourceMapUrl)),
+    scriptSourceMapUrl,
+    scriptBody: lazyBody(() => buildComboScript(resources, scriptSourceMapUrl)),
     sourceMapBody: lazyBody(() => buildComboSourceMap(resources, sourceMapOf)),
   }
 }
@@ -439,7 +468,7 @@ function buildBatch(
 function graphRow(id: string, rev: string, fields: WebBootRowFields): WebBootEntry {
   return {
     id,
-    url: comboUrl([id], rev),
+    url: browserComboUrl([id], rev),
     rev,
     ...(fields.inject !== undefined ? { inject: fields.inject } : {}),
     ...(fields.immediately ? { immediately: true } : {}),
@@ -735,7 +764,7 @@ export class ClientModuleRegistry extends Service {
 
     const batchResponses = new Map<string, LazyResponse>()
     for (const artifact of artifacts) {
-      batchResponses.set(artifact.descriptor.url, this.responses.get(artifact.descriptor.url) ?? {
+      batchResponses.set(artifact.serverUrl, this.responses.get(artifact.serverUrl) ?? {
         body: artifact.scriptBody,
         contentType: 'text/javascript; charset=utf-8',
       })
@@ -747,7 +776,7 @@ export class ClientModuleRegistry extends Service {
     const responses = new Map(batchResponses)
     for (const record of this.table.values()) {
       const artifact = buildCombo([record], this.readSourceMap, record.entry.rev)
-      responses.set(artifact.url, responses.get(artifact.url) ?? this.responses.get(artifact.url) ?? {
+      responses.set(artifact.serverUrl, responses.get(artifact.serverUrl) ?? this.responses.get(artifact.serverUrl) ?? {
         body: artifact.scriptBody,
         contentType: 'text/javascript; charset=utf-8',
       })
@@ -1067,7 +1096,7 @@ export class ClientModuleRegistry extends Service {
     const { record, fileName, sourceMap, resourceUrl } = request
     const clientPath = join(dirname(record.meta.clientPath), fileName)
     if (!existsSync(clientPath)) return undefined
-    const sourceMapUrl = chunkUrl(record.entry.id, fileName, record.entry.rev, true)
+    const scriptSourceMapUrl = scriptRelativeChunkMapUrl(fileName, record.entry.rev)
     const resource = (): ComboResource => ({
       id: record.entry.id,
       rev: record.entry.rev,
@@ -1081,7 +1110,7 @@ export class ClientModuleRegistry extends Service {
         contentType: 'application/json; charset=utf-8',
       }
       : {
-        body: lazyBody(() => buildComboScript([resource()], sourceMapUrl)),
+        body: lazyBody(() => buildComboScript([resource()], scriptSourceMapUrl)),
         contentType: 'text/javascript; charset=utf-8',
       }
     this.responses.set(resourceUrl, response)
diff --git a/packages/client/modules/tests/node-half.client.spec.ts b/packages/client/modules/tests/node-half.client.spec.ts
index 0446cb2687..843fbc0a5c 100644
--- a/packages/client/modules/tests/node-half.client.spec.ts
+++ b/packages/client/modules/tests/node-half.client.spec.ts
@@ -18,8 +18,9 @@ const MODULES_ID = '@deepseek-ai/dsh-client-modules'
 const UI_RENDERER_ID = '@deepseek-ai/dsh-client-ui-renderer'
 
 const comboUrl = (ids: readonly string[], rev: string): string =>
-  `/plugins/??${ids.map(id => `${id}/client.js`).join(',')}&rev=${rev}`
+  `./plugins/??${ids.map(id => `${id}/client.js`).join(',')}&rev=${rev}`
 const mapUrl = (url: string): string => url.replace(/\/client\.js(?=,|&rev=)/g, '/client.js.map')
+const scriptMapUrl = (url: string): string => mapUrl(url).replace(/^\.\/plugins\//, '')
 const chunkUrl = (id: string, fileName: string, rev: string): string => `/plugins/${id}/${fileName}?rev=${rev}`
 const BOOTSTRAP_URL = comboUrl([MODULES_ID], 'boot')
 const APPLICATION_URL = comboUrl([UI_RENDERER_ID], 'app')
@@ -162,7 +163,8 @@ async function routeRequest(route: Promise<WebRoute>, url: string, method = 'GET
       return response
     },
   } as unknown as ServerResponse
-  await (await route).handler({ method, url } as IncomingMessage, response)
+  const wireUrl = url.startsWith('./') ? url.slice(1) : url
+  await (await route).handler({ method, url: wireUrl } as IncomingMessage, response)
   return { status, headers, body }
 }
 
@@ -500,7 +502,7 @@ describe('client bundle activation', () => {
     const torn = constructWithRoute([packageName])
     const tornRow = torn.service.graph().entries[0]!
     expect((await routeRequest(torn.route, tornRow.url)).body.toString('utf8'))
-      .toContain(`sourceMappingURL=${mapUrl(tornRow.url)}`)
+      .toContain(`sourceMappingURL=${scriptMapUrl(tornRow.url)}`)
     const fallback = await routeRequest(torn.route, mapUrl(torn.service.graph().batches[0]!.url))
     expect(JSON.parse(fallback.body.toString('utf8'))).toMatchObject({
       sections: [{ map: { sources: [`/plugins/${packageName}/client.js`] } }],
@@ -611,7 +613,7 @@ describe('client bundle activation', () => {
     const batch = service.graph().batches[0]!
     const script = (await routeRequest(route, batch.url)).body.toString('utf8')
     expect(script).not.toContain('//# sourceURL=')
-    expect(script).toContain(`//# sourceMappingURL=${mapUrl(batch.url)}`)
+    expect(script).toContain(`//# sourceMappingURL=${scriptMapUrl(batch.url)}`)
     const payload = JSON.parse((await routeRequest(route, mapUrl(batch.url))).body.toString('utf8')) as {
       sections: { map: { mappings: string; sources: string[]; sourcesContent: string[] } }[]
     }
@@ -730,7 +732,7 @@ describe('client bundle activation', () => {
     const { service, route } = constructWithRoute([packageName])
     const row = service.graph().entries[0]!
     const singleScript = await routeRequest(route, row.url)
-    expect(singleScript.body.toString('utf8')).toContain(`sourceMappingURL=${mapUrl(row.url)}`)
+    expect(singleScript.body.toString('utf8')).toContain(`sourceMappingURL=${scriptMapUrl(row.url)}`)
     const singleMap = await routeRequest(route, mapUrl(row.url))
     expect(singleMap.status).toBe(200)
     expect(singleMap.headers).toEqual({
@@ -754,7 +756,7 @@ describe('client bundle activation', () => {
     const batchScript = await routeRequest(route, batch.url)
     expect(batchScript.status).toBe(200)
     expect(batchScript.headers?.['cache-control']).toBe('public, max-age=31536000, immutable')
-    expect(batchScript.body.toString('utf8')).toContain(`//# sourceMappingURL=${mapUrl(batch.url)}`)
+    expect(batchScript.body.toString('utf8')).toContain(`//# sourceMappingURL=${scriptMapUrl(batch.url)}`)
     const shellResponse = await service.fetchBundle(new Request(`dsh-app://app${batch.url}`))
     expect(shellResponse.status).toBe(200)
     expect(shellResponse.headers.get('cache-control')).toBe('public, max-age=31536000, immutable')
@@ -810,7 +812,7 @@ describe('client bundle activation', () => {
     const chunk = await routeRequest(route, url)
     expect(chunk.status).toBe(200)
     expect(chunk.body.toString('utf8')).toContain('terminal loaded')
-    expect(chunk.body.toString('utf8')).toContain(`sourceMappingURL=${url.replace('.js?', '.js.map?')}`)
+    expect(chunk.body.toString('utf8')).toContain(`sourceMappingURL=client.terminal.js.map?rev=${row.rev}`)
     expect((await routeRequest(route, url.replace('.js?', '.js.map?'))).status).toBe(200)
     expect((await routeRequest(route, url.replace(`rev=${row.rev}`, 'rev=stale'))).status).toBe(404)
   })
diff --git a/packages/host/frontend-static/src/index.ts b/packages/host/frontend-static/src/index.ts
index 3d218f31e6..d7360e63c1 100644
--- a/packages/host/frontend-static/src/index.ts
+++ b/packages/host/frontend-static/src/index.ts
@@ -114,12 +114,12 @@ export function apply(ctx: Context, config: Config): void {
   const distIndex = config.distIndex
   const distRoot = dirname(distIndex)
   // The dist is built with a relative base so the same files mount under any
-  // static directory; served pages also answer deep SPA-fallback paths, where
-  // relative asset URLs would resolve under the request directory, so the
-  // served form anchors them at the site root ahead of every URL-bearing tag.
+  // static directory. The served form must preserve that relative base:
+  // Hub-mounted pages live below /v1/web/<node>/, and an origin-root base would
+  // make asset, RPC, and plugin URLs escape the mount prefix.
   const renderIndex = async (): Promise<string> => {
     const body = ctx.webServer.renderIndex(await readFile(distIndex, 'utf8'))
-    return body.replace(/<head(?:\s[^>]*)?>/i, open => `${open}<base href="/">`)
+    return body.replace(/<head(?:\s[^>]*)?>/i, open => `${open}<base href="./">`)
   }
   ctx.effect(() => ctx.webServer.registerFallback(async (req, res) => {
     // Non-GET/HEAD without a matching named route is 405 (fallback-only
