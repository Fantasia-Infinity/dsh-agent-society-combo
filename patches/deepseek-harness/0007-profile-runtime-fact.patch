diff --git a/apps/cli/src/bin.ts b/apps/cli/src/bin.ts
index 7ca9f302e1..88d6471c06 100644
--- a/apps/cli/src/bin.ts
+++ b/apps/cli/src/bin.ts
@@ -31,6 +31,11 @@ export async function runCli(): Promise<void> {
   const version = readVersion()
   const invocation = parseDshArgs(process.argv.slice(2), version)
 
+  // Expose the selected profile to profile bundles as a launch-scoped fact.
+  // This keeps worker activation profile-local without putting
+  // AGENT_SOCIETY_WORKER=1 in the global $DSH_HOME/.env.
+  if ('profile' in invocation) process.env.DSH_PROFILE = invocation.profile
+
   switch (invocation.mode) {
     case 'profile': {
       const { runProfile } = await import('./profile-boot.ts')
