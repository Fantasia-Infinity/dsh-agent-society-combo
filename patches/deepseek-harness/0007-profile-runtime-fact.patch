diff --git a/apps/cli/src/bin.ts b/apps/cli/src/bin.ts
index 321849f2d..7fd9b9e4a 100644
--- a/apps/cli/src/bin.ts
+++ b/apps/cli/src/bin.ts
@@ -24,3 +24,9 @@
 const invocation = parseDshArgs(process.argv.slice(2), readVersion())
 
+// Expose the selected profile to profile bundles as a launch-scoped fact.
+// This lets a worker bundle stay enabled only in its own profile without
+// putting AGENT_SOCIETY_WORKER=1 in the global $DSH_HOME/.env. The value is
+// also available to config dumps, which must compose the same profile layer.
+if ('profile' in invocation) process.env.DSH_PROFILE = invocation.profile
+
 switch (invocation.mode) {
