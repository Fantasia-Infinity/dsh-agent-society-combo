diff --git a/patch-surface.snapshot.json b/patch-surface.snapshot.json
index 7235f3e..5797e19 100644
--- a/patch-surface.snapshot.json
+++ b/patch-surface.snapshot.json
@@ -77,6 +77,21 @@
       "disablesBeyondWebApp": [],
       "webAppDisablesBeyondTui": [],
       "insertsSharedWithWebApp": []
+    },
+    "0.1.5-rc.2": {
+      "disablesBeyondWebApp": [],
+      "webAppDisablesBeyondTui": [],
+      "insertsSharedWithWebApp": []
+    },
+    "0.1.6-alpha.2": {
+      "disablesBeyondWebApp": [
+        "workflow-worker-thread"
+      ],
+      "webAppDisablesBeyondTui": [
+        "tool-plugin-manager",
+        "workflow-ptc"
+      ],
+      "insertsSharedWithWebApp": []
     }
   }
 }
diff --git a/scripts/verify-alpha-source.mjs b/scripts/verify-alpha-source.mjs
index 737c250..9d78a08 100644
--- a/scripts/verify-alpha-source.mjs
+++ b/scripts/verify-alpha-source.mjs
@@ -10,7 +10,7 @@ import { spawnSync } from 'node:child_process'
 import { fileURLToPath } from 'node:url'
 import ts from 'typescript'
 
-const EXPECTED_UPSTREAM_VERSION = process.env.DSH_HARNESS_EXPECTED_VERSION ?? '0.1.5-rc.1'
+const EXPECTED_UPSTREAM_VERSION = process.env.DSH_HARNESS_EXPECTED_VERSION ?? '0.1.6-alpha.2'
 const tuiRoot = resolve(import.meta.dirname, '..')
 const sourceRoot = resolve(process.env.DSH_HARNESS_SOURCE_ROOT ?? join(tuiRoot, '../deepseek-harness'))
 const sourceManifestPath = join(sourceRoot, 'package.json')
diff --git a/scripts/verify-patch-surface.ts b/scripts/verify-patch-surface.ts
index d83fd0d..7cd7cee 100644
--- a/scripts/verify-patch-surface.ts
+++ b/scripts/verify-patch-surface.ts
@@ -157,8 +157,8 @@ const requireSourceBaseline = process.env.DSH_REQUIRE_ALPHA_BASELINE === '1'
 if (existsSync(sourceManifest) && existsSync(sourcePatch)) {
   const resolver = prepareUpstreamSourceResolver(sourceRoot)
   const source = baseline('source', sourceManifest, sourcePatch, resolver.baseUrl)
-  if (requireSourceBaseline && source.version !== '0.1.5-rc.1') {
-    throw new Error(`required source baseline is 0.1.5-rc.1, got ${source.version}`)
+  if (requireSourceBaseline && source.version !== '0.1.6-alpha.2') {
+    throw new Error(`required source baseline is 0.1.6-alpha.2, got ${source.version}`)
   }
   baselines.push(source)
 } else if (requireSourceBaseline) {
diff --git a/scripts/verify-upstream-contract.ts b/scripts/verify-upstream-contract.ts
index 1998f5f..7dda40e 100644
--- a/scripts/verify-upstream-contract.ts
+++ b/scripts/verify-upstream-contract.ts
@@ -29,7 +29,7 @@ assert.deepEqual(parseUpstreamVersion('0.1.2-alpha.3'), [0, 1, 2, 'alpha', 3])
 assert.ok(compareVersions(alpha, beta) < 0 && compareVersions(beta, rc) < 0)
 assert.ok(compareVersions(rc, parseUpstreamVersion('0.1.1-rc.2')!) > 0)
 assert.equal(parseUpstreamVersion('0.1.2'), undefined)
-assert.match(UPSTREAM_VALIDATED_LABEL, /^0\.1\.5-rc\.1/u)
+assert.match(UPSTREAM_VALIDATED_LABEL, /^0\.1\.6-alpha\.2/u)
 // The primary line must itself be a validated line. Without this, dropping
 // the primary from UPSTREAM_VALIDATED_VERSIONS still passes the prefix match
 // and upstreamDrift() (which inspects the INSTALLED versions, not the
diff --git a/scripts/verify-web-coexistence.mjs b/scripts/verify-web-coexistence.mjs
index 3e2f93f..664e6cf 100644
--- a/scripts/verify-web-coexistence.mjs
+++ b/scripts/verify-web-coexistence.mjs
@@ -42,8 +42,8 @@ const sourceBasePath = join(sourceRoot, 'packages/bundle/base/cordis.patch.yml')
 const requireSourceBaseline = process.env.DSH_REQUIRE_ALPHA_BASELINE === '1'
 if (existsSync(sourceWebPath) && existsSync(sourceWebManifest) && existsSync(sourceBasePath)) {
   const sourceWebVersion = JSON.parse(readFileSync(sourceWebManifest, 'utf8')).version
-  if (requireSourceBaseline && sourceWebVersion !== '0.1.5-rc.1') {
-    throw new Error(`required source baseline is 0.1.5-rc.1, got ${sourceWebVersion}`)
+  if (requireSourceBaseline && sourceWebVersion !== '0.1.6-alpha.2') {
+    throw new Error(`required source baseline is 0.1.6-alpha.2, got ${sourceWebVersion}`)
   }
   const resolver = prepareUpstreamSourceResolver(sourceRoot)
   baselines.push({
diff --git a/src/dsh-adapter/channel/agent-view-projection.ts b/src/dsh-adapter/channel/agent-view-projection.ts
index 2c92f81..9f6c397 100644
--- a/src/dsh-adapter/channel/agent-view-projection.ts
+++ b/src/dsh-adapter/channel/agent-view-projection.ts
@@ -117,7 +117,10 @@ export function createAgentViewProjection(
     // These process-wide roster observations still belong to this channel
     // owner. Revoke them so a retained Context cannot wake a dead projection.
     disposeStatus = ctx.on('agent/status', () => schedule())
-    disposeCreated = ctx.on('agent/created', () => notify())
+    disposeCreated = ctx.on('agent/created', () => {
+      notify()
+      return undefined
+    })
     disposeDisposed = ctx.on('agent/disposed', ({ agent }: { agent: { id?: unknown } }) => {
       folds.delete(String(agent.id ?? ''))
       backgroundHandles.delete(String(agent.id ?? ''))
diff --git a/src/dsh-adapter/contract.ts b/src/dsh-adapter/contract.ts
index fc9b82f..fe75418 100644
--- a/src/dsh-adapter/contract.ts
+++ b/src/dsh-adapter/contract.ts
@@ -2,7 +2,7 @@
  * Upstream compatibility contract.
  *
  * The TUI is validated against a set of upstream prerelease lines — the
- * current primary (0.1.5-rc.1) plus older lines kept in backward
+ * current primary (0.1.6-alpha.2) plus older lines kept in backward
  * compatibility across the 0.1.5, 0.1.3, 0.1.2, 0.1.1 and 0.1.0 release
  * families.
  * Every official package this adapter touches is blessed here; anything
@@ -17,13 +17,14 @@ import { readFileSync } from 'node:fs'
 import { fileURLToPath } from 'node:url'
 
 /** Primary validated upstream line (newest). */
-export const UPSTREAM_VALIDATED_VERSION = '0.1.5-rc.1'
+export const UPSTREAM_VALIDATED_VERSION = '0.1.6-alpha.2'
 
 /**
  * Explicitly supported upstream prerelease lines, oldest first.
  *
- * 0.1.5-rc.1 = primary continuous-CI line; 0.1.5-alpha.2/alpha.1 = mapped
- * compatibility lines (source-checked when the primary line moves);
+ * 0.1.6-alpha.2 = primary continuous-CI line; 0.1.5-rc.2/rc.1 plus
+ * 0.1.5-alpha.2/alpha.1 = mapped compatibility lines (source-checked when
+ * the primary line moves);
  * 0.1.3-alpha.2 = compatibility line (the only 0.1.3 build on npm);
  * 0.1.2-rc.1 = previous family (full CI coverage); 0.1.2-alpha.3–alpha.5 =
  * mapped compatibility lines; 0.1.1-rc.1/rc.2 = compatibility lines
@@ -48,6 +49,8 @@ export const UPSTREAM_VALIDATED_VERSIONS = [
   '0.1.5-alpha.1',
   '0.1.5-alpha.2',
   '0.1.5-rc.1',
+  '0.1.5-rc.2',
+  '0.1.6-alpha.2',
 ] as const
 
 /**
